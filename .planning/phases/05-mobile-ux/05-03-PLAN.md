---
phase: "05"
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/components/AutocompleteSuggestions.tsx
  - src/components/AddItemBar.tsx
  - src/components/AddItemBar.test.tsx
autonomous: true
requirements:
  - LIST-05
  - UX-03

must_haves:
  truths:
    - "Typing a prefix in the AddItemBar name input shows matching previously-added items in a dropdown (per D-02)"
    - "Each suggestion row shows item name (text-base) and category hint (text-sm, muted) (per D-03)"
    - "Tapping a suggestion populates name, category, and quantity fields — does NOT auto-submit (per D-04)"
    - "Escape key closes the dropdown and retains the typed text"
    - "No suggestions shown when the name input is empty"
    - "Dropdown closes on blur (clicking outside the suggestion list)"
    - "Arrow Up/Down navigates suggestions; Enter selects focused suggestion (per UI-SPEC keyboard contract)"
    - "Suggestion data comes from a one-time Supabase query on mount scoped to list_id (per D-01)"
    - "Duplicate item names deduplicated client-side — only most-recent entry appears per unique name"
    - "Adding an item via autocomplete still requires Add button or Enter (2-tap path maintained per D-06)"
    - "All 5 LIST-05 test stubs from Plan 01 now pass"
  artifacts:
    - path: "src/components/AutocompleteSuggestions.tsx"
      provides: "Dropdown listbox component with ARIA roles, 44px items, onMouseDown guard"
      exports: ["AutocompleteSuggestions"]
    - path: "src/components/AddItemBar.tsx"
      provides: "Autocomplete state wiring, Supabase fetch on mount, keyboard handler, ARIA combobox on name input"
      contains: "role=\"combobox\""
  key_links:
    - from: "src/components/AddItemBar.tsx"
      to: "src/components/AutocompleteSuggestions.tsx"
      via: "import and conditional render below name input"
      pattern: "AutocompleteSuggestions"
    - from: "src/components/AddItemBar.tsx"
      to: "src/lib/supabase"
      via: "useEffect loadDistinctItems fetch"
      pattern: "supabase\\.from\\('items'\\)"
---

<objective>
Wave 2: Autocomplete suggestions for the name input in AddItemBar (LIST-05, UX-03).

This plan builds the autocomplete feature from scratch. It creates a new AutocompleteSuggestions component and wires it into the existing AddItemBar. All locked decisions (D-01 through D-04) are implemented exactly as specified.

The autocomplete uses:
- A one-time Supabase query on mount (D-01): `supabase.from('items').select('name, category, quantity').eq('list_id', listId).order('created_at', { ascending: false })` with client-side dedup by lowercase name
- Local prefix filter (D-02): no server query per keystroke, no debounce needed (per UI-SPEC Claude's Discretion resolution)
- Top 5-8 matches in a dropdown (D-03): each showing name + category badge
- Populate-on-select, no auto-submit (D-04): fields populated, user confirms with Add button or Enter
- Dismiss on blur, Escape, or selection (per UI-SPEC Claude's Discretion resolution)

Purpose: Allow the user to tap a previously-added item name and have name/category/quantity pre-filled, reducing typing on repeat grocery trips.
Output: AutocompleteSuggestions.tsx (new), AddItemBar.tsx (extended with autocomplete), AddItemBar.test.tsx (5 LIST-05 stubs filled).
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/PROJECT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-CONTEXT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-RESEARCH.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-PATTERNS.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-UI-SPEC.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AutocompleteSuggestions.tsx component</name>
  <files>src/components/AutocompleteSuggestions.tsx</files>
  <read_first>
    - src/components/ItemRow.tsx — lines 1-17 (imports pattern: cn, lucide, types) and lines 185, 203, 211 (onMouseDown={(e) => e.preventDefault()} focus-scope pattern — the single most critical pattern for this component)
    - src/components/AttributionBadge.tsx — minimal display component structure (minimal props + return JSX)
    - src/lib/utils.ts — cn() utility import and usage
    - .planning/phases/05-mobile-ux/05-PATTERNS.md §AutocompleteSuggestions.tsx section — full component interface, ARIA listbox pattern, cn() usage for aria-selected highlight
    - .planning/phases/05-mobile-ux/05-UI-SPEC.md §Component Inventory / Autocomplete Suggestion Dropdown — exact className values, z-index, max-h, separator rule
    - .planning/phases/05-mobile-ux/05-RESEARCH.md §Pattern 2 — ARIA combobox listbox markup with IDs
  </read_first>
  <behavior>
    The component receives: suggestions (array of {name, category|null, quantity|null}), focusedIndex (number, -1 = none focused), onSelect (callback)
    Renders: a `<ul role="listbox" id="autocomplete-listbox">` absolutely positioned, z-50, full width of parent, max-h-[280px] overflow-y-auto, rounded-lg border border-border bg-popover shadow-md
    Each `<li>`: role="option", id="suggestion-{index}", aria-selected={index===focusedIndex}, min-h-[44px], flex row with item.name (text-base flex-1) + item.category (text-sm text-muted-foreground, only if set), border-b border-border except last item (last:border-b-0)
    Hover/focus state: bg-accent text-accent-foreground via cn() conditional on aria-selected or index===focusedIndex
    CRITICAL: onMouseDown={(e) => e.preventDefault()} on every `<li>` — prevents blur on input before onClick fires
    onClick on each `<li>`: calls onSelect(item)
  </behavior>
  <action>
    Create src/components/AutocompleteSuggestions.tsx.

    Type definition for the suggestion item (define once at top of file, re-use in interface):
    `type SuggestionItem = { name: string; category: string | null; quantity: string | null }`

    Component interface:
    ```
    interface AutocompleteSuggestionsProps {
      suggestions: SuggestionItem[]
      focusedIndex: number
      onSelect: (item: SuggestionItem) => void
    }
    ```

    The component is a named export: `export function AutocompleteSuggestions(...)`.

    Key implementation details:
    - The `<ul>` is positioned `absolute left-0 right-0` — it positions relative to the parent container in AddItemBar (which will have `relative` positioning). Do NOT use `position: fixed`.
    - The `<ul>` has className: `absolute left-0 right-0 z-50 mt-1 max-h-[280px] overflow-y-auto rounded-lg border border-border bg-popover shadow-md`
    - Each `<li>` uses `cn()` to conditionally apply `bg-accent text-accent-foreground` when `index === focusedIndex`
    - Base `<li>` className: `flex min-h-[44px] cursor-pointer items-center gap-2 border-b border-border px-3 py-2 last:border-b-0 hover:bg-accent hover:text-accent-foreground`
    - Both `onMouseDown={(e) => e.preventDefault()}` AND `onClick={() => onSelect(item)}` on each `<li>`
    - item.name renders as `<span className="flex-1 text-base">{item.name}</span>`
    - item.category (when non-null): `<span className="text-sm text-muted-foreground">{item.category}</span>`
    - No dangerouslySetInnerHTML anywhere

    Imports needed: `cn` from `@/lib/utils`, nothing else (no lucide icons, no shadcn primitives).
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx tsc --noEmit 2>&1 | grep AutocompleteSuggestions || echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AutocompleteSuggestions.tsx exists and exports `AutocompleteSuggestions`
    - Contains `role="listbox"` on the `<ul>` and `role="option"` on each `<li>`
    - Contains `id="autocomplete-listbox"` on the `<ul>`
    - Every `<li>` has `onMouseDown={(e) => e.preventDefault()}`
    - TypeScript compiles without errors for this file (`npx tsc --noEmit` shows no AutocompleteSuggestions errors)
    - No `dangerouslySetInnerHTML` usage anywhere in the file
  </acceptance_criteria>
  <done>AutocompleteSuggestions.tsx created, exports the component, TypeScript clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire autocomplete into AddItemBar and fill LIST-05 test stubs</name>
  <files>src/components/AddItemBar.tsx, src/components/AddItemBar.test.tsx</files>
  <read_first>
    - src/components/AddItemBar.tsx — full file (read the current state after Plan 02 edits: h-11 SelectTrigger and min-h-[44px] toggle are already applied; this task adds autocomplete ON TOP of those changes)
    - src/components/AddItemBar.test.tsx — full file (read the stubs created in Plan 01, filled partially in Plan 02; find the 5 LIST-05 it.todo stubs to fill in)
    - src/components/AutocompleteSuggestions.tsx — the component just created in Task 1 of this plan
    - src/stores/itemsStore.ts — lines 51-71 (fetchItems Supabase query pattern to mirror for loadDistinctItems)
    - .planning/phases/05-mobile-ux/05-PATTERNS.md §AddItemBar.tsx section — exact state additions, handleNameChange, handleKeyDown, handleSuggestionSelect, dropdown positioning with relative parent, ARIA combobox attributes on Input
    - .planning/phases/05-mobile-ux/05-RESEARCH.md §Code Examples — all three handlers (keyboard nav, suggestion selection, Supabase fetch)
    - .planning/phases/05-mobile-ux/05-RESEARCH.md §Pattern 1 — loadDistinctItems useEffect, dedup logic
    - .planning/phases/05-mobile-ux/05-RESEARCH.md §Common Pitfalls — especially Pitfall 1 (onMouseDown), Pitfall 4 (PostgREST DISTINCT)
  </read_first>
  <behavior>
    Test: "shows suggestions after typing prefix" — set mockSuggestionData=[{name:'Milk',category:'Dairy',quantity:'2'}], render AddItemBar, type 'mi' in name input, await suggestion item with text 'Milk' to appear (role="option" or text content)
    Test: "populates name/category/quantity on selection" — after suggestion appears, click it, assert name input value is 'Milk', category Select shows 'Dairy', quantity input shows '2' (expand more details to check those)
    Test: "does NOT auto-submit on selection (D-04)" — after clicking suggestion, mockAddItem must not have been called (verify with expect(mockAddItem).not.toHaveBeenCalled())
    Test: "Escape key dismisses dropdown" — type prefix, await suggestion, press Escape, assert suggestion items no longer in document; name input still has typed text
    Test: "no suggestions when input is empty" — with mockSuggestionData populated, render, do not type anything, assert no role="listbox" in document
  </behavior>
  <action>
    AddItemBar.tsx modifications:

    1. Extend imports: add `useEffect, useRef` to the React import (currently only `useState` is imported). Add `import { supabase } from '@/lib/supabase'`. Add `import { AutocompleteSuggestions } from '@/components/AutocompleteSuggestions'`.

    2. Add three new state variables after line 40 (after `const [submitting, setSubmitting] = useState(false)`):
       ```
       const [distinctItems, setDistinctItems] = useState<Array<{name: string; category: string | null; quantity: string | null}>>([])
       const [suggestions, setSuggestions] = useState<Array<{name: string; category: string | null; quantity: string | null}>>([])
       const [focusedIndex, setFocusedIndex] = useState(-1)
       ```

    3. After the state declarations, add the loadDistinctItems useEffect:
       - Calls `supabase.from('items').select('name, category, quantity').eq('list_id', listId).order('created_at', { ascending: false })`
       - On success, deduplicates by lowercase name using a Set (keeping first = most recent)
       - Calls setDistinctItems with the deduped result
       - Dependency array: [listId]

    4. Add handleNameChange function (replaces the direct setName call on the Input):
       - Sets name state
       - If value.trim().length === 0: clears suggestions, returns
       - Otherwise: filters distinctItems by case-insensitive prefix match on item.name.toLowerCase().startsWith(value.toLowerCase()), slices to 8, sets suggestions

    5. Add handleKeyDown function for keyboard navigation:
       - ArrowDown: increment focusedIndex (wraps to 0 at end)
       - ArrowUp: decrement focusedIndex (wraps to last at start)
       - Enter when focusedIndex >= 0 AND suggestions.length > 0: call handleSuggestionSelect(suggestions[focusedIndex]), e.preventDefault()
       - Escape: setSuggestions([]), setFocusedIndex(-1), e.preventDefault()
       - All other keys: pass through (do NOT intercept Enter when dropdown is closed — the form submit must still work)

    6. Add handleSuggestionSelect function (per D-04 — NO auto-submit):
       - setName(item.name)
       - If item.category: setCategory(item.category)
       - If item.quantity: setQuantity(item.quantity)
       - setSuggestions([]), setFocusedIndex(-1)
       - MUST NOT call handleSubmit

    7. Wrap the existing `<div className="flex items-center gap-2">` (the name input + Add button row) in a `<div className="relative">` container. This gives AutocompleteSuggestions its positioning context.

    8. Modify the `<Input>` for the name field:
       - Change `onChange={(e) => setName(e.target.value)}` to `onChange={(e) => handleNameChange(e.target.value)}`
       - Add `onKeyDown={handleKeyDown}`
       - Add `onBlur={() => { setSuggestions([]); setFocusedIndex(-1) }}`
       - Add `role="combobox"`, `aria-expanded={suggestions.length > 0}`, `aria-controls="autocomplete-listbox"`, `aria-autocomplete="list"`
       - Add `aria-activedescendant={focusedIndex >= 0 ? \`suggestion-\${focusedIndex}\` : undefined}`
       - Keep all existing props: type, value, placeholder, disabled, className

    9. Below the `<Input>` and before the Add `<button>`, add conditional render:
       `{suggestions.length > 0 && <AutocompleteSuggestions suggestions={suggestions} focusedIndex={focusedIndex} onSelect={handleSuggestionSelect} />}`

    10. Also clear suggestions after successful form submit: add `setSuggestions([])` and `setFocusedIndex(-1)` to the success block in handleSubmit (after setExpanded(false)).

    AddItemBar.test.tsx modifications:
    Fill the 5 LIST-05 it.todo() stubs with real implementations using the behavioral descriptions above. Use userEvent.setup() for interactions (type, click, keyboard). Use waitFor or findBy* for async Supabase mock resolution. Do not remove or modify the UX-02 tests added in Plan 02.
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run src/components/AddItemBar.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "role=\"combobox\"" src/components/AddItemBar.tsx` returns 1 match on the name Input
    - `grep "AutocompleteSuggestions" src/components/AddItemBar.tsx` returns at least 2 matches (import + render)
    - `grep "handleSuggestionSelect\|handleNameChange\|handleKeyDown" src/components/AddItemBar.tsx` returns matches for all three handlers
    - `grep "loadDistinctItems\|useEffect" src/components/AddItemBar.tsx` returns matches (Supabase fetch on mount)
    - `grep "handleSubmit\|addItem" src/components/AddItemBar.tsx` in handleSuggestionSelect context: handleSuggestionSelect does NOT call handleSubmit (grep handleSuggestionSelect block — no addItem call within it)
    - All 5 LIST-05 tests in AddItemBar.test.tsx pass (not todo)
    - `npx vitest run` exits 0 across the full suite
    - TypeScript compiles clean: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Autocomplete feature fully wired: suggestions appear on prefix, populate fields on selection, Escape/blur dismiss dropdown, Enter navigates, no auto-submit; all LIST-05 tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → AddItemBar (autocomplete fetch) | Read-only query; response data is displayed in React JSX |
| User input → suggestion filter | Input value used as prefix filter locally; never sent to DB per keystroke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering / XSS | AutocompleteSuggestions display | mitigate | All suggestion text rendered via React JSX (`{item.name}`, `{item.category}`) — React escapes HTML entities by default. No `dangerouslySetInnerHTML`. |
| T-05-03-02 | Information Disclosure | Supabase distinct-names query | mitigate | Query uses `.eq('list_id', listId)` — scoped to the current list. Supabase RLS (established Phase 1) restricts rows to the list ID in the URL. No cross-list data leakage. |
| T-05-03-03 | Tampering | Suggestion selection → form state | accept | Selecting a suggestion only sets local React state (name, category, quantity). The user must explicitly submit with the Add button or Enter. No server write on selection. |
| T-05-03-SC | Tampering | No new npm packages | accept | No package-manager installs in this plan; no slopcheck needed. |
</threat_model>

<verification>
- `npx vitest run` — full suite exits 0, all LIST-05 and UX-02 tests pass
- `npx tsc --noEmit` — exits 0, no type errors
- Manual smoke: run `npx vite` (dev server), navigate to a list with items, type a prefix in the name input — suggestions appear. Tap suggestion — name/category/quantity populate, Add button not auto-triggered. Press Escape — dropdown closes. Type in empty input — no dropdown.
</verification>

<success_criteria>
- AutocompleteSuggestions.tsx created with correct ARIA markup and onMouseDown guard
- AddItemBar.tsx extended with local state, Supabase fetch on mount, prefix filter, keyboard nav, and suggestion selection (no auto-submit)
- 5 LIST-05 test stubs filled and passing
- Full test suite remains green (78+ tests, 0 failures)
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/05-mobile-ux/05-03-SUMMARY.md` when done
</output>
