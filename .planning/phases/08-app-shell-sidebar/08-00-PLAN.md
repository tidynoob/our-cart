---
phase: 08-app-shell-sidebar
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/AppShell.test.tsx
  - src/components/Sidebar.test.tsx
  - src/router.test.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "AppShell.test.tsx exists with failing stubs covering: Menu trigger opens drawer (NAV-01), fetchLists called on mount when lists empty (NAV-01), fetchLists NOT called when lists already populated (NAV-01)"
    - "Sidebar.test.tsx exists with failing stubs covering: lists render in drawer (NAV-01), active list has aria-current='page' (NAV-02), active list has bg-sidebar-accent class (NAV-02), clicking a row navigates and closes sidebar (D-08)"
    - "router.test.tsx exists with a failing stub asserting AppShell is nested under ProtectedRoute (D-01)"
    - "All new stubs are intentionally RED — the implementation files do not exist yet"
  artifacts:
    - path: "src/components/AppShell.test.tsx"
      provides: "NAV-01 AppShell unit test stubs"
      min_lines: 60
    - path: "src/components/Sidebar.test.tsx"
      provides: "NAV-01+NAV-02+D-08 Sidebar unit test stubs"
      min_lines: 80
    - path: "src/router.test.tsx"
      provides: "D-01 router nesting assertion stub"
      min_lines: 30
  key_links:
    - from: "src/components/AppShell.test.tsx"
      to: "src/components/AppShell.tsx"
      via: "import AppShell from '@/components/AppShell'"
      pattern: "AppShell"
    - from: "src/components/Sidebar.test.tsx"
      to: "src/components/Sidebar.tsx"
      via: "import Sidebar from '@/components/Sidebar'"
      pattern: "Sidebar"
    - from: "src/router.test.tsx"
      to: "src/router.tsx"
      via: "import { router } from '@/router'"
      pattern: "router"
---

<objective>
Create Wave 0 test scaffolds for Phase 8 so every subsequent implementation task has automated verify commands that will turn green once the code exists.

Purpose: The Nyquist contract requires every behavioral task to have a pre-existing test. Writing RED stubs now establishes the verification harness before AppShell and Sidebar are built.

Output:
- src/components/AppShell.test.tsx — 4 failing unit stubs (open trigger, fetchLists guard behaviors)
- src/components/Sidebar.test.tsx — 4 failing unit stubs (list rendering, active highlight, aria-current, navigate+close)
- src/router.test.tsx — 1 failing stub (AppShell nested under ProtectedRoute in router tree)
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/ROADMAP.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/08-app-shell-sidebar/08-VALIDATION.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/08-app-shell-sidebar/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppShell.test.tsx with 4 failing stubs</name>
  <files>src/components/AppShell.test.tsx</files>
  <read_first>
    - src/components/auth/ProtectedRoute.test.tsx — analog: full file; extract the vi.mock('@/lib/supabase') shape, useAuthStore.setState pattern, MemoryRouter + nested Routes render helper, describe/it/expect structure
    - src/stores/listsStore.test.ts — analog: vi.hoisted mock shape for supabase.from (lines 4-29); copy the mockFrom/mockSelect/mockEq/mockOrder block and merge with the auth mock shape so AppShell.test.tsx has a single unified supabase mock
    - .planning/phases/08-app-shell-sidebar/08-VALIDATION.md — Per-Task Verification Map rows 08-NAV-01b/c/d; confirm exact 4 behaviors to stub
    - .planning/phases/08-app-shell-sidebar/08-PATTERNS.md — "src/components/AppShell.test.tsx" section: renderAtRoute helper nesting AppShell inside ProtectedRoute, beforeEach setState pattern for both stores
  </read_first>
  <action>
    Create src/components/AppShell.test.tsx from scratch.

    Imports: describe, it, expect, vi, beforeEach from 'vitest'; render, screen, fireEvent, waitFor from '@testing-library/react'; MemoryRouter, Routes, Route from 'react-router-dom'; AppShell from '@/components/AppShell'; useAuthStore from '@/stores/authStore'; useListsStore from '@/stores/listsStore'.

    Supabase mock (vi.hoisted pattern — merge auth + from shapes):
    - Declare mockOrder = vi.fn().mockResolvedValue({ data: [], error: null })
    - Declare mockEq = vi.fn().mockReturnThis()
    - Declare mockSelect = vi.fn().mockReturnThis()
    - Declare mockFrom = vi.fn().mockReturnValue({ select: mockSelect, eq: mockEq, order: mockOrder })
    - vi.mock('@/lib/supabase', () => ({ supabase: { from: mockFrom, auth: { onAuthStateChange: vi.fn().mockReturnValue({ data: { subscription: { unsubscribe: vi.fn() } } }) }, realtime: { setAuth: vi.fn() } } }))

    beforeEach:
    - vi.clearAllMocks()
    - useAuthStore.setState({ user: { id: 'u1' } as never, isLoading: false, session: null, error: null })
    - useListsStore.setState({ lists: [], loading: false, error: null, fetchLists: vi.fn(), createList: vi.fn(), renameList: vi.fn(), deleteList: vi.fn() } as never)

    renderAtRoute helper — nests AppShell correctly inside ProtectedRoute at path '/list/:code':
    - MemoryRouter initialEntries=['/list/abc123']
    - Routes > Route element={ProtectedRoute} > Route element={AppShell} > Route path="/list/:code" element={div with text "List Content"}
    - Note: import ProtectedRoute from '@/components/auth/ProtectedRoute'

    Write 4 it() stubs inside describe('AppShell'):

    Stub 1 (NAV-01b): 'renders Menu trigger button'
    - renderAtRoute('/list/abc123')
    - expect(screen.getByRole('button', { name: 'Open navigation' })).toBeInTheDocument()

    Stub 2 (NAV-01b): 'clicking Menu trigger opens the sidebar drawer'
    - renderAtRoute('/list/abc123')
    - fireEvent.click(screen.getByRole('button', { name: 'Open navigation' }))
    - await waitFor(() => expect(screen.getByRole('dialog')).toBeInTheDocument())

    Stub 3 (NAV-01c): 'calls fetchLists on mount when lists is empty'
    - Spy on fetchLists: const fetchSpy = vi.fn(); useListsStore.setState({ lists: [], ..., fetchLists: fetchSpy } as never)
    - renderAtRoute('/list/abc123')
    - await waitFor(() => expect(fetchSpy).toHaveBeenCalledWith('u1'))

    Stub 4 (NAV-01d): 'does NOT call fetchLists when lists is already populated'
    - const fetchSpy = vi.fn(); useListsStore.setState({ lists: [{ id: 'l1', name: 'G', share_code: 'abc123', owner_id: 'u1', created_at: '' }], loading: false, error: null, fetchLists: fetchSpy } as never)
    - renderAtRoute('/list/abc123')
    - await new Promise(r => setTimeout(r, 50))
    - expect(fetchSpy).not.toHaveBeenCalled()

    All stubs will fail with "Cannot find module '@/components/AppShell'" until Plan 01 creates it.
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run src/components/AppShell.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AppShell.test.tsx exists and is valid TypeScript (no syntax errors visible)
    - File contains exactly 4 it() stubs inside a describe('AppShell') block
    - vi.hoisted block exports mockFrom, mockSelect, mockEq, mockOrder
    - vi.mock('@/lib/supabase') declaration merges auth and from shapes
    - beforeEach resets both useAuthStore and useListsStore state
    - renderAtRoute helper nests Route(AppShell) inside Route(ProtectedRoute) with '/list/:code' child
    - npx vitest run src/components/AppShell.test.tsx exits with error "Cannot find module '@/components/AppShell'" or equivalent (RED because AppShell.tsx not yet created) — NOT a Vitest config or syntax error
  </acceptance_criteria>
  <done>AppShell.test.tsx exists with 4 failing stubs; all fail on missing module, not on test infrastructure</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar.test.tsx and router.test.tsx with failing stubs</name>
  <files>src/components/Sidebar.test.tsx, src/router.test.tsx</files>
  <read_first>
    - src/components/auth/ProtectedRoute.test.tsx — full file; extract MemoryRouter render pattern, vi.mock supabase shape
    - src/stores/listsStore.test.ts — vi.hoisted + vi.mock('@/lib/supabase') pattern (lines 4-29); Sidebar.test.tsx will need the same supabase mock shape
    - .planning/phases/08-app-shell-sidebar/08-PATTERNS.md — "src/components/Sidebar.test.tsx" section: vi.mock('react-router-dom') for useMatch + useNavigate, useListsStore.setState injection, portal query pattern (getByRole('dialog') / document.body.querySelector)
    - .planning/phases/08-app-shell-sidebar/08-VALIDATION.md — Per-Task Verification Map rows 08-NAV-01a, 08-NAV-02a, 08-NAV-02b, 08-D08, 08-D01; confirm exact behaviors
    - src/router.tsx — read the full file to understand current structure before writing router.test.tsx
  </read_first>
  <action>
    Create TWO files.

    === FILE 1: src/components/Sidebar.test.tsx ===

    Imports: describe, it, expect, vi, beforeEach from 'vitest'; render, screen, fireEvent from '@testing-library/react'; MemoryRouter, Routes, Route from 'react-router-dom'; Sidebar from '@/components/Sidebar'; useListsStore from '@/stores/listsStore'; useMatch, useNavigate from 'react-router-dom' (for mock assertions).

    Mock react-router-dom (preserve actual implementation, override only hooks used by Sidebar):
    - vi.mock('react-router-dom', async () => { const actual = await vi.importActual('react-router-dom'); return { ...actual, useMatch: vi.fn(), useNavigate: vi.fn().mockReturnValue(vi.fn()) } })

    Supabase mock (same vi.hoisted shape as AppShell.test.tsx — auth + from combined):
    - vi.mock('@/lib/supabase', () => ({ supabase: { from: vi.fn(), auth: { onAuthStateChange: vi.fn().mockReturnValue({ data: { subscription: { unsubscribe: vi.fn() } } }) }, realtime: { setAuth: vi.fn() } } }))

    sampleLists constant: [{ id: 'l1', name: 'Groceries', share_code: 'abc123', owner_id: 'u1', created_at: '' }, { id: 'l2', name: 'Hardware', share_code: 'def456', owner_id: 'u1', created_at: '' }]

    beforeEach:
    - vi.clearAllMocks()
    - useListsStore.setState({ lists: sampleLists, loading: false, error: null } as never)
    - (useMatch as ReturnType<typeof vi.fn>).mockReturnValue(null)

    renderOpenSidebar helper — renders Sidebar with open=true, onOpenChange=vi.fn(), lists=sampleLists inside MemoryRouter:
    - render(<MemoryRouter initialEntries={['/list/abc123']}><Sidebar open={true} onOpenChange={vi.fn()} lists={sampleLists} /></MemoryRouter>)

    Write 4 it() stubs inside describe('Sidebar'):

    Stub 1 (NAV-01a): 'renders all list names when open'
    - renderOpenSidebar()
    - expect(screen.getByText('Groceries')).toBeInTheDocument()
    - expect(screen.getByText('Hardware')).toBeInTheDocument()

    Stub 2 (NAV-02a): 'active list row has aria-current="page"'
    - (useMatch as ReturnType<typeof vi.fn>).mockReturnValue({ params: { code: 'abc123' } })
    - renderOpenSidebar()
    - const activeLink = screen.getByRole('link', { name: 'Groceries' })
    - expect(activeLink).toHaveAttribute('aria-current', 'page')

    Stub 3 (NAV-02b): 'active list row has visual-distinct class bg-sidebar-accent'
    - (useMatch as ReturnType<typeof vi.fn>).mockReturnValue({ params: { code: 'abc123' } })
    - renderOpenSidebar()
    - const activeLink = screen.getByRole('link', { name: 'Groceries' })
    - expect(activeLink.className).toMatch(/bg-sidebar-accent/)
    - expect(activeLink.className).toMatch(/font-semibold/)

    Stub 4 (D-08): 'clicking a list row calls onOpenChange(false)'
    - const onOpenChange = vi.fn()
    - render(<MemoryRouter><Sidebar open={true} onOpenChange={onOpenChange} lists={sampleLists} /></MemoryRouter>)
    - fireEvent.click(screen.getByRole('link', { name: 'Groceries' }))
    - expect(onOpenChange).toHaveBeenCalledWith(false)

    === FILE 2: src/router.test.tsx ===

    This test asserts the router tree structure — AppShell is nested inside ProtectedRoute for /list/:code routes.

    Imports: describe, it, expect, vi from 'vitest'; render, screen from '@testing-library/react'; createMemoryRouter, RouterProvider from 'react-router-dom'.

    Mock the page/layout components so the test doesn't need real implementations:
    - vi.mock('@/components/auth/ProtectedRoute', () => ({ default: () => <div data-testid="protected-route">ProtectedRoute</div> }))
    - vi.mock('@/components/AppShell', () => ({ default: () => <div data-testid="app-shell">AppShell</div> }))
    - vi.mock('@/pages/ListPage', () => ({ default: () => <div data-testid="list-page">ListPage</div> }))
    - vi.mock('@/pages/LandingPage', () => ({ default: () => <div data-testid="landing-page">LandingPage</div> }))
    - vi.mock('@/pages/NotFoundPage', () => ({ default: () => <div data-testid="not-found">NotFound</div> }))
    - vi.mock('@/lib/supabase', () => ({ supabase: { auth: { onAuthStateChange: vi.fn().mockReturnValue({ data: { subscription: { unsubscribe: vi.fn() } } }) }, realtime: { setAuth: vi.fn() } } }))

    Import router from '@/router' (the actual router instance).

    Stub 1 (D-01): 'visiting /list/:code passes through ProtectedRoute and AppShell'
    - This is intentionally a stub — the actual router uses real component trees. For now: import { router } from '@/router'; assert that router exists and the routes array structure is correct without rendering.
    - A lighter approach: check that the routes config includes an AppShell element by inspecting router.routes[1].children[0].element type. Use: import AppShell from '@/components/AppShell'; then import { router } from '@/router'; then expect(router.routes).toBeDefined()
    - Keep this minimal — the full integration renders too deep. The stub just confirms the module imports without error and the routes array has the expected nesting depth (router.routes[1].children[0].children[0].path === '/list/:code').

    Note: router.test.tsx stubs will fail with module import errors (AppShell.tsx not yet created) — that is the correct RED state.
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run src/components/Sidebar.test.tsx src/router.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Sidebar.test.tsx exists with 4 it() stubs (list rendering, aria-current, bg-sidebar-accent class, onOpenChange(false) on click)
    - vi.mock('react-router-dom') in Sidebar.test.tsx overrides useMatch and useNavigate while preserving actual MemoryRouter/Routes/Route
    - src/router.test.tsx exists with 1 stub asserting router routes structure
    - Both files fail with "Cannot find module" errors (expected RED for AppShell.tsx/Sidebar.tsx) — not with syntax/infrastructure errors
    - No existing test files modified by this task
  </acceptance_criteria>
  <done>Sidebar.test.tsx (4 stubs) and router.test.tsx (1 stub) exist and are intentionally RED pending Wave 1 implementation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ store | Tests mock Supabase entirely — no real network calls; store state injected via setState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — all test infrastructure (vitest, testing-library, jsdom) already present from prior phases |
</threat_model>

<verification>
After both tasks complete:
- src/components/AppShell.test.tsx exists with 4 stubs
- src/components/Sidebar.test.tsx exists with 4 stubs
- src/router.test.tsx exists with 1 stub
- npx vitest run src/components/AppShell.test.tsx src/components/Sidebar.test.tsx src/router.test.tsx exits with module-not-found errors (RED) — not config or syntax errors
- No existing tests broken
</verification>

<success_criteria>
Wave 0 complete when:
1. All 3 test files exist and are syntactically valid TypeScript
2. AppShell.test.tsx: 4 stubs covering open trigger, fetchLists guard (empty → called, populated → not called)
3. Sidebar.test.tsx: 4 stubs covering list rendering, aria-current, bg-sidebar-accent, onOpenChange(false)
4. router.test.tsx: 1 stub asserting router routes shape
5. All stubs RED because AppShell.tsx and Sidebar.tsx do not yet exist
</success_criteria>

<output>
Create .planning/phases/08-app-shell-sidebar/08-00-SUMMARY.md when done
</output>
