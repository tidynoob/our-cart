---
phase: 07-lists-infrastructure
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/stores/listsStore.test.ts
  - src/pages/LandingPage.test.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03

must_haves:
  truths:
    - "listsStore.test.ts exists with 7 failing unit stubs covering create+rollback, fetch owner-scoped, rename+rollback, delete+rollback (LIST-01/02/03)"
    - "LandingPage.test.tsx has two new tests: dialog renders 'and all its items' copy (LIST-03); cancel on dialog leaves list intact"
    - "All new test stubs fail (RED) because the implementation does not exist yet — no false-positive green"
    - "npx vitest run exits non-zero (expected — tests are red stubs)"
  artifacts:
    - path: "src/stores/listsStore.test.ts"
      provides: "Unit test stubs for listsStore (7 behaviors)"
      min_lines: 80
    - path: "src/pages/LandingPage.test.tsx"
      provides: "Updated suite with LIST-03 dialog tests"
  key_links:
    - from: "src/stores/listsStore.test.ts"
      to: "src/stores/listsStore.ts"
      via: "vi.mock('@/lib/supabase') + useListsStore.setState"
      pattern: "useListsStore"
    - from: "src/pages/LandingPage.test.tsx"
      to: "src/pages/LandingPage.tsx"
      via: "@testing-library/react render"
      pattern: "LandingPage"
---

<objective>
Create Wave 0 test scaffolds for Phase 7 so every subsequent implementation task has an automated verify command that can turn green.

Purpose: The Nyquist contract requires all 7 listsStore unit behaviors to be covered by tests and the LandingPage dialog behavior (LIST-03) to be tested. Writing stubs first establishes the RED state that implementation turns GREEN.

Output:
- src/stores/listsStore.test.ts — 7 failing unit stubs (LIST-01/02/03), chainable Supabase builder mock, useListsStore.setState reset in beforeEach
- src/pages/LandingPage.test.tsx — 2 new tests added: delete dialog renders "and all its items" copy; cancel closes dialog without calling deleteList
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/PROJECT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/ROADMAP.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/07-lists-infrastructure/07-PATTERNS.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/07-lists-infrastructure/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create listsStore.test.ts with 7 failing stubs</name>
  <files>src/stores/listsStore.test.ts</files>
  <read_first>
    - src/stores/authStore.test.ts — analog: vi.hoisted mock setup skeleton (lines 1-51), vi.clearAllMocks in beforeEach, store.setState reset, getState().action() call pattern
    - .planning/phases/07-lists-infrastructure/07-PATTERNS.md — "src/stores/listsStore.test.ts" section: full chainable builder mock block (mockFrom / mockSelect / mockInsert / mockUpdate / mockDelete / mockEq / mockOrder / mockSingle), beforeEach setState reset
    - .planning/phases/07-lists-infrastructure/07-VALIDATION.md — Per-Task Verification Map: exact 7 test names and which requirement each covers
  </read_first>
  <action>
    Create src/stores/listsStore.test.ts using the exact structure from 07-PATTERNS.md.

    Mock setup (vi.hoisted block):
    - Declare mockSingle, mockOrder, mockEq, mockSelect, mockInsert, mockUpdate, mockDelete, each as vi.fn() with .mockReturnThis() EXCEPT mockSingle (no .mockReturnThis — it is the terminal call)
    - mockFrom = vi.fn().mockReturnValue({ select, insert, update, delete: mockDelete, eq, order, single }) — use the key "delete" (reserved word; quote it in the object)
    - vi.mock('@/lib/supabase', () => ({ supabase: { from: mockFrom } }))

    beforeEach:
    - vi.clearAllMocks()
    - useListsStore.setState({ lists: [], loading: false, error: null })

    Tests to write (all start as failing stubs — the store file does not exist yet, so the import will throw; wrap each body in it.todo or write expect().toBe() that will fail correctly once the store exists):
    Use it('...', async () => { ... }) with concrete assertions where possible. Each test sets up the mock return, calls the store action, then asserts state. The tests will fail at import until listsStore.ts is created.

    Test 1 (LIST-01): 'fetchLists queries supabase.from("lists") with owner_id filter'
    - mockEq.mockResolvedValueOnce({ data: [{ id: 'l1', name: 'Groceries', share_code: 'abc12345', owner_id: 'u1', created_at: '' }], error: null })
    - await useListsStore.getState().fetchLists('u1')
    - expect(mockFrom).toHaveBeenCalledWith('lists')
    - expect(mockEq).toHaveBeenCalledWith('owner_id', 'u1')
    - expect(useListsStore.getState().lists).toHaveLength(1)

    Test 2 (LIST-01): 'createList inserts with owner_id and share_code, replaces optimistic row on success'
    - mockSingle.mockResolvedValueOnce({ data: { id: 'real-id', name: 'Groceries', share_code: 'abc12345', owner_id: 'u1', created_at: '' }, error: null })
    - await useListsStore.getState().createList('Groceries', 'u1')
    - expect(mockFrom).toHaveBeenCalledWith('lists')
    - expect(mockInsert).toHaveBeenCalledWith(expect.objectContaining({ owner_id: 'u1', name: 'Groceries' }))
    - expect(useListsStore.getState().lists[0].id).toBe('real-id')

    Test 3 (LIST-01): 'createList rolls back optimistic row on supabase error'
    - mockSingle.mockResolvedValueOnce({ data: null, error: { message: 'fail' } })
    - await useListsStore.getState().createList('Groceries', 'u1')
    - expect(useListsStore.getState().lists).toHaveLength(0)
    - expect(useListsStore.getState().error).toBe('Failed to create list')

    Test 4 (LIST-02): 'renameList applies optimistic name update before DB call'
    - Pre-seed: useListsStore.setState({ lists: [{ id: 'l1', name: 'Old', share_code: 'abc12345', owner_id: 'u1', created_at: '' }], loading: false, error: null })
    - mockEq.mockResolvedValueOnce({ error: null })
    - await useListsStore.getState().renameList('l1', 'New Name')
    - expect(useListsStore.getState().lists[0].name).toBe('New Name')

    Test 5 (LIST-02): 'renameList rolls back to previous name on supabase error'
    - Pre-seed same list as test 4
    - mockEq.mockResolvedValueOnce({ error: { message: 'fail' } })
    - await useListsStore.getState().renameList('l1', 'New Name')
    - expect(useListsStore.getState().lists[0].name).toBe('Old')
    - expect(useListsStore.getState().error).toBe('Failed to rename list')

    Test 6 (LIST-03): 'deleteList removes row optimistically'
    - Pre-seed: useListsStore.setState({ lists: [{ id: 'l1', name: 'Groceries', share_code: 'abc12345', owner_id: 'u1', created_at: '' }], loading: false, error: null })
    - mockEq.mockResolvedValueOnce({ error: null })
    - await useListsStore.getState().deleteList('l1')
    - expect(useListsStore.getState().lists).toHaveLength(0)

    Test 7 (LIST-03): 'deleteList rolls back (re-inserts list row) on supabase error'
    - Pre-seed same single-item list
    - mockEq.mockResolvedValueOnce({ error: { message: 'fail' } })
    - await useListsStore.getState().deleteList('l1')
    - expect(useListsStore.getState().lists).toHaveLength(1)
    - expect(useListsStore.getState().error).toBe('Failed to delete list')

    Note on mock chain: mockOrder.mockReturnThis() makes .order() return the same mock object. For fetchLists the terminal call is the implicit .then() of the whole chain resolved by mockEq. Ensure mockEq for fetchLists resolves via .mockResolvedValueOnce on mockEq (the last chained call in fetchLists is .eq().order() — actually order is after eq; adjust: mockOrder.mockResolvedValueOnce). Check the exact chain order in listsStore RESEARCH.md Pattern 1 — fetchLists chains: .from().select().eq().order(). So mockOrder is the terminal call for fetchLists and mockSingle is terminal for createList.

    Imports at top: import { describe, it, expect, vi, beforeEach } from 'vitest'; import { useListsStore } from './listsStore'
  </action>
  <verify>
    <automated>cd C:/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run src/stores/listsStore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/listsStore.test.ts file exists
    - File contains exactly 7 it() test cases
    - vi.hoisted block exports mockFrom, mockSelect, mockInsert, mockUpdate, mockDelete, mockEq, mockOrder, mockSingle
    - vi.mock('@/lib/supabase') declaration present
    - beforeEach calls vi.clearAllMocks() and useListsStore.setState({ lists: [], loading: false, error: null })
    - npx vitest run src/stores/listsStore.test.ts exits with errors (expected RED — listsStore.ts does not exist yet); error must be "Cannot find module './listsStore'" or similar import error, NOT a test infrastructure error
  </acceptance_criteria>
  <done>listsStore.test.ts exists with 7 test stubs that will fail until listsStore.ts is implemented in Plan 01</done>
</task>

<task type="auto">
  <name>Task 2: Update LandingPage.test.tsx with LIST-03 dialog tests</name>
  <files>src/pages/LandingPage.test.tsx</files>
  <read_first>
    - src/pages/LandingPage.test.tsx — read the ENTIRE existing file before touching it; understand current mock setup, existing 3 tests, imports, and how user/authStore is mocked
    - .planning/phases/07-lists-infrastructure/07-VALIDATION.md — row 7-confirm: "Delete confirmation dialog renders 'and all its items'; cancel leaves list intact (no delete called)"
    - .planning/phases/07-lists-infrastructure/07-UI-SPEC.md — Copywriting Contract: delete dialog title "Delete '{listName}'?", description "This removes the list and all its items permanently.", cancel button "Cancel"
  </read_first>
  <action>
    Append two new describe blocks (or two new it() tests in an existing describe) to the existing LandingPage.test.tsx. Do NOT modify or delete any existing tests.

    The new tests require mocking listsStore. Add the listsStore mock at the top of the file alongside existing mocks:
    - vi.mock('@/stores/listsStore', () => ({ useListsStore: vi.fn() }))
    - import { useListsStore } from '@/stores/listsStore' at top

    Inside each new test, set up useListsStore mock to return a lists array with one list so the delete trigger renders:
    - (useListsStore as ReturnType<typeof vi.fn>).mockReturnValue({ lists: [{ id: 'l1', name: 'Test List', share_code: 'abc12345', owner_id: 'u1', created_at: '' }], loading: false, error: null, fetchLists: vi.fn(), createList: vi.fn(), renameList: vi.fn(), deleteList: vi.fn() })

    Also ensure the authStore mock (already in the file) has a user with id matching owner_id so rename/delete controls render. Check how authStore is currently mocked in LandingPage.test.tsx and extend accordingly.

    Test A (LIST-03): 'delete dialog renders with "all its items" copy when Trash2 button clicked'
    - render(<LandingPage />)
    - Click the delete trigger button (aria-label="Delete Test List")
    - expect(screen.getByText(/This removes the list and all its items permanently/)).toBeInTheDocument()
    - expect(screen.getByRole('button', { name: 'Delete' })).toBeInTheDocument()
    - expect(screen.getByRole('button', { name: 'Cancel' })).toBeInTheDocument()

    Test B (LIST-03): 'cancel on delete dialog closes without calling deleteList'
    - render(<LandingPage />)
    - Click delete trigger (aria-label="Delete Test List")
    - Confirm dialog is open (text "This removes" visible)
    - Click "Cancel" button
    - expect(screen.queryByText(/This removes the list/)).not.toBeInTheDocument()
    - The deleteList mock must NOT have been called: expect(mockDeleteList).not.toHaveBeenCalled()

    Note: These tests will fail with "Cannot find module '@/stores/listsStore'" until Plan 01 creates listsStore.ts. That is acceptable — Wave 0 stubs are expected RED.
  </action>
  <verify>
    <automated>cd C:/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run src/pages/LandingPage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - LandingPage.test.tsx still has all original tests (none removed)
    - Two new test cases added: one for dialog copy rendering, one for cancel behavior
    - vi.mock('@/stores/listsStore') added
    - npx vitest run src/pages/LandingPage.test.tsx shows the original tests may still pass; the 2 new tests fail due to missing listsStore module (expected RED)
  </acceptance_criteria>
  <done>LandingPage.test.tsx updated with 2 new failing LIST-03 tests; existing tests untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ store | Tests mock Supabase entirely — no real network calls, no auth.uid() evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | npm installs | accept | No new packages in this phase — all dependencies installed in prior phases. No slopcheck required. |
</threat_model>

<verification>
After both tasks complete:
- src/stores/listsStore.test.ts exists with 7 test stubs
- src/pages/LandingPage.test.tsx has 2 new tests appended
- npx vitest run exits non-zero (tests are RED — that is correct)
- No existing passing tests broken
</verification>

<success_criteria>
Wave 0 complete when:
1. listsStore.test.ts contains 7 it() stubs with concrete assertions per the VALIDATION.md behavior map
2. LandingPage.test.tsx has 2 new LIST-03 dialog tests
3. All new tests are RED (implementation not yet built) — zero false-positive green stubs
4. npx vitest run src/stores/listsStore.test.ts exits with import/module error (not a Vitest config error)
</success_criteria>

<output>
Create .planning/phases/07-lists-infrastructure/07-00-SUMMARY.md when done
</output>
