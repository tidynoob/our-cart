---
phase: 07-lists-infrastructure
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/pages/ListPage.tsx
autonomous: true
requirements:
  - LIST-02
  - LIST-03

must_haves:
  truths:
    - "ListPage header reflects the live list name from listsStore cache when available — rename from lists-home is reflected immediately in ListPage header (D-06 single-session optimistic)"
    - "ListPage shows rename and delete affordances only when user.id === list.owner_id (owner guard)"
    - "ListPage delete confirmation dialog uses disablePointerDismissal, showCloseButton={false}, variant='destructive', title \"Delete '{name}'?\", description 'This removes the list and all its items permanently.' per D-08"
    - "On successful delete from ListPage, navigate('/') is called — deleted list becomes unreachable at its URL (success criterion 4)"
    - "ListPage's existing share_code fetch path is preserved — direct URL navigation and legacy owner_id=NULL lists still load correctly (D-03, D-10)"
    - "owner_id added to ListPage's local List interface and select query so the owner guard works"
    - "npx vitest run exits 0 — full suite green"
  artifacts:
    - path: "src/pages/ListPage.tsx"
      provides: "ListPage with rename/delete affordances and navigate-away on delete"
      contains: "navigate('/')"
  key_links:
    - from: "src/pages/ListPage.tsx"
      to: "src/stores/listsStore.ts"
      via: "useListsStore selector for live name + deleteList/renameList actions"
      pattern: "useListsStore"
    - from: "src/pages/ListPage.tsx"
      to: "src/stores/authStore.ts"
      via: "useAuthStore for user.id owner guard"
      pattern: "useAuthStore"
    - from: "src/pages/ListPage.tsx"
      to: "react-router-dom"
      via: "navigate('/') on delete success"
      pattern: "navigate\\('/'\\)"
---

<objective>
Add rename and delete affordances to ListPage so users can manage a list from within the list view, and ensure rename propagates immediately from the Zustand store.

Purpose: D-08 explicitly says "if the user deletes the list they are currently viewing (/list/:code), navigate to / after success." D-06 says rename is "reflected immediately everywhere the name renders." Without this plan, both behaviors are broken — renamed lists still show the old name in the ListPage header, and there is no path to delete from within the list.

Output:
- src/pages/ListPage.tsx — owner_id added to local interface and select query; useListsStore wired for live name, renameList, and deleteList; owner-only rename/delete controls in header area; delete dialog with navigate('/') on success
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/PROJECT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/ROADMAP.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/07-lists-infrastructure/07-CONTEXT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/07-lists-infrastructure/07-UI-SPEC.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/07-lists-infrastructure/07-PATTERNS.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/07-lists-infrastructure/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire listsStore into ListPage for live name, rename, and delete</name>
  <files>src/pages/ListPage.tsx</files>
  <read_first>
    - src/pages/ListPage.tsx — read the ENTIRE file before touching anything; identify: local List interface (lines 22-27 — add owner_id), select query (line 69 — add owner_id), list.name usage in header, clearDialogOpen pattern (lines 45-46) to mirror for delete, clear-checked Dialog (lines 317-342) as delete dialog template, navigate import (line 2), existing auth imports
    - .planning/phases/07-lists-infrastructure/07-PATTERNS.md — "src/pages/ListPage.tsx" section: exact lines to change for interface, select, dialog imports (add DialogDescription), owner guard pattern, navigate-away
    - .planning/phases/07-lists-infrastructure/07-RESEARCH.md — Pitfall 5 (rename not reflected in ListPage header), Pitfall 3 (delete navigation race), Open Question 2 (rename/delete in both locations), Open Question 3 (listsStore.lists pre-population — do NOT call fetchLists from ListPage)
    - .planning/phases/07-lists-infrastructure/07-UI-SPEC.md — Copywriting Contract: delete dialog title, description, button labels; Rename interaction: same inline Input pattern as lists-home
    - src/stores/listsStore.ts — verify deleteList and renameList signatures
    - src/stores/authStore.ts — verify useAuthStore is already imported in ListPage or add it
  </read_first>
  <action>
    Modify src/pages/ListPage.tsx to add owner-only rename/delete affordances and live name from store. The share_code fetch path MUST survive unchanged (D-03).

    Step 1 — Update local List interface and select query:
    - Add owner_id: string to the local List interface (currently only has id, name, share_code, created_at)
    - Update the select string from 'id, name, share_code, created_at' to 'id, name, share_code, owner_id, created_at'
    - These two changes allow the owner guard (user.id === list.owner_id) to work

    Step 2 — Add imports:
    - import { useListsStore } from '@/stores/listsStore' (if not already present)
    - import { useAuthStore } from '@/stores/authStore' (check if already imported)
    - import { Pencil, Trash2 } from 'lucide-react' (check if lucide-react already imported)
    - Add DialogDescription to the existing Dialog import destructure (it is likely missing — add it alongside Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter)

    Step 3 — Add Zustand selectors and live name derivation:
    In the component body (after existing useState declarations):
    - const user = useAuthStore((state) => state.user) — if not already present
    - const storedLists = useListsStore((state) => state.lists)
    - const renameList = useListsStore((state) => state.renameList)
    - const deleteList = useListsStore((state) => state.deleteList)
    - const storedName = storedLists.find((l) => l.id === list?.id)?.name
    - const displayName = storedName ?? list?.name  — use storedName (live) when available, fall back to local state (Pitfall 5 fix per D-06)
    - Replace all uses of list?.name or list.name in the rendered JSX with displayName (except the owner check: user?.id === list?.owner_id uses the local list state)

    Step 4 — Add local UI state for rename and delete:
    - const [listRenameOpen, setListRenameOpen] = useState(false) — controls inline rename mode in header
    - const [listRenameName, setListRenameName] = useState('') — rename input value
    - const [deleteListDialogOpen, setDeleteListDialogOpen] = useState(false)
    - const [deleteListLoading, setDeleteListLoading] = useState(false)

    Step 5 — Owner guard and affordance placement:
    Check: const isOwner = Boolean(user && list && user.id === list.owner_id)
    Show rename/delete controls only when isOwner is true.

    Place the rename trigger (Pencil icon button) and delete trigger (Trash2 icon button) in the list header area — near the list name display. Check the existing header markup in ListPage.tsx to find the right place (likely near the h1/heading that shows list.name). Keep the header compact; this is a minimal Phase 7 affordance (full polish is Phase 8).

    Rename flow in header:
    - When not renaming (listRenameOpen === false): show displayName + Pencil icon button (aria-label="Rename {displayName}") + Trash2 icon button (aria-label="Delete {displayName}")
    - When renaming (listRenameOpen === true): show Input (value=listRenameName, autoFocus, aria-label="Rename list", onKeyDown: Enter → handleListRename, Escape → cancelListRename) + "Save name" Button (variant="outline" size="sm") + "Cancel" Button (variant="ghost" size="sm")
    - Opening rename: setListRenameOpen(true); setListRenameName(displayName ?? '')

    handleListRename: if !listRenameName.trim() return (no-op); await renameList(list!.id, listRenameName.trim()); setListRenameOpen(false)
    cancelListRename: setListRenameOpen(false)

    Step 6 — Delete confirmation dialog:
    Copy the clear-checked Dialog structure (lines 317-342 of ListPage) exactly but adapted for list delete.
    - Dialog open={deleteListDialogOpen} onOpenChange={(open) => { if (!open && !deleteListLoading) setDeleteListDialogOpen(false) }} disablePointerDismissal
    - DialogContent showCloseButton={false}
    - DialogHeader > DialogTitle: Delete '{displayName}'?
    - DialogDescription: "This removes the list and all its items permanently."
    - DialogFooter:
      - Button variant="outline" disabled={deleteListLoading} onClick={() => setDeleteListDialogOpen(false)}: "Cancel"
      - Button variant="destructive" disabled={deleteListLoading} onClick={handleListDeleteConfirm}: {deleteListLoading ? 'Deleting…' : 'Delete'}

    Step 7 — handleListDeleteConfirm:
    setDeleteListLoading(true)
    await deleteList(list!.id)
    setDeleteListLoading(false)
    navigate('/')
    — navigate AFTER await (per Pitfall 3: navigate only after DB confirms, not fire-and-forget). The optimistic remove in the store has already happened synchronously.

    CRITICAL: Do NOT call fetchLists from ListPage (per Open Question 3 and RESEARCH.md). ListPage does not drive the store's list collection — LandingPage does. ListPage only reads from the store cache for the live name.

    CRITICAL: The existing share_code fetch (lines ~60-82) must remain UNCHANGED. Legacy lists (owner_id=NULL) and direct URL access both rely on it (D-03, D-10). isOwner will be false for legacy lists (owner_id=NULL), so rename/delete affordances are automatically hidden for them.
  </action>
  <verify>
    <automated>cd C:/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - ListPage.tsx local List interface contains owner_id: string
    - ListPage.tsx select query contains 'owner_id'
    - ListPage.tsx contains useListsStore selector for storedLists
    - ListPage.tsx contains: const displayName = storedName ?? list?.name (or equivalent live-name derivation)
    - ListPage.tsx contains disablePointerDismissal on the delete-list Dialog
    - ListPage.tsx contains "This removes the list and all its items permanently." (grep check)
    - ListPage.tsx contains navigate('/') called after await deleteList (grep check: navigate.*'/'.*deleteList or equivalent)
    - ListPage.tsx does NOT contain a fetchLists call
    - ListPage.tsx original share_code fetch unchanged (supabase.from('lists').select(...).eq('share_code', code) still present)
    - npx vitest run exits 0 — full suite green (all existing + Wave 0 stubs)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ListPage shows owner-only rename/delete; live name from store cache; delete navigates to / on success; share_code fetch path preserved for legacy + direct nav (D-03, D-10)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ListPage → listsStore | Page reads list name from store cache (read-only); mutations go through store actions which validate and call Supabase |
| ListPage delete flow → navigate('/') | After delete confirms at DB layer, navigate removes the user from the now-deleted list's route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | ListPage rename/delete affordance | mitigate | isOwner guard (user.id === list.owner_id) hides affordances from non-owners and legacy NULL lists. RLS policies enforce at DB layer even if UI guard is bypassed. |
| T-07-11 | Tampering | ListPage delete — navigate race | mitigate | await deleteList() before navigate('/') per Pitfall 3 — navigation only after DB confirms delete. Prevents stale list state on back-navigation. |
| T-07-12 | Information Disclosure | Legacy lists (owner_id=NULL) | accept | Legacy lists accessible by direct URL (D-10); isOwner=false hides rename/delete. RLS IS NULL branch allows read by any authenticated user — intentional Phase 6 legacy-compat behavior for a 2-user household app. Acceptable risk. |
| T-07-SC | Tampering | npm installs | accept | No new packages. |
</threat_model>

<verification>
After task completes:
- npx vitest run exits 0 (full suite green)
- npx tsc --noEmit exits 0
- ListPage.tsx has owner_id in interface, select query, and isOwner guard
- navigate('/') called after await deleteList
</verification>

<success_criteria>
1. ListPage header shows live name from listsStore cache (Pitfall 5 fix, D-06)
2. Rename and delete affordances visible only when user is the list owner (isOwner guard)
3. Delete dialog: disablePointerDismissal, "This removes the list and all its items permanently.", navigate('/') on success
4. ListPage's share_code fetch path survives unchanged (D-03, D-10)
5. Full Vitest suite exits 0
6. npx tsc --noEmit clean
</success_criteria>

<output>
Create .planning/phases/07-lists-infrastructure/07-03-SUMMARY.md when done
</output>
