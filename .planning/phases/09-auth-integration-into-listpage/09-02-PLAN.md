---
phase: 09-auth-integration-into-listpage
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/components/AttributionBadge.tsx
  - src/components/AttributionBadge.test.tsx
  - src/components/ItemRow.tsx
  - src/components/ItemRow.test.tsx
autonomous: true
requirements:
  - PROF-02
must_haves:
  truths:
    - "AttributionBadge renders a circular <img> with referrerPolicy='no-referrer' when avatarUrl prop is provided and no error"
    - "AttributionBadge falls back to colored-initial badge when avatarUrl is absent or img fires onError"
    - "ItemRow shows live auth display name + Google avatar for items where item.user_id === currentUserId"
    - "ItemRow shows frozen added_by + initial badge for items where user_id differs from currentUserId"
    - "ItemRow shows '?' badge for items with null added_by and no user_id match"
    - "AttributionBadge.test.tsx all 5 cases GREEN"
    - "ItemRow.test.tsx compiles and passes with user_id: null added to baseItem"
  artifacts:
    - path: "src/components/AttributionBadge.tsx"
      provides: "Avatar + initials fallback badge"
      contains: "avatarUrl"
    - path: "src/components/ItemRow.tsx"
      provides: "Live attribution for own items"
      contains: "isOwnItem"
  key_links:
    - from: "src/components/ItemRow.tsx"
      to: "src/components/AttributionBadge.tsx"
      via: "avatarUrl prop"
      pattern: "avatarUrl"
    - from: "src/components/ItemRow.tsx"
      to: "item.user_id"
      via: "equality check"
      pattern: "user_id.*currentUserId"
---

<objective>
Extend AttributionBadge with the optional avatarUrl prop and onError fallback, then update ItemRow to resolve attribution live from auth identity for own items (D-04/D-06/D-07). This satisfies PROF-02 (Google avatar on own items) and is the component layer that ListPage (09-04) threads current-user props into.

Purpose: D-07 avatar extension and D-04/D-06 live attribution are companion changes — both files are in the same rendering chain, and AttributionBadge must be updated before ItemRow can use its new prop.
Output: Updated AttributionBadge.tsx + AttributionBadge.test.tsx GREEN; updated ItemRow.tsx + ItemRow.test.tsx GREEN.
</objective>

<execution_context>
@C:/Users/mitch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-auth-integration-into-listpage/09-CONTEXT.md
@.planning/phases/09-auth-integration-into-listpage/09-RESEARCH.md
@.planning/phases/09-auth-integration-into-listpage/09-PATTERNS.md
@.planning/phases/09-auth-integration-into-listpage/09-UI-SPEC.md
@src/components/AttributionBadge.tsx
@src/components/AttributionBadge.test.tsx
@src/components/ItemRow.tsx
@src/components/ItemRow.test.tsx
@src/types/item.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AttributionBadge with optional avatarUrl prop (D-07, PROF-02)</name>
  <files>src/components/AttributionBadge.tsx, src/components/AttributionBadge.test.tsx</files>

  <read_first>
  - src/components/AttributionBadge.tsx — read the full file. It is currently 29 lines with name + className props. Understand the existing div structure, cn() usage, getAttributionColor(), getInitials().
  - src/components/AttributionBadge.test.tsx — read the Wave 0 scaffold. Tests (2) and (3) are currently RED; (1), (4), (5) are GREEN. After this task all 5 should be GREEN.
  - .planning/phases/09-auth-integration-into-listpage/09-PATTERNS.md — "AttributionBadge.tsx (component, transform)" section for the complete updated component code.
  - .planning/phases/09-auth-integration-into-listpage/09-UI-SPEC.md — "AttributionBadge — Avatar Extension" section. Size: h-7 w-7. overflow-hidden on wrapper. No border ring.
  - .planning/phases/09-auth-integration-into-listpage/09-RESEARCH.md — Pattern 3 (referrerPolicy requirement) and Pitfall 1 (Google CDN 403 without no-referrer) and Pitfall 2 (onError never fires for empty src — use avatarUrl && !imgError guard).
  </read_first>

  <behavior>
  - Test (already in scaffold, now GREEN): renders initials when no avatarUrl
  - Test (was RED, now GREEN): renders img with referrerPolicy="no-referrer" when avatarUrl present (PROF-02)
  - Test (was RED, now GREEN): falls back to initials when img fires onError (PROF-02)
  - Test (already GREEN): wrapper has aria-label "{name} added this"
  - Test (already in scaffold, now GREEN): does not render img when avatarUrl is undefined
  </behavior>

  <action>
  Rewrite src/components/AttributionBadge.tsx with these changes from the current 29-line version:

  (1) Add `import { useState } from 'react'` at the top (before the attribution/utils imports).

  (2) Add `avatarUrl?: string` to the AttributionBadgeProps interface after name, before className.

  (3) Inside the component body, add: const [imgError, setImgError] = useState(false) and const showImg = avatarUrl && !imgError — this is the critical guard that prevents rendering an img for empty/undefined src (Pitfall 2).

  (4) In the wrapper div: add `overflow-hidden` to the cn() className string (clips the circular img). Apply the `style={{ backgroundColor: color.bg, color: color.text }}` ONLY when !showImg (avoid tinting the avatar image). When showImg is true, pass style={undefined}.

  (5) Replace the {getInitials(name)} inside the div with a ternary: when showImg render the img element; else render {getInitials(name)}.

  The img element: src={avatarUrl} (TypeScript: avatarUrl is string here because showImg guard already confirmed it is truthy), alt={name}, className="h-full w-full object-cover rounded-full", referrerPolicy="no-referrer" (MANDATORY — Google CDN 403 without this per Pitfall 1), onError={() => setImgError(true)}.

  The wrapper aria-label remains `${name} added this` — unchanged.

  After updating the component, verify the Wave 0 tests now pass by checking AttributionBadge.test.tsx test (2) expectation: container.querySelector('img') returns the img element and img.getAttribute('referrerpolicy') === 'no-referrer'.
  </action>

  <verify>
    <automated>npx vitest run src/components/AttributionBadge.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <done>All 5 AttributionBadge tests pass GREEN. npx tsc --noEmit reports no TypeScript errors on AttributionBadge.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update ItemRow with live attribution and current-user props (D-04, D-05, D-06, PROF-02)</name>
  <files>src/components/ItemRow.tsx, src/components/ItemRow.test.tsx</files>

  <read_first>
  - src/components/ItemRow.tsx — read lines 1-30 (imports + interface) and lines 245-270 (attribution block, currently lines 251-261 per PATTERNS.md). Understand ItemRowProps and the existing attribution JSX.
  - src/components/ItemRow.test.tsx — read the baseItem definition (lines 72-81). Must add user_id: null to baseItem so the Item type is satisfied after Plan 09-01 added the field.
  - src/types/item.ts — confirm user_id: string | null now exists (added in Plan 09-01).
  - .planning/phases/09-auth-integration-into-listpage/09-PATTERNS.md — "ItemRow.tsx (component, transform)" section for exact new props (currentUserId, currentUserDisplayName, currentUserAvatarUrl) and the updated attribution block with isOwnItem logic.
  - .planning/phases/09-auth-integration-into-listpage/09-RESEARCH.md — Pattern 2 (attribution resolution) and D-06 resolution order: (1) own item → live name + avatar, (2) added_by → frozen snapshot, (3) null → "?" badge.
  </read_first>

  <behavior>
  - Test: baseItem with user_id: null in ItemRow.test.tsx compiles without error (type fix)
  - Test: when item.user_id matches currentUserId, AttributionBadge receives currentUserDisplayName + currentUserAvatarUrl (PROF-02 / D-04)
  - Test: when item.user_id does not match currentUserId, AttributionBadge receives item.added_by (existing behavior, D-06)
  - Test: when item.user_id is null, falls back to added_by branch (D-06 case 2)
  </behavior>

  <action>
  src/components/ItemRow.tsx:
  (1) Extend ItemRowProps interface with three optional props after onToggle:
    - currentUserId?: string | null
    - currentUserDisplayName?: string
    - currentUserAvatarUrl?: string | null
  Destructure them in the function signature alongside existing props.

  (2) In the display-mode render section, before (or inside) the attribution JSX block at approximately lines 251-261, add:
    const isOwnItem = item.user_id != null && item.user_id === currentUserId

  (3) Replace the existing attribution block (the if item.added_by conditional) with the three-branch ternary from PATTERNS.md:
    - isOwnItem → AttributionBadge with name={currentUserDisplayName ?? item.added_by ?? '?'} and avatarUrl={currentUserAvatarUrl ?? undefined}
    - else if item.added_by → AttributionBadge with name={item.added_by} (no avatarUrl — frozen snapshot, initials only)
    - else → the existing "?" div with aria-label="Unknown person added this"

  Keep all other ItemRow code unchanged — this is a targeted replacement of the attribution block only.

  src/components/ItemRow.test.tsx:
  (1) Add `user_id: null` to baseItem (after added_by: 'Alice'). This satisfies the Item type after Plan 09-01 added the field.
  (2) Add a new describe block 'ItemRow — attribution (PROF-02/D-06)' with two tests:
    - Own item: render with item having user_id: 'user-1', currentUserId: 'user-1', currentUserDisplayName: 'Mitchell', currentUserAvatarUrl: 'https://avatar.url'. Expect the rendered attribution area to show the name 'Mitchell' OR have an img element (either assertion confirms own-item path). Use screen.getByLabelText('Mitchell added this') if the aria-label is present.
    - Non-own item: render with item having user_id: 'user-2', currentUserId: 'user-1', added_by: 'Alice'. Expect screen.getByLabelText('Alice added this') to be in document.
  </action>

  <verify>
    <automated>npx vitest run src/components/ItemRow.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <done>All ItemRow tests pass GREEN including the new attribution tests. No TypeScript errors on ItemRow.tsx (npx tsc --noEmit).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google CDN → browser img | Avatar URL from user_metadata; cross-origin request |
| item.added_by → JSX render | User-supplied string rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | AttributionBadge img (XSS) | mitigate | avatarUrl and name rendered via JSX — no dangerouslySetInnerHTML. React escapes string props. |
| T-09-02-02 | Tampering | Google CDN 403 (referrer leak) | mitigate | referrerPolicy="no-referrer" on every Google avatar img tag — prevents Referer header from being sent. See RESEARCH.md Pitfall 1. |
| T-09-02-03 | Spoofing | Attribution ownership (item.user_id match) | mitigate | isOwnItem = item.user_id != null AND item.user_id === currentUserId. Both conditions required — null user_id falls through to added_by path. |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
npx vitest run src/components/AttributionBadge.test.tsx src/components/ItemRow.test.tsx
All tests GREEN. npx tsc --noEmit passes.
</verification>

<success_criteria>
- AttributionBadge accepts optional avatarUrl; renders img with referrerPolicy="no-referrer" when present; onError falls back to initials
- ItemRow attribution: own item (user_id match) → live name + avatarUrl; else item.added_by → initials; else "?" badge
- All test files GREEN
- TypeScript clean (npx tsc --noEmit)
</success_criteria>

<output>
Create .planning/phases/09-auth-integration-into-listpage/09-02-SUMMARY.md when done
</output>
