---
phase: 09-auth-integration-into-listpage
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/AttributionBadge.test.tsx
  - src/stores/uiStore.test.ts
autonomous: true
requirements:
  - PROF-02
  - NAV-03
must_haves:
  truths:
    - "AttributionBadge.test.tsx exists and runs with failing (RED) stubs for avatar render + onError fallback"
    - "uiStore.test.ts exists and runs with failing (RED) stubs for restoreBanner + dismissBanner coverage"
  artifacts:
    - path: "src/components/AttributionBadge.test.tsx"
      provides: "PROF-02 test scaffolds"
      contains: "AttributionBadge"
    - path: "src/stores/uiStore.test.ts"
      provides: "NAV-03 test scaffolds"
      contains: "restoreBanner"
  key_links:
    - from: "src/components/AttributionBadge.test.tsx"
      to: "src/components/AttributionBadge.tsx"
      via: "import"
      pattern: "AttributionBadge"
    - from: "src/stores/uiStore.test.ts"
      to: "src/stores/uiStore.ts"
      via: "import"
      pattern: "useUIStore"
---

<objective>
Create the two Wave 0 test scaffold files mandated by VALIDATION.md. These files must exist before Wave 1+ plans execute so their verify commands resolve. Stubs that fail (RED) on missing implementation are correct — green tests require the Wave 1 store additions and Wave 2 component changes.

Purpose: Satisfy the Nyquist requirement that every implementation task has an automated verify command pointing to a real file.
Output: src/components/AttributionBadge.test.tsx, src/stores/uiStore.test.ts
</objective>

<execution_context>
@C:/Users/mitch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-auth-integration-into-listpage/09-CONTEXT.md
@.planning/phases/09-auth-integration-into-listpage/09-VALIDATION.md
@.planning/phases/09-auth-integration-into-listpage/09-PATTERNS.md
@src/components/SyncStatus.test.tsx
@src/stores/authStore.test.ts
@src/stores/uiStore.ts
@src/components/AttributionBadge.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create uiStore.test.ts scaffold</name>
  <files>src/stores/uiStore.test.ts</files>

  <read_first>
  - src/stores/uiStore.ts — full file (14 lines). Understand the UIState interface, dismissedBanners Set, and dismissBanner action that restoreBanner mirrors.
  - src/stores/authStore.test.ts — lines 1-52. Copy the describe/beforeEach/setState reset pattern; uiStore has NO supabase dep so omit vi.mock('@/lib/supabase').
  - .planning/phases/09-auth-integration-into-listpage/09-PATTERNS.md — the "uiStore.test.ts (test — NEW FILE)" section for the exact test shape with three restoreBanner cases.
  </read_first>

  <behavior>
  - Test: dismissBanner adds a code to dismissedBanners Set (existing behavior, verify it still works)
  - Test: dismissBanner creates a new Set reference (Zustand reactivity)
  - Test: restoreBanner removes a code from dismissedBanners (NAV-03) — will be RED until Wave 1 adds the action
  - Test: restoreBanner returns a new Set reference (Zustand reactivity) — will be RED until Wave 1
  - Test: restoreBanner does not throw when code is not in Set — will be RED until Wave 1
  </behavior>

  <action>
  Create src/stores/uiStore.test.ts. Import describe, it, expect, beforeEach from 'vitest'. Import useUIStore from './uiStore'. No supabase mock needed — uiStore has no async calls.

  Structure two describe blocks:

  describe('uiStore — dismissBanner'): beforeEach resets useUIStore.setState({ dismissedBanners: new Set() }). Tests: (1) adds code to Set, (2) new Set reference after add.

  describe('uiStore — restoreBanner'): beforeEach sets useUIStore.setState({ dismissedBanners: new Set(['abc123']) }). Tests referencing NAV-03 in comments: (1) 'removes the code from dismissedBanners (NAV-03)' — calls restoreBanner('abc123'), expects has('abc123') === false; (2) 'returns a new Set reference (NAV-03)' — snapshot before/after, expect(after).not.toBe(before); (3) 'does not throw when restoring a code not in the Set' — expect(() => restoreBanner('not-there')).not.toThrow().

  For the restoreBanner tests: call useUIStore.getState().restoreBanner — this action does not exist yet in Wave 0, so these tests will be RED. That is correct and expected.
  </action>

  <verify>
    <automated>npx vitest run src/stores/uiStore.test.ts 2>&1 | tail -20</automated>
  </verify>

  <done>File exists. dismissBanner tests pass GREEN. restoreBanner tests fail RED with "restoreBanner is not a function" or property access error (not a syntax/import error). No compilation errors on the test file itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AttributionBadge.test.tsx scaffold</name>
  <files>src/components/AttributionBadge.test.tsx</files>

  <read_first>
  - src/components/AttributionBadge.tsx — full file (29 lines). Note the current props interface: name + className only. The avatarUrl prop does not exist yet — tests for it will be RED.
  - src/components/SyncStatus.test.tsx — lines 1-44. Copy the describe/render/screen pattern. AttributionBadge has no store dep so no vi.mock needed.
  - .planning/phases/09-auth-integration-into-listpage/09-PATTERNS.md — the "AttributionBadge.test.tsx (test — NEW FILE)" section for the five test cases.
  - .planning/phases/09-auth-integration-into-listpage/09-RESEARCH.md — Pattern 3 (avatarUrl, referrerPolicy="no-referrer", onError fallback).
  </read_first>

  <behavior>
  - Test: renders initials badge when no avatarUrl (existing behavior, PROF-02 fallback)
  - Test: renders img tag with referrerPolicy="no-referrer" when avatarUrl prop present (PROF-02) — RED until Wave 2
  - Test: falls back to initials when img fires onError (PROF-02) — RED until Wave 2
  - Test: wrapper div has aria-label="{name} added this" (accessibility)
  - Test: does NOT render img tag when avatarUrl is undefined (onError guard) — RED until Wave 2
  </behavior>

  <action>
  Create src/components/AttributionBadge.test.tsx. Import describe, it, expect, vi from 'vitest'. Import render, screen, fireEvent from '@testing-library/react'. Import AttributionBadge from './AttributionBadge'. No store mocks needed.

  Single describe('AttributionBadge') block with beforeEach(() => vi.clearAllMocks()). Five it() cases:

  (1) 'renders initials when no avatarUrl (PROF-02 fallback)': render with name="Alice", expect no img role, expect initials text to be in document. Use getInitials convention — single initial or two initials. Check screen.queryByRole('img') is null.

  (2) 'renders img with referrerPolicy="no-referrer" when avatarUrl present (PROF-02)': render with name="Alice" avatarUrl="https://example.com/avatar.jpg". Expect container.querySelector('img') not null and img.getAttribute('referrerpolicy') === 'no-referrer'. Will be RED until Wave 2.

  (3) 'falls back to initials when img fires onError (PROF-02)': render with name="Alice" avatarUrl="https://bad.url". Get img via container.querySelector('img'), call fireEvent.error(img). Expect screen.queryByRole('img') to be null after error. Will be RED until Wave 2.

  (4) 'wrapper has aria-label "{name} added this"': render with name="Bob". Expect screen.getByLabelText('Bob added this') to be in document.

  (5) 'does NOT render img when avatarUrl is undefined': render with name="Carol" and no avatarUrl prop. Expect container.querySelector('img') to be null.

  Note: test (3) checks for img absence after error — the component re-renders with imgError=true hiding the img. For test (3), after fireEvent.error the initials should appear. You can verify with screen.queryByRole('img') === null (img is hidden by conditional render, not just hidden via CSS).
  </action>

  <verify>
    <automated>npx vitest run src/components/AttributionBadge.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <done>File exists. Tests (1), (4), and (5) pass GREEN (no avatarUrl = existing behavior). Tests (2) and (3) fail RED because avatarUrl prop doesn't exist yet. No import/syntax errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → component | Test-supplied props go directly to component render; no external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-SC | Tampering | test file imports | accept | No npm installs; all deps are existing devDependencies already in package.json |
</threat_model>

<verification>
npx vitest run src/stores/uiStore.test.ts src/components/AttributionBadge.test.tsx
Expected: both files run; dismissBanner + AttributionBadge no-avatar cases pass; restoreBanner + avatarUrl cases fail RED (not syntax errors).
</verification>

<success_criteria>
- src/stores/uiStore.test.ts exists, imports resolve, dismissBanner describe block is GREEN
- src/components/AttributionBadge.test.tsx exists, imports resolve, no-avatar tests are GREEN
- Vitest reports the restoreBanner and avatarUrl tests as failures (not errors), confirming stubs are correctly wired to implementation slots
</success_criteria>

<output>
Create .planning/phases/09-auth-integration-into-listpage/09-00-SUMMARY.md when done
</output>
