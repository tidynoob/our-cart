---
phase: 02-list-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/item.ts
  - src/lib/categories.ts
  - src/lib/attribution.ts
  - src/stores/itemsStore.ts
  - src/lib/categories.test.ts
  - src/lib/attribution.test.ts
  - src/components/ui/dialog.tsx
  - src/components/ui/select.tsx
autonomous: false
requirements: [LIST-01, LIST-02, LIST-03, LIST-04, LIST-06]

must_haves:
  truths:
    - "Item type interface matches Supabase items table schema including added_by column"
    - "Category list defines 9 categories in correct order with grouping utility"
    - "Attribution utility produces deterministic color from name and extracts first initial"
    - "Items store has fetchItems, addItem, updateItem, deleteItem actions with optimistic updates"
    - "Supabase items table has added_by column and UPDATE/DELETE RLS policies"
    - "shadcn Dialog and Select components are installed and importable"
  artifacts:
    - path: "src/types/item.ts"
      provides: "Item interface with all DB columns"
      contains: "added_by"
    - path: "src/lib/categories.ts"
      provides: "CATEGORIES array and groupItemsByCategory function"
      exports: ["CATEGORIES", "groupItemsByCategory"]
    - path: "src/lib/attribution.ts"
      provides: "getAttributionColor and getInitials functions"
      exports: ["getAttributionColor", "getInitials"]
    - path: "src/stores/itemsStore.ts"
      provides: "Zustand items store with CRUD actions"
      exports: ["useItemsStore"]
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn Dialog component"
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select component"
  key_links:
    - from: "src/stores/itemsStore.ts"
      to: "src/lib/supabase.ts"
      via: "import supabase singleton"
      pattern: "import.*supabase.*from.*@/lib/supabase"
    - from: "src/stores/itemsStore.ts"
      to: "src/types/item.ts"
      via: "import Item interface"
      pattern: "import.*Item.*from.*@/types/item"
---

<objective>
Foundation layer for Phase 2 List Management: schema migration (adds added_by column + UPDATE/DELETE RLS policies), shadcn Dialog and Select component installation, Item type definition, category and attribution utilities with tests, and the full itemsStore with optimistic CRUD actions.

Purpose: All subsequent plans (add item UI, display, edit/delete) depend on the store, types, utilities, and DB schema being in place. This plan establishes every contract that Plans 02 and 03 consume. Per D-08, the schema migration adds the added_by column. Per D-04/D-05/D-06, the categories utility defines the grouping logic. Per D-07, the attribution utility provides deterministic color hashing.

Output: 6 new source files, 2 test files, 2 shadcn UI components, and a verified Supabase schema migration.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-list-management/02-CONTEXT.md
@.planning/phases/02-list-management/02-RESEARCH.md
@.planning/phases/02-list-management/02-PATTERNS.md
@.planning/phases/02-list-management/02-UI-SPEC.md
@src/stores/uiStore.ts
@src/lib/supabase.ts
@src/lib/extractShareCode.ts
@src/lib/generateCode.test.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run Supabase schema migration (adds added_by column + UPDATE/DELETE RLS policies)</name>
  <what-built>SQL migration script for the items table — adds the added_by text column (nullable) and creates UPDATE + DELETE RLS policies scoped by list_id foreign key. This is required per D-08 and the security enforcement requirement before any item edit/delete code can function.</what-built>
  <how-to-verify>
    1. Open your Supabase project dashboard -> SQL Editor
    2. Paste and run the following SQL:

    ALTER TABLE items ADD COLUMN added_by text;

    CREATE POLICY "anon_update_items" ON items FOR UPDATE TO anon
      USING (EXISTS (SELECT 1 FROM lists WHERE lists.id = items.list_id))
      WITH CHECK (EXISTS (SELECT 1 FROM lists WHERE lists.id = items.list_id));

    CREATE POLICY "anon_delete_items" ON items FOR DELETE TO anon
      USING (EXISTS (SELECT 1 FROM lists WHERE lists.id = items.list_id));

    3. Verify: In Table Editor, click on "items" table -> columns should now show "added_by" (text, nullable)
    4. Verify: In Authentication -> Policies -> items table should now show 4 policies: anon_select_items, anon_insert_items, anon_update_items, anon_delete_items
  </how-to-verify>
  <resume-signal>Type "migration done" after verifying all 4 policies exist on the items table and the added_by column is visible</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install shadcn components, create Item type, categories utility, attribution utility, and itemsStore</name>
  <files>src/types/item.ts, src/lib/categories.ts, src/lib/attribution.ts, src/stores/itemsStore.ts, src/lib/categories.test.ts, src/lib/attribution.test.ts, src/components/ui/dialog.tsx, src/components/ui/select.tsx</files>
  <read_first>
    - src/stores/uiStore.ts (Zustand store pattern: double-invocation, interface above, named export with use prefix)
    - src/lib/supabase.ts (singleton import pattern)
    - src/lib/extractShareCode.ts (utility module pattern: JSDoc, named export, pure function)
    - src/lib/generateCode.test.ts (test pattern: describe/it/expect from vitest)
    - src/pages/ListPage.tsx (existing List interface at lines 7-12 for field naming convention)
    - .planning/phases/02-list-management/02-RESEARCH.md (Pattern 1-4 code examples)
    - .planning/phases/02-list-management/02-PATTERNS.md (analog patterns for each file)
    - .planning/phases/02-list-management/02-UI-SPEC.md (attribution colors section)
    - components.json (shadcn CLI config — verify base-nova style)
  </read_first>
  <behavior>
    - categories.test.ts: groupItemsByCategory returns categories in predefined order (Produce first, Uncategorized last)
    - categories.test.ts: empty categories are omitted from result
    - categories.test.ts: items with null/undefined/unrecognized category go to Uncategorized
    - categories.test.ts: items within a category maintain insertion order
    - attribution.test.ts: getInitials returns uppercase first character of name
    - attribution.test.ts: getAttributionColor returns blue slot for even charCodeAt(0), green slot for odd
    - attribution.test.ts: same name always returns same color (deterministic)
    - attribution.test.ts: getAttributionColor returns object with bg and text properties
  </behavior>
  <action>
    1. Install shadcn Dialog and Select: run `npx shadcn@latest add dialog select` — this generates src/components/ui/dialog.tsx and src/components/ui/select.tsx using the existing components.json config (base-nova style, @base-ui/react primitives).

    2. Create src/types/item.ts: export an Item interface with fields matching the Supabase items table — id (string), list_id (string), name (string), quantity (string | null), category (string | null), checked (boolean), added_by (string | null), created_at (string). All field names are snake_case matching DB columns.

    3. Create src/lib/categories.ts: export CATEGORIES as a readonly array of 9 strings in order: Produce, Dairy, Meat, Bakery, Frozen, Beverages, Snacks, Other, Uncategorized. Export a Category type derived from the array. Export groupItemsByCategory(items: Item[]) that returns an array of { category: Category, items: Item[] } objects — only categories with items are included, ordered per the CATEGORIES array. Items with null/unrecognized category map to Uncategorized. Per D-04, D-05, D-06.

    4. Create src/lib/attribution.ts: export PERSON_COLORS as a 2-element readonly array with oklch color objects — slot 0 is blue {bg: 'oklch(0.75 0.15 250)', text: 'oklch(0.30 0.10 250)'}, slot 1 is green {bg: 'oklch(0.80 0.15 150)', text: 'oklch(0.35 0.10 150)'}. Export getAttributionColor(name: string) returning PERSON_COLORS[name.charCodeAt(0) % 2]. Export getInitials(name: string) returning name.charAt(0).toUpperCase(). Per D-07 and UI spec attribution colors table.

    5. Create src/stores/itemsStore.ts: Zustand store following the uiStore.ts pattern (create<ItemsState>()((set, get) => ({...}))). Interface ItemsState with: items (Item[]), loading (boolean), error (string | null), editingItemId (string | null), deletingItemId (string | null). Actions: fetchItems(listId: string), addItem(listId, name, quantity?, category?, addedBy?), updateItem(id, changes: Partial<Pick<Item, 'name' | 'quantity' | 'category'>>), deleteItem(id), setEditingItemId(id | null), setDeletingItemId(id | null). Each mutation action does optimistic update first then calls Supabase, rolling back on error. Use crypto.randomUUID() for temp IDs on addItem. Import Item from @/types/item and supabase from @/lib/supabase.

    6. Create src/lib/categories.test.ts: vitest tests for groupItemsByCategory covering: correct order, empty categories omitted, null/unrecognized category → Uncategorized, items maintain insertion order within category.

    7. Create src/lib/attribution.test.ts: vitest tests for getAttributionColor (deterministic, correct slot assignment by charCodeAt(0) % 2, returns bg/text object) and getInitials (uppercase first char).
  </action>
  <verify>
    <automated>npx vitest run src/lib/categories.test.ts src/lib/attribution.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/types/item.ts exports an Item interface with 8 fields: id, list_id, name, quantity, category, checked, added_by, created_at
    - src/lib/categories.ts exports CATEGORIES (9-element array starting with "Produce", ending with "Uncategorized"), Category type, and groupItemsByCategory function
    - src/lib/attribution.ts exports PERSON_COLORS, getAttributionColor, and getInitials
    - src/stores/itemsStore.ts exports useItemsStore with fetchItems, addItem, updateItem, deleteItem, setEditingItemId, setDeletingItemId actions
    - src/components/ui/dialog.tsx exists and exports Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription
    - src/components/ui/select.tsx exists and exports Select, SelectContent, SelectItem, SelectTrigger, SelectValue
    - npx vitest run src/lib/categories.test.ts exits 0 with all tests passing
    - npx vitest run src/lib/attribution.test.ts exits 0 with all tests passing
    - npx tsc --noEmit exits 0 (TypeScript compilation clean)
  </acceptance_criteria>
  <done>All utility modules, types, store, and shadcn components exist with passing tests and clean TypeScript compilation. The itemsStore correctly implements optimistic CRUD with Supabase calls. Dialog and Select components are importable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase | All CRUD operations pass through RLS policies |
| User input → items table | name, quantity, category, added_by are user-provided text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | items table UPDATE/DELETE | mitigate | RLS policies anon_update_items and anon_delete_items enforce list_id FK exists in lists table — migration in Task 1 |
| T-02-02 | Tampering | item name/quantity/category | mitigate | React JSX auto-escaping; no dangerouslySetInnerHTML anywhere in phase; all values rendered via interpolation |
| T-02-03 | Spoofing | localStorage name (added_by) | accept | Cosmetic attribution only for 2-person private app; not an access control mechanism |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages installed — shadcn CLI copies files using already-installed @base-ui/react (verified in Phase 1 Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run` — all tests pass (existing + new)
- `npx tsc --noEmit` — no TypeScript errors
- Supabase items table has added_by column and 4 RLS policies
- shadcn dialog and select components importable from @/components/ui/
</verification>

<success_criteria>
- Item type, categories utility, attribution utility, and itemsStore all exist with correct exports
- categories.test.ts and attribution.test.ts pass with 100% of behaviors verified
- Supabase schema migration applied (added_by column + UPDATE/DELETE policies)
- shadcn Dialog and Select components installed
- TypeScript builds cleanly
</success_criteria>

<output>
Create `.planning/phases/02-list-management/02-01-SUMMARY.md` when done
</output>
