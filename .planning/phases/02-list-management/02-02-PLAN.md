---
phase: 02-list-management
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/NamePromptDialog.tsx
  - src/components/AddItemBar.tsx
  - src/components/AttributionBadge.tsx
  - src/components/CategorySection.tsx
  - src/components/ItemRow.tsx
  - src/pages/ListPage.tsx
autonomous: true
requirements: [LIST-01, LIST-04, LIST-06]

must_haves:
  truths:
    - "User is prompted for their name on first visit to a list (stored in localStorage)"
    - "User can add an item with just a name via the input bar and Enter/tap"
    - "User can expand the add bar to set quantity and category (dropdown shows only 8 real categories, no Uncategorized)"
    - "Items display grouped by category with bold section headers in CATEGORY_ORDER"
    - "Each item shows a colored initials badge with aria-label for screen readers"
    - "Empty categories are not shown; Uncategorized group appears at bottom for null-category items"
    - "ListPage loads items on mount via fetchItems and shows loading/error states"
    - "All text inputs are 16px+ to prevent iOS zoom"
  artifacts:
    - path: "src/components/NamePromptDialog.tsx"
      provides: "Modal dialog that captures user name on first list visit"
      min_lines: 30
    - path: "src/components/AddItemBar.tsx"
      provides: "Item entry form with expand for quantity/category, duplicate-submit prevention"
      min_lines: 50
    - path: "src/components/AttributionBadge.tsx"
      provides: "Colored circle with initials and accessible label"
      min_lines: 15
    - path: "src/components/CategorySection.tsx"
      provides: "Category header + list of ItemRow components"
      min_lines: 20
    - path: "src/components/ItemRow.tsx"
      provides: "Single item row with badge, name, quantity"
      min_lines: 25
    - path: "src/pages/ListPage.tsx"
      provides: "Orchestrates all components with store integration, lifecycle management"
  key_links:
    - from: "src/components/AddItemBar.tsx"
      to: "src/stores/itemsStore.ts"
      via: "useItemsStore addItem action"
      pattern: "useItemsStore.*addItem"
    - from: "src/pages/ListPage.tsx"
      to: "src/stores/itemsStore.ts"
      via: "useItemsStore fetchItems + loading + error"
      pattern: "useItemsStore.*(fetchItems|loading|error)"
    - from: "src/pages/ListPage.tsx"
      to: "src/lib/categories.ts"
      via: "groupItemsByCategory"
      pattern: "groupItemsByCategory"
    - from: "src/components/AttributionBadge.tsx"
      to: "src/lib/attribution.ts"
      via: "getAttributionColor and getInitials"
      pattern: "getAttributionColor|getInitials"
    - from: "src/components/NamePromptDialog.tsx"
      to: "localStorage"
      via: "our-cart-name-{listId} key"
      pattern: "our-cart-name-"
---

<objective>
Vertical slice delivering the core "add and view items" experience: name prompt dialog on first visit, AddItemBar with expand for quantity/category (dropdown excludes Uncategorized), items displayed grouped by category with accessible attribution badges, and ListPage lifecycle (load list, load items, load name, render). After this plan, a user can open the list, enter their name, add grocery items, and see them organized by category with colored initials showing who added each item.

Purpose: This is the primary user-facing vertical slice for Phase 2 — it covers LIST-01 (add item), LIST-04 (attribution), and LIST-06 (category grouping) end-to-end. Per D-01/D-02/D-03 the add bar is a pinned input with expandable details. Per D-04/D-05/D-06 items group by category. Per D-07 each item shows a colored initials badge.

Addresses review concerns: Category dropdown excludes Uncategorized (MEDIUM), iOS input zoom prevention via 16px+ inputs (MEDIUM), ListPage lifecycle with fetchItems/loading/error (MEDIUM), add normalization with trim/null/duplicate-submit prevention (MEDIUM), accessible badge labels (MEDIUM), deterministic sort within categories by created_at (LOW).

Output: 5 new components + updated ListPage delivering the complete add-and-view flow.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-list-management/02-CONTEXT.md
@.planning/phases/02-list-management/02-RESEARCH.md
@.planning/phases/02-list-management/02-PATTERNS.md
@.planning/phases/02-list-management/02-UI-SPEC.md
@.planning/phases/02-list-management/02-01-SUMMARY.md
@src/stores/itemsStore.ts
@src/types/item.ts
@src/lib/categories.ts
@src/lib/attribution.ts
@src/pages/ListPage.tsx
@src/components/ui/dialog.tsx
@src/components/ui/select.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@src/components/CreateListForm.tsx
@src/components/ShareBanner.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: NamePromptDialog, AttributionBadge, and AddItemBar components</name>
  <files>src/components/NamePromptDialog.tsx, src/components/AttributionBadge.tsx, src/components/AddItemBar.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (Dialog component exports and prop names — verify close button mechanism)
    - src/components/ui/select.tsx (Select component exports: Select, SelectContent, SelectItem, SelectTrigger, SelectValue)
    - src/components/ui/input.tsx (Input component signature and props)
    - src/components/ui/button.tsx (Button variants: default, outline, ghost, destructive; sizes: default, sm, lg, icon)
    - src/components/CreateListForm.tsx (form submission pattern: e.preventDefault, trim, validation, async handler, error display, disabled during loading)
    - src/lib/attribution.ts (getAttributionColor, getInitials exports)
    - src/lib/categories.ts (SELECTABLE_CATEGORIES array — these are the dropdown values, NOT CATEGORY_ORDER)
    - src/stores/itemsStore.ts (useItemsStore, addItem signature)
    - src/lib/utils.ts (cn utility import path)
    - .planning/phases/02-list-management/02-UI-SPEC.md (Add Item Bar and Name Prompt Dialog contracts)
  </read_first>
  <action>
    Create src/components/NamePromptDialog.tsx:
    - Props: open (boolean), listId (string), onNameSaved (callback with name string)
    - Uses shadcn Dialog in controlled mode: open={open}, onOpenChange set to no-op to prevent dismiss
    - DialogContent with no close button (either hide via prop or do not render DialogClose)
    - DialogHeader with DialogTitle "What's your name?" and DialogDescription "So your partner knows who added what"
    - Single Input (auto-focused) with placeholder "Your name", value from local useState
    - Input must be font-size 16px or larger (text-base class) to prevent iOS zoom on focus — addresses review concern #5
    - Button "Save" with variant default, disabled when input is empty after trim
    - On submit: trim name, call localStorage.setItem(`our-cart-name-${listId}`, name), call onNameSaved(name)
    - Per UI spec: cannot dismiss without entering name
    - Wrap input + button in a form element so Enter key submits naturally

    Create src/components/AttributionBadge.tsx:
    - Props: name (string), className? (string)
    - Renders a 28px circle div (w-7 h-7 rounded-full flex items-center justify-center shrink-0) with background color from getAttributionColor(name).bg via inline style
    - Contains centered text: getInitials(name) in 12px font-semibold, color from getAttributionColor(name).text via inline style
    - aria-label="{name} added this" per accessibility spec — addresses review concern about accessible badge labels
    - Uses cn() for class composition with optional className prop

    Create src/components/AddItemBar.tsx:
    - Props: listId (string), addedBy (string)
    - Local state: name (string), quantity (string), category (string, initially empty ""), expanded (boolean), submitting (boolean)
    - Form with onSubmit handler: e.preventDefault(), guard against double-submit (if submitting return early), validate name.trim() non-empty, set submitting=true, call useItemsStore addItem(listId, name.trim(), quantity.trim() || undefined, category || undefined, addedBy), clear all fields, collapse expanded, set submitting=false. Addresses review concern about duplicate-submit prevention.
    - Add normalization per review: trim name, empty quantity becomes undefined (not stored as empty string), empty category becomes undefined (not "Uncategorized"). Per review concern #3 — Uncategorized is never a stored value.
    - Layout per D-01/D-02/D-03: row with Input (flex-1, placeholder "Add an item...", min-h-[48px] for tap target, text-base class for 16px to prevent iOS zoom) + Button (Plus icon from lucide-react, size "icon" with w-12 h-12 for 48px, primary variant). Addresses review concern #5 (iOS input zoom).
    - Submit on Enter key (native form submit) or button tap
    - Below input row: "More details" text link (text-sm, text-muted-foreground) that toggles expanded state
    - When expanded: row with Input (placeholder "Qty", w-20, text-base for iOS zoom prevention) + Select dropdown with SELECTABLE_CATEGORIES only (NOT CATEGORY_ORDER, NOT Uncategorized) + placeholder "Category". Addresses review concern #3 — dropdown never shows Uncategorized.
    - Category Select uses shadcn Select component; onValueChange sets category state; a "clear" option or leaving unselected results in category = "" (stored as null in DB)
    - Disable form inputs while submitting=true to prevent double-submit
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NamePromptDialog.tsx exports NamePromptDialog component that renders a Dialog with title "What's your name?" and an Input + "Save" Button
    - src/components/NamePromptDialog.tsx calls localStorage.setItem with key pattern "our-cart-name-{listId}"
    - src/components/NamePromptDialog.tsx Input has class containing "text-base" (16px minimum for iOS)
    - src/components/AttributionBadge.tsx exports AttributionBadge that renders a div with aria-label containing "added this"
    - src/components/AttributionBadge.tsx calls getAttributionColor(name) for inline style colors
    - src/components/AddItemBar.tsx exports AddItemBar with an Input placeholder "Add an item..." with text-base class
    - src/components/AddItemBar.tsx has a "More details" expand trigger that reveals quantity Input and category Select
    - src/components/AddItemBar.tsx Select iterates SELECTABLE_CATEGORIES (not CATEGORY_ORDER) — no "Uncategorized" option
    - src/components/AddItemBar.tsx has submitting state that disables form during async operation
    - src/components/AddItemBar.tsx calls addItem with trimmed name and undefined for empty quantity/category (not empty string)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Three components created: NamePromptDialog captures user name with iOS-safe input sizing, AttributionBadge renders colored initials with accessible label, AddItemBar provides item entry with double-submit protection and correct category semantics (no Uncategorized in dropdown). TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: CategorySection, ItemRow, and ListPage integration with lifecycle</name>
  <files>src/components/CategorySection.tsx, src/components/ItemRow.tsx, src/pages/ListPage.tsx</files>
  <read_first>
    - src/pages/ListPage.tsx (current full file — must understand existing structure, useEffect pattern, loading/error rendering)
    - src/components/ShareBanner.tsx (props interface pattern, named export, Tailwind classes)
    - src/stores/itemsStore.ts (useItemsStore exports: items, loading, error, fetchItems, editingItemId)
    - src/types/item.ts (Item interface fields)
    - src/lib/categories.ts (groupItemsByCategory function signature and return type, CATEGORY_ORDER)
    - src/lib/attribution.ts (getAttributionColor, getInitials)
    - src/components/AddItemBar.tsx (just created — props: listId, addedBy)
    - src/components/NamePromptDialog.tsx (just created — props: open, listId, onNameSaved)
    - src/components/AttributionBadge.tsx (just created — props: name)
    - .planning/phases/02-list-management/02-UI-SPEC.md (Item Row, Category Sections, Empty States contracts)
  </read_first>
  <action>
    Create src/components/ItemRow.tsx:
    - Props: item (Item type), isEditing (boolean), onTap (() => void)
    - Display mode (isEditing=false): renders a row with AttributionBadge (item.added_by or "?"), item name (text-base i.e. 16px, flex-1), quantity if present (text-sm, text-muted-foreground, right-aligned)
    - Row has min-h-[48px], px-3, py-2, flex items-center gap-3, border-b border-border, cursor-pointer
    - onClick calls onTap (enters edit mode — actual edit rendering added in Plan 03)
    - Hover/active state: hover:bg-secondary active:bg-secondary
    - For this plan, display mode only — edit mode rendering will be added in Plan 03
    - If item.added_by is null, render "?" as initials with a neutral gray color (no AttributionBadge crash)

    Create src/components/CategorySection.tsx:
    - Props: category (string), items (Item[]), onItemTap ((id: string) => void)
    - Renders category header: text-xs uppercase font-semibold tracking-wide text-muted-foreground bg-secondary px-2 py-1 rounded-sm
    - Header has role="heading" aria-level={3} per accessibility spec
    - Maps over items rendering ItemRow for each, passing isEditing=false (edit state wired in Plan 03), onTap={() => onItemTap(item.id)}
    - Items within a section are already sorted by created_at from the store fetch (ascending) — no additional client sort needed here since groupItemsByCategory preserves insertion order
    - Per D-06: section header is bold, empty categories are never rendered (handled by grouping util)

    Modify src/pages/ListPage.tsx — explicit lifecycle per review concern (validate/load list, load items, load name, render):
    - Add imports: useItemsStore, groupItemsByCategory, NamePromptDialog, AddItemBar, CategorySection
    - Add local state: userName (string | null), initialized to null (not from localStorage yet — wait for list to load)
    - After list loads successfully (existing fetchList succeeds and sets list state): read localStorage.getItem(`our-cart-name-${list.id}`) to initialize userName. If found, set userName. If not, userName stays null which opens the dialog.
    - Add a second useEffect dependent on list.id that calls fetchItems(list.id) from the items store
    - Read items, loading as itemsLoading, error as itemsError from useItemsStore (keep existing loading/error for list fetch separate)
    - If list is loading: show existing "Loading..." state
    - If list error: show existing "List not found" state
    - Once list is loaded: show NamePromptDialog with open={userName === null}, listId=list.id, onNameSaved sets userName state
    - Show items loading state if itemsLoading is true (simple "Loading items..." text below AddItemBar)
    - Show items error state if itemsError is set ("Could not load items" with retry link)
    - Compute grouped = groupItemsByCategory(items) for rendering
    - Render AddItemBar (listId=list.id, addedBy=userName || "") inside the items area div — only fully functional when userName is set
    - Render grouped categories: map over grouped array rendering CategorySection for each, passing category, items, onItemTap that calls setEditingItemId
    - Empty state: when items.length === 0 and not itemsLoading, show "Your list is empty" heading + "Add your first item above" body
    - Keep existing ShareBanner logic intact
    - Read setEditingItemId from useItemsStore and pass as handler to CategorySection onItemTap
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ItemRow.tsx exports ItemRow displaying AttributionBadge + item name (text-base) + optional quantity in a min-h-[48px] tappable row
    - src/components/ItemRow.tsx handles null added_by gracefully (renders "?" or similar fallback)
    - src/components/CategorySection.tsx exports CategorySection rendering a category header (role="heading" aria-level={3}) followed by ItemRow list
    - src/pages/ListPage.tsx imports and renders NamePromptDialog, AddItemBar, and CategorySection components
    - src/pages/ListPage.tsx has a useEffect that calls fetchItems(list.id) when list is loaded
    - src/pages/ListPage.tsx reads loading and error from useItemsStore for items loading/error display
    - src/pages/ListPage.tsx reads userName from localStorage with key "our-cart-name-{list.id}" after list loads
    - src/pages/ListPage.tsx calls groupItemsByCategory(items) and maps result to CategorySection components
    - src/pages/ListPage.tsx shows empty state "Your list is empty" when items array is empty and not loading
    - src/pages/ListPage.tsx shows "Loading items..." or equivalent when itemsLoading is true
    - All existing tests still pass (npx vitest run exits 0)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Complete vertical slice for add-and-view: user sees loading states, enters their name via dialog, adds items via the input bar, and sees items displayed grouped by category with accessible attribution badges. ListPage has proper lifecycle (load list -> load items -> load name -> render). Empty state shown when no items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> item name/quantity/category | Untrusted text rendered in UI |
| localStorage -> added_by column | Device-local name stored in DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Item name/quantity XSS | mitigate | All values rendered via JSX interpolation in ItemRow and CategorySection — React auto-escapes; no dangerouslySetInnerHTML |
| T-02-06 | Spoofing | added_by from localStorage | accept | Cosmetic attribution; name is used for display only, not access control. 2-person private app. |
| T-02-07 | Denial of Service | Rapid item submission | mitigate | submitting flag in AddItemBar prevents duplicate submissions; Supabase rate limits provide additional protection |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — all components use existing dependencies |
</threat_model>

<verification>
- `npx tsc --noEmit` — TypeScript compilation clean
- `npx vitest run` — all tests pass
- Manual verification: open list -> name prompt appears -> enter name -> add item -> item appears in category group with initials badge and accessible label
</verification>

<success_criteria>
- User is prompted for name on first list visit (dialog cannot be dismissed without entry)
- Items can be added via the input bar with Enter or button tap; double-submit prevented
- "More details" reveals quantity and category fields; dropdown shows 8 categories (no Uncategorized)
- Items display grouped by category with correct order per CATEGORY_ORDER
- Each item shows colored initials badge with aria-label "{name} added this"
- All text inputs are 16px+ (text-base) to prevent iOS zoom
- ListPage has explicit lifecycle: load list, fetch items (with loading/error state), load name, render
- Empty state shown when list has no items
- TypeScript and tests pass
</success_criteria>

<output>
Create `.planning/phases/02-list-management/02-02-SUMMARY.md` when done
</output>
