---
phase: 02-list-management
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/NamePromptDialog.tsx
  - src/components/AddItemBar.tsx
  - src/components/AttributionBadge.tsx
  - src/components/CategorySection.tsx
  - src/components/ItemRow.tsx
  - src/pages/ListPage.tsx
autonomous: true
requirements: [LIST-01, LIST-04, LIST-06]

must_haves:
  truths:
    - "User is prompted for their name on first visit to a list (stored in localStorage)"
    - "User can add an item with just a name via the input bar and Enter/tap"
    - "User can expand the add bar to set quantity and category"
    - "Items display grouped by category with bold section headers"
    - "Each item shows a colored initials badge indicating who added it"
    - "Empty categories are not shown; Uncategorized appears at bottom"
  artifacts:
    - path: "src/components/NamePromptDialog.tsx"
      provides: "Modal dialog that captures user name on first list visit"
      min_lines: 30
    - path: "src/components/AddItemBar.tsx"
      provides: "Item entry form with expand for quantity/category"
      min_lines: 50
    - path: "src/components/AttributionBadge.tsx"
      provides: "Colored circle with initials"
      min_lines: 15
    - path: "src/components/CategorySection.tsx"
      provides: "Category header + list of ItemRow components"
      min_lines: 20
    - path: "src/components/ItemRow.tsx"
      provides: "Single item row with badge, name, quantity"
      min_lines: 25
    - path: "src/pages/ListPage.tsx"
      provides: "Orchestrates all components with store integration"
  key_links:
    - from: "src/components/AddItemBar.tsx"
      to: "src/stores/itemsStore.ts"
      via: "useItemsStore addItem action"
      pattern: "useItemsStore.*addItem"
    - from: "src/pages/ListPage.tsx"
      to: "src/stores/itemsStore.ts"
      via: "useItemsStore fetchItems"
      pattern: "useItemsStore.*fetchItems"
    - from: "src/pages/ListPage.tsx"
      to: "src/lib/categories.ts"
      via: "groupItemsByCategory"
      pattern: "groupItemsByCategory"
    - from: "src/components/AttributionBadge.tsx"
      to: "src/lib/attribution.ts"
      via: "getAttributionColor and getInitials"
      pattern: "getAttributionColor|getInitials"
    - from: "src/components/NamePromptDialog.tsx"
      to: "localStorage"
      via: "our-cart-name-{listId} key"
      pattern: "our-cart-name-"
---

<objective>
Vertical slice delivering the core "add and view items" experience: name prompt dialog on first visit, AddItemBar with expand for quantity/category, items displayed grouped by category with attribution badges. After this plan, a user can open the list, enter their name, add grocery items, and see them organized by category with colored initials showing who added each item.

Purpose: This is the primary user-facing vertical slice for Phase 2 — it covers LIST-01 (add item), LIST-04 (attribution), and LIST-06 (category grouping) end-to-end. Per D-01/D-02/D-03 the add bar is a pinned input with expandable details. Per D-04/D-05/D-06 items group by category. Per D-07 each item shows a colored initials badge.

Output: 5 new components + updated ListPage delivering the complete add-and-view flow.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-list-management/02-CONTEXT.md
@.planning/phases/02-list-management/02-RESEARCH.md
@.planning/phases/02-list-management/02-PATTERNS.md
@.planning/phases/02-list-management/02-UI-SPEC.md
@.planning/phases/02-list-management/02-01-SUMMARY.md
@src/stores/itemsStore.ts
@src/types/item.ts
@src/lib/categories.ts
@src/lib/attribution.ts
@src/pages/ListPage.tsx
@src/components/ui/dialog.tsx
@src/components/ui/select.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@src/components/CreateListForm.tsx
@src/components/ShareBanner.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: NamePromptDialog, AttributionBadge, and AddItemBar components</name>
  <files>src/components/NamePromptDialog.tsx, src/components/AttributionBadge.tsx, src/components/AddItemBar.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (Dialog component exports and prop names — verify close button mechanism)
    - src/components/ui/select.tsx (Select component exports: Select, SelectContent, SelectItem, SelectTrigger, SelectValue)
    - src/components/ui/input.tsx (Input component signature and props)
    - src/components/ui/button.tsx (Button variants: default, outline, ghost, destructive; sizes: default, sm, lg, icon)
    - src/components/CreateListForm.tsx (form submission pattern: e.preventDefault, trim, validation, async handler, error display)
    - src/lib/attribution.ts (getAttributionColor, getInitials exports)
    - src/lib/categories.ts (CATEGORIES array — exclude Uncategorized from dropdown)
    - src/stores/itemsStore.ts (useItemsStore, addItem signature)
    - src/lib/utils.ts (cn utility import path)
    - .planning/phases/02-list-management/02-UI-SPEC.md (Add Item Bar and Name Prompt Dialog contracts)
  </read_first>
  <action>
    Create src/components/NamePromptDialog.tsx:
    - Props: open (boolean), listId (string), onNameSaved (callback with name string)
    - Uses shadcn Dialog in controlled mode: open={open}, onOpenChange set to no-op to prevent dismiss
    - DialogContent with no close button (either hide via prop or do not render DialogClose)
    - DialogHeader with DialogTitle "What's your name?" and DialogDescription "So your partner knows who added what"
    - Single Input (auto-focused) with placeholder "Your name", value from local useState
    - Button "Save" with variant default, disabled when input is empty after trim
    - On submit: trim name, call localStorage.setItem(`our-cart-name-${listId}`, name), call onNameSaved(name)
    - Per UI spec: cannot dismiss without entering name

    Create src/components/AttributionBadge.tsx:
    - Props: name (string), className? (string)
    - Renders a 28px circle div with background color from getAttributionColor(name).bg
    - Contains centered text: getInitials(name) in 12px font-semibold, color from getAttributionColor(name).text
    - aria-label="{name} added this" per accessibility spec
    - Uses cn() for class composition

    Create src/components/AddItemBar.tsx:
    - Props: listId (string), addedBy (string)
    - Local state: name (string), quantity (string), category (string), expanded (boolean), loading (boolean)
    - Form with onSubmit handler: e.preventDefault(), validate name.trim() non-empty, call useItemsStore addItem(listId, name.trim(), quantity.trim() || undefined, category || undefined, addedBy), clear all fields, collapse expanded
    - Layout per D-01/D-02/D-03: row with Input (flex-1, placeholder "Add an item...", 48px height) + Button (Plus icon from lucide-react, 48px square, primary variant)
    - Submit on Enter key (native form submit) or button tap
    - Below input row: "More details" text link (14px, muted-foreground) that toggles expanded state
    - When expanded: row with Input (placeholder "Qty", 80px width) + Select dropdown with CATEGORIES (exclude "Uncategorized") + defaulting to empty/no selection (placeholder "Category")
    - Category Select uses shadcn Select component; onValueChange sets category state
    - Per UI spec: input 48px height for mobile tap target
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NamePromptDialog.tsx exports NamePromptDialog component that renders a Dialog with title "What's your name?" and an Input + "Save" Button
    - src/components/NamePromptDialog.tsx calls localStorage.setItem with key pattern "our-cart-name-{listId}"
    - src/components/AttributionBadge.tsx exports AttributionBadge that renders a 28px circle with initials and aria-label
    - src/components/AddItemBar.tsx exports AddItemBar with an Input placeholder "Add an item..." and a Plus icon Button
    - src/components/AddItemBar.tsx has a "More details" expand trigger that reveals quantity Input and category Select
    - src/components/AddItemBar.tsx calls useItemsStore addItem on form submit with trimmed name
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Three components created: NamePromptDialog captures user name and stores in localStorage, AttributionBadge renders colored initials circle, AddItemBar provides the full item entry form with expand for details. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: CategorySection, ItemRow, and ListPage integration</name>
  <files>src/components/CategorySection.tsx, src/components/ItemRow.tsx, src/pages/ListPage.tsx</files>
  <read_first>
    - src/pages/ListPage.tsx (current full file — must understand existing structure before modifying)
    - src/components/ShareBanner.tsx (props interface pattern, named export, Tailwind classes)
    - src/stores/itemsStore.ts (useItemsStore exports: items, loading, fetchItems, editingItemId)
    - src/types/item.ts (Item interface fields)
    - src/lib/categories.ts (groupItemsByCategory function signature and return type)
    - src/lib/attribution.ts (getAttributionColor, getInitials)
    - src/components/AddItemBar.tsx (just created — props: listId, addedBy)
    - src/components/NamePromptDialog.tsx (just created — props: open, listId, onNameSaved)
    - src/components/AttributionBadge.tsx (just created — props: name)
    - .planning/phases/02-list-management/02-UI-SPEC.md (Item Row, Category Sections, Empty States contracts)
  </read_first>
  <action>
    Create src/components/ItemRow.tsx:
    - Props: item (Item type), isEditing (boolean), onTap (() => void)
    - Display mode (isEditing=false): renders a row with AttributionBadge (item.added_by or "?"), item name (16px, flex-1), quantity if present (14px, muted-foreground, right-aligned)
    - Row has min-h-[48px], px-3, py-2, flex items-center gap-3, border-b border-border (except last via CSS), cursor-pointer
    - onClick calls onTap (enters edit mode — handled by parent/store in Plan 03)
    - Hover/active state: hover:bg-secondary
    - For this plan, display mode only — edit mode rendering will be added in Plan 03

    Create src/components/CategorySection.tsx:
    - Props: category (string), items (Item[]), editingItemId (string | null), onItemTap ((id: string) => void)
    - Renders category header: uppercase, 14px/font-semibold, text-muted-foreground, bg-secondary, px-2 py-1
    - Header has role="heading" aria-level={3} per accessibility spec
    - Maps over items rendering ItemRow for each, passing isEditing={item.id === editingItemId}
    - Per D-06: section header is bold, empty categories are never rendered (handled by grouping util)

    Modify src/pages/ListPage.tsx:
    - Add imports: useItemsStore, groupItemsByCategory, NamePromptDialog, AddItemBar, CategorySection
    - Add local state: userName (string | null), initialized from localStorage.getItem(`our-cart-name-${list.id}`) after list loads
    - Add useEffect that calls fetchItems(list.id) after list is loaded (add to existing useEffect or new one dependent on list)
    - Read items, loading (items loading), editingItemId from useItemsStore
    - Compute grouped = groupItemsByCategory(items) for rendering
    - Render NamePromptDialog with open={!userName} after list loads, onNameSaved sets userName state
    - Render AddItemBar (listId=list.id, addedBy=userName) inside the items area div — only when userName is set
    - Render grouped categories: map over grouped array rendering CategorySection for each
    - Empty state: when items.length === 0 and not loading, show "Your list is empty" heading + "Add your first item above" body
    - Keep existing ShareBanner logic intact
    - setEditingItemId passed as onItemTap to CategorySection
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ItemRow.tsx exports ItemRow displaying AttributionBadge + item name + optional quantity in a tappable row
    - src/components/CategorySection.tsx exports CategorySection rendering a category header (role="heading" aria-level={3}) followed by ItemRow list
    - src/pages/ListPage.tsx imports and renders NamePromptDialog, AddItemBar, and CategorySection components
    - src/pages/ListPage.tsx calls useItemsStore fetchItems(list.id) when list is loaded
    - src/pages/ListPage.tsx calls groupItemsByCategory(items) and maps result to CategorySection components
    - src/pages/ListPage.tsx shows empty state "Your list is empty" when items array is empty
    - src/pages/ListPage.tsx reads userName from localStorage with key "our-cart-name-{list.id}"
    - All existing tests still pass (npx vitest run exits 0)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Complete vertical slice for add-and-view: user can enter their name, add items via the input bar, see items displayed grouped by category with attribution badges. Empty state shown when no items. ListPage orchestrates all components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → item name/quantity/category | Untrusted text rendered in UI |
| localStorage → added_by column | Device-local name stored in DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Item name/quantity XSS | mitigate | All values rendered via JSX interpolation in ItemRow and CategorySection — React auto-escapes; no dangerouslySetInnerHTML |
| T-02-05 | Spoofing | added_by from localStorage | accept | Cosmetic attribution; name is used for display only, not access control. 2-person private app. |
| T-02-06 | Denial of Service | Rapid item submission | accept | 2-person app; Supabase rate limits protect against abuse. No client-side throttle needed. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan — all components use existing dependencies |
</threat_model>

<verification>
- `npx tsc --noEmit` — TypeScript compilation clean
- `npx vitest run` — all tests pass
- Manual verification: open list → name prompt appears → enter name → add item → item appears in category group with initials badge
</verification>

<success_criteria>
- User is prompted for name on first list visit (dialog cannot be dismissed without entry)
- Items can be added via the input bar with Enter or button tap
- "More details" reveals quantity and category fields
- Items display grouped by category with correct order per CATEGORIES array
- Each item shows colored initials badge from the person who added it
- Empty state shown when list has no items
- TypeScript and tests pass
</success_criteria>

<output>
Create `.planning/phases/02-list-management/02-02-SUMMARY.md` when done
</output>
