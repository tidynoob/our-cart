---
phase: 02-list-management
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/components/ItemRow.tsx
  - src/components/DeleteConfirmation.tsx
  - src/pages/ListPage.tsx
autonomous: true
requirements: [LIST-02, LIST-03]

must_haves:
  truths:
    - "User can tap an item row to enter inline edit mode with editable name, quantity, and category fields"
    - "Edits save on blur or Enter key press"
    - "Trash icon appears only in edit mode"
    - "Tapping trash shows inline delete confirmation with Cancel and Delete buttons"
    - "Confirming delete removes the item from the list"
    - "User can cancel edit mode without saving changes"
  artifacts:
    - path: "src/components/ItemRow.tsx"
      provides: "Display mode + inline edit mode with save on blur/Enter"
      min_lines: 60
    - path: "src/components/DeleteConfirmation.tsx"
      provides: "Inline confirmation row with Cancel and Delete buttons"
      min_lines: 20
  key_links:
    - from: "src/components/ItemRow.tsx"
      to: "src/stores/itemsStore.ts"
      via: "useItemsStore updateItem and setEditingItemId"
      pattern: "useItemsStore.*(updateItem|setEditingItemId)"
    - from: "src/components/ItemRow.tsx"
      to: "src/stores/itemsStore.ts"
      via: "useItemsStore setDeletingItemId"
      pattern: "setDeletingItemId"
    - from: "src/components/DeleteConfirmation.tsx"
      to: "src/stores/itemsStore.ts"
      via: "useItemsStore deleteItem"
      pattern: "useItemsStore.*deleteItem"
---

<objective>
Vertical slice delivering inline edit and delete functionality: tap an item row to edit its name/quantity/category in place, save on blur or Enter, delete with trash icon and inline confirmation. After this plan, a user can modify or remove any item from the list.

Purpose: Completes LIST-02 (edit item) and LIST-03 (delete item) requirements. Per D-09, edit mode is inline — fields become editable in the same row. Per D-10, trash icon only appears in edit mode. Per D-11, delete requires inline confirmation with Cancel/Delete buttons before removal.

Output: Updated ItemRow with edit mode, new DeleteConfirmation component, minor ListPage adjustment.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-list-management/02-CONTEXT.md
@.planning/phases/02-list-management/02-RESEARCH.md
@.planning/phases/02-list-management/02-PATTERNS.md
@.planning/phases/02-list-management/02-UI-SPEC.md
@.planning/phases/02-list-management/02-01-SUMMARY.md
@.planning/phases/02-list-management/02-02-SUMMARY.md
@src/components/ItemRow.tsx
@src/stores/itemsStore.ts
@src/types/item.ts
@src/lib/categories.ts
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@src/components/ui/select.tsx
@src/pages/ListPage.tsx
@src/components/CategorySection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeleteConfirmation component and ItemRow edit mode</name>
  <files>src/components/DeleteConfirmation.tsx, src/components/ItemRow.tsx</files>
  <read_first>
    - src/components/ItemRow.tsx (current display-only implementation from Plan 02 — understand props, structure, imports)
    - src/stores/itemsStore.ts (updateItem, deleteItem, setEditingItemId, setDeletingItemId, editingItemId, deletingItemId)
    - src/types/item.ts (Item interface — fields available for editing)
    - src/lib/categories.ts (CATEGORIES array — needed for category Select in edit mode)
    - src/components/ui/input.tsx (Input component props)
    - src/components/ui/button.tsx (Button variants: ghost, destructive; sizes: sm, icon)
    - src/components/ui/select.tsx (Select, SelectContent, SelectItem, SelectTrigger, SelectValue exports)
    - src/components/ShareBanner.tsx (button group pattern: flex items-center gap-2)
    - .planning/phases/02-list-management/02-UI-SPEC.md (Edit Mode and Delete Confirmation contracts)
  </read_first>
  <action>
    Create src/components/DeleteConfirmation.tsx:
    - Props: itemId (string), onCancel (() => void)
    - Reads deleteItem from useItemsStore
    - Renders a row with background transition to light red (bg-red-50 or oklch(0.95 0.05 27)), px-3 py-2, flex items-center justify-between
    - Left side: "Delete this item?" text in 14px
    - Right side: two buttons — "Cancel" (variant ghost, size sm, calls onCancel) and "Delete" (variant destructive, size sm, calls deleteItem(itemId))
    - Focus management: auto-focus the Cancel button on mount (safest default per accessibility)

    Modify src/components/ItemRow.tsx to add edit mode:
    - Add props: isDeleting (boolean), onDelete (() => void) — in addition to existing item, isEditing, onTap
    - When isDeleting is true: render DeleteConfirmation instead of the normal row, passing itemId=item.id, onCancel that calls setDeletingItemId(null) from store
    - When isEditing is true: replace display content with edit fields:
      - Local state for editName, editQuantity, editCategory — initialized from item.name, item.quantity, item.category on entering edit mode (use useEffect with isEditing dependency to reset values)
      - Row 1: Input for name (flex-1, value=editName, autoFocus), Input for quantity (w-16, value=editQuantity, placeholder "Qty")
      - Row 2: Select for category (value=editCategory, CATEGORIES excluding Uncategorized as options, placeholder "Category"), Trash2 icon button (variant ghost, size icon, onClick calls setDeletingItemId(item.id) from store), "Cancel" text button (variant ghost, size sm, onClick calls setEditingItemId(null))
      - Save on blur: handleSave function that calls updateItem(item.id, { name: editName.trim(), quantity: editQuantity.trim() || null, category: editCategory || null }) — only if values actually changed
      - Save on Enter key: onKeyDown handler on name input that calls handleSave + setEditingItemId(null) on Enter
      - Blur handler on name and quantity inputs calls handleSave
      - Validation: name must be non-empty after trim; if empty on blur, revert to original item.name without saving
    - Import Trash2 from lucide-react, Input from @/components/ui/input, Button from @/components/ui/button, Select/SelectContent/SelectItem/SelectTrigger/SelectValue from @/components/ui/select
    - Import CATEGORIES from @/lib/categories (filter out "Uncategorized" for dropdown options)
    - Import useItemsStore for updateItem, setEditingItemId, setDeletingItemId
    - Per D-09: save on blur or Enter. Per D-10: trash only in edit mode. Per D-11: confirmation before delete.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DeleteConfirmation.tsx exports DeleteConfirmation with "Delete this item?" text and Cancel + Delete buttons
    - src/components/DeleteConfirmation.tsx Delete button calls useItemsStore deleteItem(itemId)
    - src/components/ItemRow.tsx renders Input fields for name and quantity when isEditing=true
    - src/components/ItemRow.tsx renders Select dropdown for category when isEditing=true
    - src/components/ItemRow.tsx renders Trash2 icon button only when isEditing=true
    - src/components/ItemRow.tsx has handleSave function that calls updateItem with changed values
    - src/components/ItemRow.tsx handles Enter key on name input to trigger save
    - src/components/ItemRow.tsx renders DeleteConfirmation when isDeleting=true
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ItemRow supports both display mode and inline edit mode. Edit mode shows name/quantity inputs, category dropdown, trash icon, and cancel button. Save triggers on blur/Enter. Delete shows inline confirmation. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire edit/delete state in ListPage and CategorySection</name>
  <files>src/components/CategorySection.tsx, src/pages/ListPage.tsx</files>
  <read_first>
    - src/pages/ListPage.tsx (current state after Plan 02 — understand all imports, store usage, rendering)
    - src/components/CategorySection.tsx (current implementation — props, how it maps ItemRow)
    - src/components/ItemRow.tsx (updated props: item, isEditing, isDeleting, onTap, onDelete)
    - src/stores/itemsStore.ts (editingItemId, deletingItemId, setEditingItemId, setDeletingItemId selectors)
  </read_first>
  <action>
    Modify src/components/CategorySection.tsx:
    - Add prop: deletingItemId (string | null) — alongside existing editingItemId
    - Add prop: onItemDelete ((id: string) => void) — alongside existing onItemTap
    - Pass isDeleting={item.id === deletingItemId} and onDelete={() => onItemDelete(item.id)} to each ItemRow

    Modify src/pages/ListPage.tsx:
    - Read deletingItemId from useItemsStore in addition to existing editingItemId
    - Read setDeletingItemId from useItemsStore
    - Pass deletingItemId and onItemDelete={() => setDeletingItemId(id)} to each CategorySection
    - Ensure clicking outside an editing item (clicking another item or the background) exits edit mode: the onItemTap handler should call setEditingItemId(id) which already clears deletingItemId in the store action
    - No other changes needed — the store actions handle all optimistic update/rollback logic
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CategorySection.tsx passes isDeleting and onDelete props to each ItemRow
    - src/pages/ListPage.tsx reads deletingItemId from useItemsStore
    - src/pages/ListPage.tsx passes deletingItemId to CategorySection components
    - Tapping an item row sets editingItemId in store (entering edit mode)
    - Tapping a different item while one is editing switches edit mode to the new item
    - All existing tests pass (npx vitest run exits 0)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Edit and delete flows are fully wired: tap item → edit mode with fields, blur/Enter saves, trash → confirmation → delete removes item. All state managed through itemsStore with optimistic updates. Full CRUD functionality complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User edit input → Supabase UPDATE | Modified name/quantity/category sent to DB |
| User delete action → Supabase DELETE | Item removal request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Edited item name XSS | mitigate | React JSX auto-escaping on all Input values and rendered text; no dangerouslySetInnerHTML |
| T-02-08 | Elevation of Privilege | Unauthorized UPDATE | mitigate | RLS policy anon_update_items enforces list_id FK exists in lists table (applied in Plan 01 migration) |
| T-02-09 | Elevation of Privilege | Unauthorized DELETE | mitigate | RLS policy anon_delete_items enforces list_id FK exists in lists table (applied in Plan 01 migration) |
| T-02-10 | Repudiation | Item deletion without trace | accept | 2-person private app; no audit trail needed. Deletion is confirmed via UI prompt per D-11. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` — TypeScript compilation clean
- `npx vitest run` — all tests pass
- Manual verification: tap item → edit mode → change name → blur saves → tap trash → "Delete this item?" → confirm → item removed
</verification>

<success_criteria>
- Tapping an item row enters inline edit mode with editable name, quantity, and category fields
- Blur or Enter key saves edits to Supabase via optimistic update
- Trash icon visible only in edit mode; tapping shows delete confirmation
- Delete confirmation has Cancel and Delete buttons; confirm removes item
- Cancel button exits edit mode without saving
- Only one item can be in edit mode at a time
- All TypeScript and vitest checks pass
</success_criteria>

<output>
Create `.planning/phases/02-list-management/02-03-SUMMARY.md` when done
</output>
