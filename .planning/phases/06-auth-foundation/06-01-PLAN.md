---
phase: 06-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/authStore.test.ts
  - src/components/auth/ProtectedRoute.test.tsx
  - src/pages/LandingPage.test.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Supabase project has Google OAuth provider enabled"
    - "OAuth redirect URLs are configured in all 3 required locations"
    - "authStore.test.ts exists with failing tests for AUTH-01 and AUTH-02 behaviors"
    - "ProtectedRoute.test.tsx exists with failing tests for AUTH-03 behaviors"
    - "LandingPage.test.tsx has new failing tests for conditional auth rendering"
  artifacts:
    - path: "src/stores/authStore.test.ts"
      provides: "Unit tests for authStore — signInWithGoogle, initialize, session restoration"
      exports: []
    - path: "src/components/auth/ProtectedRoute.test.tsx"
      provides: "Unit tests for ProtectedRoute — loading spinner, redirect, outlet render, sessionStorage"
      exports: []
    - path: "src/pages/LandingPage.test.tsx"
      provides: "Tests for LandingPage conditional rendering (login screen vs authenticated content)"
      exports: []
  key_links:
    - from: "src/stores/authStore.test.ts"
      to: "src/stores/authStore.ts"
      via: "import useAuthStore"
      pattern: "useAuthStore"
    - from: "src/components/auth/ProtectedRoute.test.tsx"
      to: "src/components/auth/ProtectedRoute.tsx"
      via: "import ProtectedRoute"
      pattern: "ProtectedRoute"
---

<objective>
Configure OAuth provider in Supabase and Google Cloud Console (blocking human checkpoint), then create all Wave 0 test scaffolds. These test files are written with failing tests first — they will fail because authStore.ts and ProtectedRoute.tsx do not yet exist. Plans 02 and 03 implement the code that makes them pass.

Purpose: Satisfy Nyquist rule (automated verify exists before code is written) and unblock OAuth testing. The OAuth configuration is a hard prerequisite — no auth code can be end-to-end tested without it.

Output: Configured OAuth providers and three test scaffold files with RED tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-auth-foundation/06-CONTEXT.md
@.planning/phases/06-auth-foundation/06-RESEARCH.md
@.planning/phases/06-auth-foundation/06-PATTERNS.md
@.planning/phases/06-auth-foundation/06-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure Google OAuth in Supabase Dashboard and Google Cloud Console</name>
  <read_first>
    .planning/phases/06-auth-foundation/06-RESEARCH.md — Pattern 10: OAuth Redirect URL Matrix, Pitfall 3: Redirect URL Mismatch, Environment Availability section
  </read_first>
  <what-built>
    Plans 02–04 implement Google OAuth sign-in. Before any OAuth flow can be tested end-to-end, three places must be configured manually. Claude cannot access the Supabase Dashboard or Google Cloud Console.
  </what-built>
  <how-to-verify>
    Complete ALL steps before resuming. Missing any step will cause OAuth redirect_uri_mismatch errors.

    === SUPABASE DASHBOARD ===

    Step 1 — Enable Google OAuth provider:
    - Go to: Supabase Dashboard → Authentication → Providers → Google
    - Toggle "Enable Sign in with Google" ON
    - Leave Client ID and Client Secret blank for now (filled in after Google Cloud Console step)

    Step 2 — Set Site URL:
    - Go to: Supabase Dashboard → Authentication → URL Configuration → Site URL
    - Set to: http://localhost:5173
    - (Production: will be updated to Vercel domain before prod deployment)

    Step 3 — Add Redirect URLs allowlist:
    - Go to: Supabase Dashboard → Authentication → URL Configuration → Redirect URLs
    - Add: http://localhost:5173/**
    - (Production: add https://your-app.vercel.app/** when deploying)

    === GOOGLE CLOUD CONSOLE ===

    Step 4 — Create OAuth 2.0 credentials:
    - Go to: https://console.cloud.google.com → APIs & Services → Credentials
    - Click "Create Credentials" → "OAuth 2.0 Client ID"
    - Application type: Web application
    - Name: "Our Cart"
    - Under "Authorized redirect URIs" add EXACTLY:
      https://<your-project-ref>.supabase.co/auth/v1/callback
      (Replace <your-project-ref> with your Supabase project reference, found in Project Settings → General)
    - Click Save. Copy the Client ID and Client Secret.

    Step 5 — Return to Supabase and paste credentials:
    - Go back to: Supabase Dashboard → Authentication → Providers → Google
    - Paste the Client ID and Client Secret from Step 4
    - Click Save

    === VERIFICATION ===

    Step 6 — Confirm all three locations are configured:
    - Supabase Dashboard → Authentication → Providers: Google shows as "Enabled"
    - Supabase Dashboard → Authentication → URL Configuration → Site URL: http://localhost:5173
    - Supabase Dashboard → Authentication → URL Configuration → Redirect URLs: includes http://localhost:5173/**
    - Google Cloud Console → Credentials → OAuth 2.0 Client ID shows the Supabase callback URI

    Expected: All four confirmation points visible. OAuth will not work until all four are true.
  </how-to-verify>
  <resume-signal>Type "configured" when all six steps are complete and all four verification points are confirmed.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 test scaffolds for authStore, ProtectedRoute, and LandingPage</name>
  <files>
    src/stores/authStore.test.ts
    src/components/auth/ProtectedRoute.test.tsx
    src/pages/LandingPage.test.tsx
  </files>
  <read_first>
    src/stores/itemsStore.test.ts — full file (vi.hoisted mock pattern, beforeEach reset, describe/it structure)
    src/pages/ListPage.test.tsx — lines 1–110 (MemoryRouter render helper, store setState injection, vi.mock supabase)
    .planning/phases/06-auth-foundation/06-PATTERNS.md — Pattern assignments for authStore.test.ts and ProtectedRoute.test.tsx sections
    .planning/phases/06-auth-foundation/06-VALIDATION.md — Per-Task Verification Map and Wave 0 Requirements
  </read_first>
  <behavior>
    authStore.test.ts behaviors (all initially RED — authStore.ts does not exist yet):
    - initialize() calls supabase.auth.onAuthStateChange exactly once
    - initialize() sets user and session from the INITIAL_SESSION event callback
    - initialize() sets isLoading: false after the callback fires
    - initialize() sets isLoading: false when callback receives null session (not signed in)
    - initialize() calls supabase.realtime.setAuth(token) with the session access_token
    - initialize() calls supabase.realtime.setAuth(null) when session is null
    - initialize() returns a cleanup function that calls subscription.unsubscribe()
    - signInWithGoogle() calls supabase.auth.signInWithOAuth with provider: 'google'
    - signInWithGoogle() sets error state when signInWithOAuth returns an error

    ProtectedRoute.test.tsx behaviors (all initially RED — ProtectedRoute.tsx does not exist yet):
    - renders a loading spinner (animate-spin element) when isLoading: true
    - redirects to "/" when isLoading: false and user: null
    - stores location.pathname in sessionStorage('returnTo') before redirecting
    - renders Outlet content when isLoading: false and user is populated

    LandingPage.test.tsx behaviors (new tests appended to existing file, RED until Plan 04):
    - renders a button with text "Sign in with Google" when user: null and isLoading: false
    - renders "Create a list" heading when user is populated and isLoading: false
    - renders loading spinner (animate-spin element) when isLoading: true
  </behavior>
  <action>
    Write src/stores/authStore.test.ts using the exact vi.hoisted + vi.mock pattern from itemsStore.test.ts. The mock captures the onAuthStateChange callback so tests can simulate auth events by calling the captured callback directly. Use vi.hoisted to define mockOnAuthStateChange (captures callback), mockSignInWithOAuth, mockSignOut, and a mockSetAuth for supabase.realtime. Mock '@/lib/supabase' to expose supabase.auth and supabase.realtime. In beforeEach, call vi.clearAllMocks() and reset the store state via useAuthStore.setState({ user: null, session: null, isLoading: true, error: null }). Tests call initialize() then invoke the captured callback with a fake session object ({ user: { id: 'user-1' }, access_token: 'tok' }) or null to simulate sign-in / not-signed-in states.

    Write src/components/auth/ProtectedRoute.test.tsx using the MemoryRouter render helper pattern from ListPage.test.tsx. Create a renderAtRoute(path) helper that wraps ProtectedRoute as a layout route over a /list/:code child route, with a / fallback route rendering "Home". Inject auth state via useAuthStore.setState(...) in each test — no direct supabase calls needed in ProtectedRoute so the supabase mock only needs the auth and realtime namespaces. The sessionStorage test should call sessionStorage.clear() in beforeEach and assert sessionStorage.getItem('returnTo') equals '/list/TEST' after rendering at '/list/TEST' with user: null.

    Read src/pages/LandingPage.test.tsx first to see its current structure and existing imports. Append a new describe block "auth-conditional rendering" with the three new behaviors listed above. The tests should use useAuthStore.setState() to inject auth state and look for 'Sign in with Google' text for the login case and 'Create a list' text for the authenticated case. The supabase mock from the existing file's vi.mock will apply to these new tests — do not add a second vi.mock call.

    Important: All test files import from modules that do not yet exist (authStore.ts, ProtectedRoute.tsx). Running the test suite after this task will produce compile/import errors, not test failures. That is expected — Wave 0 means the test infrastructure exists; Plans 02 and 03 create the implementations.
  </action>
  <verify>
    <automated>cd "C:/Users/mitch/Documents/CodingProjects/our-cart" && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/authStore.test.ts exists and imports from '@/stores/authStore'
    - src/components/auth/ProtectedRoute.test.tsx exists and imports ProtectedRoute from '@/components/auth/ProtectedRoute'
    - src/pages/LandingPage.test.tsx has a describe block containing a test with text 'Sign in with Google'
    - The three test files cover all 12 behaviors listed in the behavior block (grep for describe/it/test strings)
    - Existing 93 tests still pass (the new test files produce import errors but do not break passing tests — vitest reports them as error/fail, not as breaking the existing suite)
    - No vi.mock('@/lib/supabase') appears twice in the same test file
  </acceptance_criteria>
  <done>Three test scaffold files exist with behaviors matching the Wave 0 requirements in VALIDATION.md. Existing test suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Google OAuth | Browser is redirected to Google; Supabase allowlist validates redirectTo |
| Supabase Auth → App | Supabase controls allowed redirect destinations via allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | OAuth redirect URI manipulation | Mitigate | Supabase allowlist validates redirectTo values server-side; wildcard scoped to app origin only (http://localhost:5173/**) |
| T-06-02 | Information Disclosure | Google Client Secret in Supabase Dashboard | Accept | Secret is stored in Supabase Dashboard (not in code); never committed to git; Supabase encrypts provider credentials at rest |
| T-06-SC | Tampering | npm/pip/cargo installs | Accept | No new package installs in this plan — all packages already installed (RESEARCH.md Package Legitimacy Audit confirms no new dependencies) |
</threat_model>

<verification>
After both tasks complete:
1. Supabase Dashboard → Authentication → Providers: Google provider is enabled with Client ID + Secret
2. Supabase Dashboard → Authentication → URL Configuration: Site URL = http://localhost:5173, Redirect URLs includes http://localhost:5173/**
3. Google Cloud Console → Credentials: OAuth 2.0 Client ID exists with https://<project-ref>.supabase.co/auth/v1/callback as authorized redirect URI
4. src/stores/authStore.test.ts exists
5. src/components/auth/ProtectedRoute.test.tsx exists
6. src/pages/LandingPage.test.tsx contains 'Sign in with Google' test string
7. npx vitest run — existing 93 tests still pass (new files may error due to missing implementations, which is expected)
</verification>

<success_criteria>
- OAuth pre-code checklist complete (3-location redirect URL matrix configured per D-12/D-13)
- Wave 0 test scaffolds created for all three required files (VALIDATION.md Wave 0 Requirements satisfied)
- Existing test suite integrity maintained — 93 tests still passing
</success_criteria>

<output>
Create .planning/phases/06-auth-foundation/06-01-SUMMARY.md when done
</output>
