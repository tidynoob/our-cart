---
phase: 10-list-sharing
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/pages/InvitePage.test.tsx
  - src/components/ShareBanner.test.tsx
autonomous: true
requirements:
  - SHARE-02
must_haves:
  truths:
    - "InvitePage test file exists with failing stubs covering spinner, valid-redirect, null-return, and error-return cases"
    - "ShareBanner test file has an assertion that the copied URL contains /invite/ not /list/"
  artifacts:
    - path: "src/pages/InvitePage.test.tsx"
      provides: "SHARE-02 unit test stubs (mock supabase.rpc)"
      contains: "redeem_invite"
    - path: "src/components/ShareBanner.test.tsx"
      provides: "Updated URL-prefix assertion for D-05"
      contains: "/invite/"
  key_links:
    - from: "src/pages/InvitePage.test.tsx"
      to: "src/pages/InvitePage.tsx"
      via: "component import (file not yet created — stubs skip import to avoid compile error)"
      pattern: "InvitePage"
---

<objective>
Create Wave 0 test stubs for Phase 10 so the Nyquist gate has RED tests ready before any implementation begins.

Purpose: Tests must exist (and fail correctly) before the code they cover is written — this establishes the RED state for InvitePage and cements the /invite/ URL contract for ShareBanner.
Output: src/pages/InvitePage.test.tsx (new), src/components/ShareBanner.test.tsx (modified — one new assertion added).
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-list-sharing/10-CONTEXT.md
@.planning/phases/10-list-sharing/10-RESEARCH.md
@.planning/phases/10-list-sharing/10-PATTERNS.md
@.planning/phases/10-list-sharing/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create InvitePage.test.tsx with failing stubs</name>
  <files>src/pages/InvitePage.test.tsx</files>
  <read_first>
    - src/pages/ListPage.test.tsx — import block pattern, MemoryRouter + Routes + Route scaffold, vi.mock supabase pattern, renderAtRoute helper, beforeEach vi.clearAllMocks, waitFor usage
    - .planning/phases/10-list-sharing/10-PATTERNS.md §"src/pages/InvitePage.test.tsx" — complete test scaffold with rpc mock, renderAtRoute, all 5 test scenarios
    - .planning/phases/10-list-sharing/10-VALIDATION.md §Wave 0 Requirements — expected stub behaviors
  </read_first>
  <behavior>
    - Test 1 (stub): renders spinner element (class `animate-spin`) before RPC resolves — RPC promise never settles; expect spinner in DOM
    - Test 2 (stub): on RPC returning `{ data: { list_id: 'uuid', share_code: 'ABC12345' }, error: null }` → navigates to `/list/ABC12345` with `replace: true`
    - Test 3 (stub): on RPC returning `{ data: null, error: null }` → renders "This invite link is invalid or has expired."
    - Test 4 (stub): on RPC returning `{ data: null, error: { message: 'db error' } }` → renders invalid text (same condition as null data)
    - Test 5 (stub): navigate is called with `{ replace: true }` on success path
  </behavior>
  <action>
    Create src/pages/InvitePage.test.tsx. InvitePage.tsx does NOT exist yet — the test file must NOT import it directly or it will fail to compile. Use a lazy/dynamic import strategy OR mark the test with `it.todo` stubs for all 5 scenarios so the file is valid TypeScript but all tests are pending (todo). This is the Wave 0 stub pattern: stubs exist and show up in the test run as pending, not as compile errors.

    Structure: `describe('InvitePage', () => { it.todo('renders spinner while redeeming'); it.todo('navigates to /list/:code with replace:true on valid RPC response'); it.todo('shows invalid invite message when data is null'); it.todo('shows invalid invite message when error is returned'); it.todo('does not navigate when code param is missing') })`.

    Use `vi.mock('@/lib/supabase', ...)` stub at the top even if unused yet, so the import wiring is ready for Task 1 expansion in Wave 3 (10-03-PLAN.md). Module mock: `{ supabase: { rpc: vi.fn() } }`.

    Follow the exact import ordering and describe/it style from ListPage.test.tsx. Use `@/` path alias on all imports. Do not import InvitePage.tsx itself in this stub file — the component does not exist yet.
  </action>
  <verify>
    <automated>npx vitest run src/pages/InvitePage --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|todo|pending|InvitePage)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/pages/InvitePage.test.tsx exists and contains the string "redeem_invite" (in the vi.mock block) and "animate-spin" or "spinner" (in a todo description or comment)
    - `npx vitest run src/pages/InvitePage` exits 0 (all stubs are todo — not failing)
    - Output shows at least 4 `todo` tests under describe('InvitePage')
    - No TypeScript compile errors from the file
  </acceptance_criteria>
  <done>InvitePage.test.tsx exists, npx vitest run src/pages/InvitePage exits 0, output shows 4+ todo stubs under InvitePage describe block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add /invite/ URL assertion to ShareBanner.test.tsx</name>
  <files>src/components/ShareBanner.test.tsx</files>
  <read_first>
    - src/components/ShareBanner.test.tsx — full existing file; note the "Copy link" test at line 42 that asserts stringContaining('ABC12345')
    - .planning/phases/10-list-sharing/10-PATTERNS.md §"src/components/ShareBanner.test.tsx" — the exact new assertion to add
    - src/components/ShareBanner.tsx line 14 — current URL shape (still /list/ until 10-04-PLAN.md changes it)
  </read_first>
  <behavior>
    - Existing test "clicking Copy link calls navigator.clipboard.writeText with the full URL containing listCode": currently passes with `/list/ABC12345`
    - New assertion added to the same test: `expect(navigator.clipboard.writeText).toHaveBeenCalledWith(expect.stringContaining('/invite/ABC12345'))` — this assertion MUST FAIL at Wave 0 (RED state) because ShareBanner still uses `/list/` until 10-04 implements D-05
  </behavior>
  <action>
    In the existing "clicking Copy link" test (the `it` block at line 42 of ShareBanner.test.tsx), add a second `expect` assertion inside the `waitFor` callback after the existing `stringContaining('ABC12345')` check:

    `expect(navigator.clipboard.writeText).toHaveBeenCalledWith(expect.stringContaining('/invite/ABC12345'))`

    This is the only change to this file. Keep all other tests, imports, mocks, and the DEFAULT_PROPS constant exactly as-is. The new assertion will fail (RED) until 10-04 changes ShareBanner line 14.
  </action>
  <verify>
    <automated>npx vitest run src/components/ShareBanner --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|invite|copy)"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ShareBanner.test.tsx contains the string "/invite/ABC12345"
    - Running `npx vitest run src/components/ShareBanner` shows the "clicking Copy link" test FAILING (RED) because ShareBanner.tsx still has `/list/` on line 14
    - All other tests in the describe block still pass (renders listCode, Dismiss button, Share button presence/absence)
    - No TypeScript errors in the file
  </acceptance_criteria>
  <done>ShareBanner.test.tsx contains /invite/ABC12345 assertion; the Copy link test shows RED (failing) while all other ShareBanner tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → source file | Tests import from app source; no untrusted input in Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-W0-01 | Tampering | Test files | accept | Wave 0 creates only test stubs; no production code paths open. Test files cannot introduce runtime vulnerabilities. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase per RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
Run after both tasks:
1. `npx vitest run src/pages/InvitePage` — exits 0, shows 4+ todo stubs
2. `npx vitest run src/components/ShareBanner` — 1 failing test (Copy link /invite/ assertion RED), 4 passing
3. `npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- InvitePage.test.tsx exists with 4+ it.todo stubs; file compiles clean
- ShareBanner.test.tsx has /invite/ABC12345 assertion that fails (RED) until 10-04 implements D-05
- Full vitest suite (`npx vitest run`) shows: 1 new failure (ShareBanner Copy link) + 4 pending todos; no regressions on other tests
</success_criteria>

<output>
Create .planning/phases/10-list-sharing/10-00-SUMMARY.md when done
</output>
