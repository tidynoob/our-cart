---
phase: 10-list-sharing
plan: "06"
type: execute
wave: 6
depends_on:
  - "10-05"
files_modified:
  - supabase/migrations/lists_membership.sql
  - supabase/migrations/items_membership.sql
autonomous: false
requirements:
  - SHARE-01
gap_closure: true
must_haves:
  truths:
    - "An authenticated user sees only lists they own (owner_id = auth.uid()) or are a member of (is_list_member)"
    - "The unconditional owner_id IS NULL branch is gone from all lists and items RLS policies"
    - "Legacy null-owner rows in the live DB are resolved (deleted, backfilled, or given membership rows) before the open grant is removed"
    - "Both Gmail accounts see separate list sets — neither sees the other account's lists"
  artifacts:
    - path: "supabase/migrations/lists_membership.sql"
      provides: "Corrected lists SELECT/UPDATE/DELETE policies — no unconditional IS NULL branch"
      contains: "is_list_member"
    - path: "supabase/migrations/items_membership.sql"
      provides: "Corrected items SELECT/INSERT/UPDATE/DELETE policies — no unconditional IS NULL branch"
      contains: "is_list_member"
  key_links:
    - from: "lists_select USING clause"
      to: "auth.uid()"
      via: "every branch must reference auth.uid() or is_list_member"
      pattern: "auth\\.uid\\(\\)|is_list_member"
---

<objective>
Close the data-isolation gap (UAT Test 1 major failure): authenticated users see every list with owner_id IS NULL across both accounts. Fix by removing the unconditional owner_id IS NULL branch from all six affected RLS policy clauses in lists_membership.sql and items_membership.sql, and resolving the legacy null-owner rows in the live DB via a human-action gate before the corrected policies are applied.

Purpose: RLS is the sole access gate (D-08 removed app-side filtering). The unconditional null branch must be eliminated so every surviving branch scopes by auth.uid() or is_list_member — no row is visible to a user who neither owns it nor joined it.
Output: Corrected migration files committed to source control + corrected policies applied to live Supabase + UAT Test 1 passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-list-sharing/10-UAT.md
@.planning/debug/null-owner-list-leak.md
@supabase/migrations/lists_membership.sql
@supabase/migrations/items_membership.sql
@src/stores/listsStore.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Resolve legacy null-owner rows in live Supabase before policy patch</name>
  <what-built>
    Nothing yet. This checkpoint must complete before the corrected policies are written or applied.
    The corrected policies drop the unconditional owner_id IS NULL branch, so any null-owner row with
    no is_list_member entry becomes invisible to all users after the patch. You must resolve those
    rows first — otherwise real lists could disappear.
  </what-built>
  <how-to-verify>
    Open Supabase Dashboard → SQL Editor for this project.

    Step 1 — Audit null-owner rows:
    Run this query to see what you're dealing with:

      SELECT id, name, created_at, owner_id, share_code
      FROM public.lists
      WHERE owner_id IS NULL
      ORDER BY created_at;

    Step 2 — Decide per row:

    For each null-owner list, choose one of three actions:

    A) DELETE IT — if it is a test list or stale data you do not need:
       DELETE FROM public.lists WHERE owner_id IS NULL AND id = '<row-id>';
       (Cascades: items on deleted lists are automatically removed if FK is ON DELETE CASCADE,
       otherwise delete items first: DELETE FROM public.items WHERE list_id = '<row-id>';)

    B) ASSIGN OWNER — if you know which Gmail account created it, look up the user_id:
       SELECT id, email FROM auth.users;
       Then:
       UPDATE public.lists SET owner_id = '<user-uuid>' WHERE id = '<row-id>';

    C) ADD MEMBERSHIP ROW — if you want both accounts to keep access without assigning an owner
       (the corrected policy keeps is_list_member as a branch):
       INSERT INTO public.list_members (list_id, user_id)
       VALUES ('<row-id>', '<user-uuid-account-1>'), ('<row-id>', '<user-uuid-account-2>');

    Step 3 — Verify no null-owner rows remain unresolved:

      SELECT COUNT(*) FROM public.lists WHERE owner_id IS NULL;

    The count does not have to be 0 — but any remaining null-owner rows MUST have corresponding
    list_members rows covering every user who needs access, because the corrected policy will
    only grant access via owner_id = auth.uid() OR is_list_member(id).

    If any remaining null-owner row has NO list_members entries and you still need access to it:
    either backfill owner_id or insert membership rows before proceeding.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "all null rows deleted" — you deleted all null-owner lists
    - "all null rows backfilled" — you assigned owner_id to all null-owner lists
    - "null rows have membership" — remaining null-owner lists have list_members rows for all needed users
    - or describe what you did, and Claude will confirm it is safe to proceed
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rewrite lists_membership.sql and items_membership.sql to remove unconditional IS NULL branch</name>
  <files>supabase/migrations/lists_membership.sql, supabase/migrations/items_membership.sql</files>
  <action>
    Rewrite both migration files. Do NOT use fenced code blocks in this action — the file content is described below as prose directives.

    LISTS_MEMBERSHIP.SQL — replace all three policy bodies as follows:

    The lists_select policy currently has three branches: owner_id IS NULL, auth.uid() = owner_id, is_list_member(id). Remove the owner_id IS NULL branch. The new USING clause for lists_select must be exactly two branches joined by OR: (select auth.uid()) = owner_id OR public.is_list_member(id). Roles remain: TO anon, authenticated.

    The lists_update policy has the same three-branch shape in both USING and WITH CHECK. Remove owner_id IS NULL from both. New shape for USING and WITH CHECK: (select auth.uid()) = owner_id OR public.is_list_member(id). Role: TO authenticated.

    The lists_delete policy has owner_id IS NULL OR (select auth.uid()) = owner_id. Remove owner_id IS NULL. New shape: USING ((select auth.uid()) = owner_id). Role: TO authenticated.

    Preserve all DROP POLICY IF EXISTS statements, the comment block at the top of the file, and all other SQL that does not involve the three-branch access shape.

    ITEMS_MEMBERSHIP.SQL — replace the USING and WITH CHECK clauses in all four item policies (items_select, items_insert, items_update, items_delete).

    Each currently has three branches: list_id IN (SELECT id FROM public.lists WHERE owner_id IS NULL), list_id IN (SELECT id FROM public.lists WHERE owner_id = (select auth.uid())), public.is_list_member(list_id). Remove the first branch (owner_id IS NULL subselect). New shape for every clause: list_id IN (SELECT id FROM public.lists WHERE owner_id = (select auth.uid())) OR public.is_list_member(list_id).

    The redeem_invite function in Section 2 of items_membership.sql is unchanged — do not touch it.

    Preserve all DROP POLICY IF EXISTS statements, comment blocks, role grants, and Section 2 (redeem_invite) exactly as-is.

    After writing both files: run `npx tsc --noEmit` to confirm no TypeScript regressions (these are pure SQL files but confirms the build is clean).

    IMPORTANT: The updated migration files are the source-of-truth for what will be applied to the live DB in Task 3. Review both files after writing to confirm:
    - Zero occurrences of "owner_id IS NULL" remain in any USING or WITH CHECK clause
    - Every remaining branch references (select auth.uid()) or is_list_member
    - redeem_invite function is intact
  </action>
  <verify>
    <automated>grep -n "owner_id IS NULL" C:/Users/mitch/Documents/CodingProjects/our-cart/supabase/migrations/lists_membership.sql C:/Users/mitch/Documents/CodingProjects/our-cart/supabase/migrations/items_membership.sql</automated>
  </verify>
  <done>
    - grep for "owner_id IS NULL" in both files returns zero matches in any USING or WITH CHECK position
    - lists_membership.sql lists_select USING is "(select auth.uid()) = owner_id OR public.is_list_member(id)"
    - items_membership.sql all four policies use "list_id IN (SELECT id FROM public.lists WHERE owner_id = (select auth.uid())) OR public.is_list_member(list_id)"
    - redeem_invite function body is unchanged
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Apply corrected migration files to live Supabase via SQL Editor</name>
  <what-built>
    Claude has updated supabase/migrations/lists_membership.sql and supabase/migrations/items_membership.sql
    with the corrected policy bodies (owner_id IS NULL branch removed from all clauses).
    You must now apply these corrected files to the live Supabase database.
  </what-built>
  <how-to-verify>
    Open Supabase Dashboard → SQL Editor for this project (https://app.supabase.com).

    Step 1 — Apply lists_membership.sql:
    Open the file supabase/migrations/lists_membership.sql in your editor.
    Copy the entire file contents.
    Paste into the SQL Editor and click Run.
    Expected: "Success. No rows returned." for each statement.

    Step 2 — Apply items_membership.sql:
    Open supabase/migrations/items_membership.sql.
    Copy the entire file contents.
    Paste into the SQL Editor and click Run.
    Expected: "Success. No rows returned." for the policy statements;
    redeem_invite CREATE OR REPLACE will show "Success. No rows returned." too.

    Step 3 — Verify policies updated:
    Go to Authentication → Policies → lists table.
    Click on the "lists_select" policy. The USING expression should NOT contain "owner_id IS NULL".
    It should read approximately: "(( SELECT auth.uid()) = owner_id) OR is_list_member(id)"

    Go to Authentication → Policies → items table.
    Click any items policy. The USING/WITH CHECK expression should NOT contain "owner_id IS NULL".
    It should contain "owner_id = (select auth.uid())" and "is_list_member".

    Step 4 — Quick sanity check in SQL Editor:
    Run: SELECT COUNT(*) FROM public.lists WHERE owner_id IS NULL;
    If count > 0: confirm those rows all have list_members entries (from Task 1).
    If they do not, STOP and complete Task 1 resolution before re-applying.
  </how-to-verify>
  <resume-signal>
    Type "policies applied" after both files are applied and the policy viewer confirms
    owner_id IS NULL is gone from the USING clauses, or describe any error output.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT re-verify — confirm data isolation holds for both Gmail accounts</name>
  <what-built>
    Corrected RLS policies are live. This checkpoint re-runs UAT Test 1 with both accounts
    to confirm the data-isolation gap is closed.
  </what-built>
  <how-to-verify>
    Step 1 — Account A (primary Gmail):
    Open the app (your deployed Vercel URL or http://localhost:5173).
    Sign in with Gmail Account A.
    Observe the list sidebar / landing page. Record all list names visible.

    Step 2 — Account B (partner Gmail):
    Open an incognito window (or a different browser).
    Sign in with Gmail Account B.
    Observe all list names visible.

    Step 3 — Cross-check:
    Lists owned by Account A must NOT appear for Account B (unless Account B was explicitly
    invited and joined via /invite/ flow).
    Lists owned by Account B must NOT appear for Account A (same caveat).
    Any shared list (joined via invite) should appear for BOTH accounts — this is correct behavior.

    Step 4 — Null-owner lists:
    If any null-owner lists existed and you assigned ownership (Task 1 option B):
    Only the owning account should see them.
    If you added membership rows (Task 1 option C):
    Both accounts should see them — this is correct (intentional shared access).
    If you deleted them (Task 1 option A):
    Neither account should see them — correct.

    UAT Test 1 passes when: no list appears for an account that neither owns it nor was
    explicitly invited to it.
  </how-to-verify>
  <resume-signal>
    Type "isolation verified" if both accounts see only their correct lists,
    or describe which lists are still leaking and under which account.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RLS USING clause → Postgres row filter | Every authenticated caller's query is filtered by these expressions; removing the open branch tightens the gate |
| Supabase Dashboard SQL Editor → live DB | Direct SQL execution writes to production; idempotent DROP/CREATE policy pattern minimizes risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Information Disclosure | lists_select USING (owner_id IS NULL branch) | mitigate | Remove unconditional branch; every remaining branch requires auth.uid() match or is_list_member membership — no unauthenticated/cross-user row exposure |
| T-10-06-02 | Information Disclosure | items_select/insert/update/delete USING (owner_id IS NULL subselect) | mitigate | Remove null-owner subselect branch from all four item policies; items inherit list-level access scope |
| T-10-06-03 | Denial of Service (self-inflicted) | Legacy null-owner rows inaccessible after patch | mitigate | Task 1 human-action gate resolves all null-owner rows before policy patch is applied; acceptance criteria confirm resolution |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. grep for "owner_id IS NULL" in lists_membership.sql and items_membership.sql returns zero matches in USING/WITH CHECK position
2. Human confirms policy viewer in Supabase Dashboard shows corrected clauses
3. UAT Test 1 re-run: both Gmail accounts see only their own lists + lists explicitly joined via invite
4. npx tsc --noEmit exits 0 (no regressions)
</verification>

<success_criteria>
- supabase/migrations/lists_membership.sql: zero occurrences of "owner_id IS NULL" in any USING or WITH CHECK clause
- supabase/migrations/items_membership.sql: zero occurrences of "owner_id IS NULL" in any USING or WITH CHECK clause; redeem_invite function intact
- Live Supabase policies updated — dashboard policy viewer confirms corrected USING expressions
- UAT Test 1 passes: Account A and Account B each see only their own lists and explicitly shared lists
- No new issues introduced (UAT Tests 2-6 still pass)
</success_criteria>

<output>
Create .planning/phases/10-list-sharing/10-06-SUMMARY.md when done
</output>
