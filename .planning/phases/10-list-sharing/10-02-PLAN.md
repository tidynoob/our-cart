---
phase: 10-list-sharing
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - supabase/migrations/list_members.sql
  - supabase/migrations/lists_membership.sql
  - supabase/migrations/items_membership.sql
autonomous: false
requirements:
  - SHARE-01
must_haves:
  truths:
    - "Live Supabase database has list_members table with composite PK (list_id, user_id)"
    - "is_list_member function exists in public schema with SECURITY DEFINER"
    - "redeem_invite function exists in public schema with SECURITY DEFINER"
    - "lists and items RLS policies have been replaced by the membership-aware versions"
    - "supabase db push exits 0 with no errors"
  artifacts:
    - path: "supabase/migrations/list_members.sql"
      provides: "Applied to live DB"
      contains: "SECURITY DEFINER"
    - path: "supabase/migrations/lists_membership.sql"
      provides: "Applied to live DB"
      contains: "is_list_member"
    - path: "supabase/migrations/items_membership.sql"
      provides: "Applied to live DB"
      contains: "redeem_invite"
  key_links:
    - from: "supabase db push"
      to: "live Postgres"
      via: "Supabase CLI migration apply"
      pattern: "Applied"
---

<objective>
Apply the three Phase 10 migration files to the live Supabase database. This is the BLOCKING gate: app code in Wave 3 depends on the live schema existing. Build/type checks pass without this push (types come from config, not the live DB), creating false-positive verification — do not skip this plan.

Purpose: Turn SQL files into live DB objects. Once complete, redeem_invite RPC is callable, RLS policies enforce membership, and item realtime events flow to members.
Output: Live DB state — list_members table exists, is_list_member and redeem_invite functions exist, lists/items policies are membership-aware.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-list-sharing/10-CONTEXT.md
@.planning/phases/10-list-sharing/10-RESEARCH.md
@supabase/migrations/list_members.sql
@supabase/migrations/lists_membership.sql
@supabase/migrations/items_membership.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push migrations to live Supabase database [BLOCKING]</name>
  <files>supabase/migrations/list_members.sql, supabase/migrations/lists_membership.sql, supabase/migrations/items_membership.sql</files>
  <read_first>
    - supabase/migrations/list_members.sql — verify it exists and contains SECURITY DEFINER before pushing
    - supabase/migrations/lists_membership.sql — verify it exists and contains is_list_member
    - supabase/migrations/items_membership.sql — verify it exists and contains redeem_invite
    - .planning/phases/10-list-sharing/10-CONTEXT.md §D-01, §D-04 — confirm expected schema artifacts
  </read_first>
  <action>
    Pre-flight: Confirm all three migration files exist in supabase/migrations/ and each contains the expected key SQL objects (SECURITY DEFINER, is_list_member, redeem_invite, PRIMARY KEY).

    Run: `supabase db push`

    If the push requires interactive confirmation (e.g., "Are you sure you want to push these migrations?"), answer yes. If it requires a DB password that is not in the environment, the task cannot proceed non-interactively — set autonomous: false and surface the credential requirement to the user.

    If push succeeds (exit 0), verify the live schema:
    `supabase db execute --sql "SELECT routine_name FROM information_schema.routines WHERE routine_schema = 'public' AND routine_name IN ('is_list_member', 'redeem_invite');"`
    Expected output: 2 rows.

    `supabase db execute --sql "SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'list_members';"`
    Expected output: 1 row (list_members).

    If push fails: Read the error output carefully. Common failure modes and resolutions:
    - "relation already exists" on list_members — the CREATE TABLE IF NOT EXISTS should prevent this; if it fires anyway, the migration may have been partially applied. Run `supabase db reset` only if confirmed safe by the user.
    - "function is_list_member does not exist" on lists_membership.sql — migration apply order issue. Confirm list_members.sql < lists_membership.sql alphabetically.
    - "policy already exists" — DROP POLICY IF EXISTS should prevent this; check migration file for missing drops.
  </action>
  <verify>
    <automated>supabase db execute --sql "SELECT COUNT(*) FROM information_schema.routines WHERE routine_schema = 'public' AND routine_name IN ('is_list_member', 'redeem_invite');" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits 0 with no errors
    - Live DB query for routines returns COUNT = 2 (both is_list_member and redeem_invite exist)
    - Live DB query for tables returns list_members in public schema
    - `supabase db execute --sql "SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='list_members';"` returns list_id, user_id, created_at
    - No app startup or TypeScript errors introduced (run `npx tsc --noEmit` to confirm)
  </acceptance_criteria>
  <done>supabase db push exits 0; live DB has list_members table + is_list_member + redeem_invite functions; TypeScript build still clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live schema objects exist and basic RLS is active</name>
  <what-built>supabase db push applied list_members table + is_list_member + redeem_invite functions + rewritten lists/items policies to live Supabase</what-built>
  <how-to-verify>
    Open the Supabase dashboard for this project (https://app.supabase.com → your project → Table Editor).

    Step 1 — Confirm list_members table: In Table Editor, verify "list_members" appears in the table list with columns list_id (uuid), user_id (uuid), created_at (timestamptz).

    Step 2 — Confirm functions: Go to Database → Functions. Verify "is_list_member" and "redeem_invite" appear in the public schema. Both should show Security: DEFINER.

    Step 3 — Confirm policies updated: Go to Authentication → Policies → lists table. The "lists_select" policy USING clause should show "is_list_member" or "owner_id IS NULL OR...". The old "lists_select" (owner-only) should be gone and replaced.

    Step 4 — Quick smoke test (optional but recommended): In Database → SQL Editor, run:
    `SELECT public.redeem_invite('NOTEXIST');`
    Expected: returns NULL (not an error). This confirms the function exists and handles unknown codes correctly.
  </how-to-verify>
  <resume-signal>Type "schema verified" if all four steps pass, or describe any discrepancies (e.g., "functions missing", "policies not updated").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase CLI → live DB | Migration push writes to production database; irreversible without rollback |
| DB SQL editor → SECURITY DEFINER functions | Direct SQL execution in dashboard bypasses app layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Elevation of Privilege | redeem_invite SECURITY DEFINER (live) | mitigate | Verified in human checkpoint: function shows Security=DEFINER in dashboard; GRANT TO authenticated only confirmed at migration write time (10-01). |
| T-10-07 | Tampering | Migration apply order | mitigate | Alphabetical filename ordering enforced (list_members < lists_membership < items_membership); acceptance criteria confirms is_list_member exists before policies that call it. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
After schema push:
1. `supabase db push` exits 0
2. Live DB routines query returns 2 (is_list_member + redeem_invite)
3. Live DB tables query returns list_members
4. Human checkpoint: dashboard confirms Security=DEFINER on both functions, lists_select policy shows is_list_member
</verification>

<success_criteria>
- supabase db push succeeds with no errors
- list_members table visible in Supabase dashboard with correct columns
- is_list_member and redeem_invite visible in Functions with Security=DEFINER
- lists_select policy updated (is_list_member appears in USING clause)
- Human reviewer types "schema verified"
</success_criteria>

<output>
Create .planning/phases/10-list-sharing/10-02-SUMMARY.md when done
</output>
