---
phase: 10-list-sharing
plan: "04"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - src/components/ShareBanner.tsx
  - src/stores/listsStore.ts
autonomous: true
requirements:
  - SHARE-01
  - SHARE-02
must_haves:
  truths:
    - "ShareBanner constructs shareUrl as /invite/${listCode} not /list/${listCode} (D-05)"
    - "listsStore.fetchLists no longer filters by .eq('owner_id', userId) — relies on RLS to return owned + member lists (D-08)"
    - "ShareBanner.test.tsx /invite/ assertion now passes GREEN (was RED after Wave 0)"
    - "Partner's redeemed list appears in their sidebar after fetchLists is called"
  artifacts:
    - path: "src/components/ShareBanner.tsx"
      provides: "Invite URL construction (D-05)"
      contains: "/invite/"
    - path: "src/stores/listsStore.ts"
      provides: "Membership-aware list fetch (D-08)"
  key_links:
    - from: "src/components/ShareBanner.tsx shareUrl"
      to: "/invite/${listCode}"
      via: "template literal on line 14"
      pattern: "/invite/\\$\\{listCode\\}"
    - from: "src/stores/listsStore.ts fetchLists"
      to: "supabase .from('lists').select(...).order(...)"
      via: "Supabase client — no .eq('owner_id') filter"
      pattern: "from\\('lists'\\)"
---

<objective>
Repoint the ShareBanner URL from /list/ to /invite/ (one-line change, D-05) and remove the owner_id filter from listsStore.fetchLists so member lists appear in the sidebar (D-08).

Purpose: D-05 turns the existing share button into an invite link — without this change, sharing the link sends partners to ListPage where RLS blocks them (pre-join). D-08 makes member lists appear in the partner's sidebar after they redeem. Both changes are small and parallel with 10-03 (no shared files).
Output: ShareBanner.tsx line 14 changed; listsStore.ts fetchLists .eq() line removed.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-list-sharing/10-CONTEXT.md
@.planning/phases/10-list-sharing/10-RESEARCH.md
@.planning/phases/10-list-sharing/10-PATTERNS.md
@src/components/ShareBanner.tsx
@src/stores/listsStore.ts
@src/components/ShareBanner.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint ShareBanner URL to /invite/ (D-05)</name>
  <files>src/components/ShareBanner.tsx</files>
  <read_first>
    - src/components/ShareBanner.tsx — full file; line 14 is the only line to change
    - src/components/ShareBanner.test.tsx — the /invite/ABC12345 assertion added in Wave 0 that is currently RED; this task makes it GREEN
    - .planning/phases/10-list-sharing/10-PATTERNS.md §"src/components/ShareBanner.tsx" — shows current line 14 and the target change
    - .planning/phases/10-list-sharing/10-CONTEXT.md §D-05 — locked: change /list/ to /invite/ at line 14
  </read_first>
  <action>
    One-line change in src/components/ShareBanner.tsx line 14. Change:
    `const shareUrl = \`${window.location.origin}/list/${listCode}\``
    to:
    `const shareUrl = \`${window.location.origin}/invite/${listCode}\``

    No other changes. The copy button handler, Web Share handler, dismiss button, props interface, JSX structure — all remain exactly as-is. The comment on line 13 ("Construct the share URL...") can be updated to reference D-05: "// Construct the invite URL — /invite/:code triggers redemption flow (D-05)" but this is optional.
  </action>
  <verify>
    <automated>npx vitest run src/components/ShareBanner --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep "shareUrl" src/components/ShareBanner.tsx` shows `/invite/` (not `/list/`)
    - `npx vitest run src/components/ShareBanner` exits 0 — ALL 5 tests pass, including the Copy link /invite/ assertion that was RED in Wave 0
    - `grep "/list/" src/components/ShareBanner.tsx` returns no matches (the old URL is gone)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ShareBanner.tsx line 14 uses /invite/; all 5 ShareBanner tests pass GREEN including the /invite/ assertion; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove owner_id filter from listsStore.fetchLists (D-08)</name>
  <files>src/stores/listsStore.ts</files>
  <read_first>
    - src/stores/listsStore.ts lines 22-36 — current fetchLists; the .eq('owner_id', userId) line at line 28 is the only change
    - .planning/phases/10-list-sharing/10-RESEARCH.md §Pattern 7 — exact line to drop; note on userId param signature (keep it, don't cascade to callers); pitfall 3 warning (filter removal behavior change)
    - .planning/phases/10-list-sharing/10-PATTERNS.md §"src/stores/listsStore.ts" — current code vs target code
    - .planning/phases/10-list-sharing/10-CONTEXT.md §D-08 — locked: drop .eq('owner_id', userId), rely on RLS
  </read_first>
  <action>
    One-line removal in src/stores/listsStore.ts. In the fetchLists action, remove the `.eq('owner_id', userId)` chain line (line 28). The Supabase query chain becomes:
    `supabase.from('lists').select('id, name, share_code, owner_id, created_at').order('created_at', { ascending: false })`

    Keep the `userId` parameter in the `fetchLists: async (userId)` signature — removing it would require updating LandingPage and AppShell callers (cascading change not in scope per D-08 discretion recommendation). The parameter becomes unused; add a comment: `// userId kept for API compatibility — RLS now handles access filtering (D-08)` or use `_userId` prefix to silence TypeScript unused-parameter warnings if the tsconfig enforces that.

    No other changes to listsStore.ts. The error string 'Failed to load lists', the set() calls, data ?? [], and order direction all remain identical.
  </action>
  <verify>
    <automated>npx vitest run src/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "eq('owner_id'" src/stores/listsStore.ts` returns no matches (filter removed)
    - `grep "fetchLists" src/stores/listsStore.ts` still shows `async (userId)` or `async (_userId)` — signature preserved
    - `npx tsc --noEmit` exits 0 (no TS errors — if unused-param is a TS error, use _userId prefix)
    - `npx vitest run src/` exits 0 — full unit suite green, no regressions in listsStore tests
  </acceptance_criteria>
  <done>listsStore.fetchLists has no .eq('owner_id') filter; userId param kept for API compatibility; TypeScript clean; full unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShareBanner → clipboard/Web Share | Invite URL written to clipboard; user decides to share it |
| listsStore.fetchLists → Supabase RLS | Unfiltered query relies entirely on RLS to scope results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | fetchLists filter removal | mitigate | RLS is the sole access boundary post-D-08. If RLS migration (10-01) has NOT been pushed (10-02 blocked), the unfiltered query would return zero rows for authenticated users (existing RLS still in effect — owner-only). No data leak from removing the app-level filter before the schema push. |
| T-10-04-02 | Tampering | ShareBanner /invite/ URL | accept | URL contains only the list's share_code (nanoid(8), already public via the existing /list/:code route). Pointing to /invite/ instead of /list/ does not expose new information. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
After both tasks:
1. `grep "shareUrl" src/components/ShareBanner.tsx` shows /invite/
2. `grep "eq('owner_id'" src/stores/listsStore.ts` returns empty (filter gone)
3. `npx vitest run src/components/ShareBanner` — 5/5 tests pass (Copy link /invite/ test now GREEN)
4. `npx vitest run src/` — full suite green, no regressions
5. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- ShareBanner.tsx line 14 uses /invite/; all 5 ShareBanner tests green (Wave 0 RED assertion now GREEN)
- listsStore.fetchLists has no .eq('owner_id') filter; userId param kept
- Full unit suite green; TypeScript clean
- ShareBanner.test.tsx Wave 0 RED test is now GREEN — Nyquist gate satisfied for SHARE-02
</success_criteria>

<output>
Create .planning/phases/10-list-sharing/10-04-SUMMARY.md when done
</output>
