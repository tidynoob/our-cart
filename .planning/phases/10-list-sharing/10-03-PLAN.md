---
phase: 10-list-sharing
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - src/pages/InvitePage.tsx
  - src/router.tsx
autonomous: true
requirements:
  - SHARE-02
must_haves:
  truths:
    - "InvitePage.tsx exists: on mount calls supabase.rpc('redeem_invite', { p_share_code: code }), navigates to /list/:share_code with replace:true on success, shows invalid-invite text on null/error (D-03, D-04)"
    - "router.tsx registers /invite/:code inside ProtectedRoute but outside AppShell (D-03, D-06)"
    - "Unauthenticated partner flow works: ProtectedRoute stores returnTo=/invite/:code, OAuth round-trip, LandingPage reads returnTo and navigates back to InvitePage (D-06)"
    - "InvitePage.test.tsx todos are converted to real passing tests"
  artifacts:
    - path: "src/pages/InvitePage.tsx"
      provides: "Invite redemption page"
      contains: "redeem_invite"
    - path: "src/router.tsx"
      provides: "Route registration for /invite/:code"
      contains: "/invite/:code"
  key_links:
    - from: "src/router.tsx /invite/:code route"
      to: "src/pages/InvitePage.tsx"
      via: "element prop"
      pattern: "InvitePage"
    - from: "src/pages/InvitePage.tsx"
      to: "supabase.rpc('redeem_invite')"
      via: "useEffect on mount"
      pattern: "rpc.*redeem_invite"
    - from: "src/pages/InvitePage.tsx"
      to: "navigate('/list/:code')"
      via: "useNavigate + replace:true"
      pattern: "navigate.*replace.*true"
---

<objective>
Create InvitePage.tsx and wire it into router.tsx. Also activate the Wave 0 InvitePage test stubs into real passing tests now that the component exists.

Purpose: The InvitePage is the join mechanism — it is the only way a non-owner becomes a member. The router placement (inside ProtectedRoute, outside AppShell) enables the unauthenticated-partner OAuth flow (D-06) to work without any new auth code.
Output: src/pages/InvitePage.tsx, modified src/router.tsx, activated src/pages/InvitePage.test.tsx tests.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-list-sharing/10-CONTEXT.md
@.planning/phases/10-list-sharing/10-RESEARCH.md
@.planning/phases/10-list-sharing/10-PATTERNS.md
@src/router.tsx
@src/pages/InvitePage.test.tsx
@src/components/auth/ProtectedRoute.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create InvitePage.tsx and activate test stubs</name>
  <files>src/pages/InvitePage.tsx, src/pages/InvitePage.test.tsx</files>
  <read_first>
    - src/pages/ListPage.tsx lines 1-16 (imports), lines 38-44 (useParams/useEffect pattern), lines 92-114 (async useEffect fetch pattern) — InvitePage follows the same structure
    - src/components/auth/ProtectedRoute.tsx lines 10-16 — spinner markup to copy exactly
    - .planning/phases/10-list-sharing/10-RESEARCH.md §Pattern 6 (InvitePage component — full component code)
    - .planning/phases/10-list-sharing/10-PATTERNS.md §"src/pages/InvitePage.tsx" — analog imports, useParams destructuring, rpc call pattern, invalid state markup
    - .planning/phases/10-list-sharing/10-PATTERNS.md §"src/pages/InvitePage.test.tsx" — renderAtRoute helper, vi.mocked usage, all 5 test scenarios
    - src/pages/InvitePage.test.tsx — current stub state (it.todo items to convert to real tests)
  </read_first>
  <behavior>
    - Test 1: renders spinner (element with class `animate-spin`) immediately before RPC resolves — mock rpc returns a never-settling promise
    - Test 2: valid RPC response `{ data: { list_id: 'uuid-123', share_code: 'ABC12345' }, error: null }` → component navigates to `/list/ABC12345`
    - Test 3: navigate called with `{ replace: true }` on success (same scenario as test 2)
    - Test 4: RPC returns `{ data: null, error: null }` → renders text "This invite link is invalid or has expired."
    - Test 5: RPC returns `{ data: null, error: { message: 'db error' } }` → renders same invalid text
  </behavior>
  <action>
    PART A — Create src/pages/InvitePage.tsx:
    Imports: `useEffect, useState` from 'react'; `useParams, useNavigate` from 'react-router-dom'; `supabase` from '@/lib/supabase' (use @/ alias).
    State: `const [invalid, setInvalid] = useState(false)`.
    Params: `const { code } = useParams<{ code: string }>()`.
    Navigate: `const navigate = useNavigate()`.
    useEffect deps: `[code, navigate]`. Guard: if (!code) { navigate('/'); return }. Async inner: call `supabase.rpc('redeem_invite', { p_share_code: code })`, destructure `{ data, error }`. On `error || !data`: setInvalid(true). On success: `navigate('/list/${data.share_code}', { replace: true })`.
    Render invalid state: `<div className="min-h-screen flex flex-col items-center justify-center p-4 gap-4"><p className="text-sm text-muted-foreground">This invite link is invalid or has expired.</p><a href="/" className="text-sm text-blue-600 underline">Back to home</a></div>`
    Render loading/spinner (default): exact markup from ProtectedRoute.tsx lines 10-16: `<div className="min-h-screen flex items-center justify-center"><div className="animate-spin rounded-full h-8 w-8 border-b-2 border-gray-900" /></div>`.
    Export: `export default function InvitePage()`.

    PART B — Activate InvitePage.test.tsx:
    Convert all `it.todo(...)` stubs into real `it(...)` tests. Add import for InvitePage from './InvitePage'. Add `renderAtRoute` helper (MemoryRouter + Routes + Route at `/invite/:code` + stub routes for `/list/:code` and `/`). Use `vi.mocked(supabase.rpc)` to control resolved values in each test. Use `waitFor` for async navigation assertions. Tests must pass against the new InvitePage.tsx component.

    Do NOT add store imports to InvitePage.tsx — redeem is a direct supabase.rpc call with no store state (per D-03/PATTERNS.md).
  </action>
  <verify>
    <automated>npx vitest run src/pages/InvitePage --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/InvitePage.tsx exists and contains "redeem_invite" and "animate-spin" and "navigate.*replace" (in source)
    - `npx vitest run src/pages/InvitePage` exits 0 with 5 passing tests (no todos remaining)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors in InvitePage.tsx)
    - Component uses `@/lib/supabase` import (not relative path)
    - useEffect dependency array is `[code, navigate]`
    - navigate on success uses `{ replace: true }` (back-button from /list/:code does not re-run RPC)
    - Invalid state renders "This invite link is invalid or has expired." text
    - Loading state renders element with class `animate-spin`
  </acceptance_criteria>
  <done>InvitePage.tsx created; all 5 InvitePage tests pass; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Register /invite/:code route in router.tsx</name>
  <files>src/router.tsx</files>
  <read_first>
    - src/router.tsx — full current file (22 lines); note ProtectedRoute children structure with AppShell nested inside
    - .planning/phases/10-list-sharing/10-PATTERNS.md §"src/router.tsx" — exact result shape (InvitePage as sibling to AppShell inside ProtectedRoute, NOT inside AppShell)
    - .planning/phases/10-list-sharing/10-RESEARCH.md §Pattern 6 (Router registration) — pitfall: do NOT put InvitePage inside AppShell
    - .planning/phases/10-list-sharing/10-CONTEXT.md §D-03, §D-06 — /invite/:code inside ProtectedRoute; no new auth-gate logic needed
  </read_first>
  <action>
    Modify src/router.tsx. Two changes only:
    1. Add import: `import InvitePage from '@/pages/InvitePage'` — after the existing imports, before the router export.
    2. Add route entry `{ path: '/invite/:code', element: <InvitePage /> }` as a sibling to the AppShell route object inside the ProtectedRoute children array — NOT nested inside the `{ element: <AppShell />, children: [...] }` object.

    The ProtectedRoute children array must look like:
    `[ { element: <AppShell />, children: [{ path: '/list/:code', element: <ListPage /> }] }, { path: '/invite/:code', element: <InvitePage /> } ]`

    Keep all other routes (`/`, `*` NotFoundPage) unchanged. The { path: '/' } LandingPage route stays outside ProtectedRoute.

    Rationale for placement (document in a comment above the InvitePage route entry): "InvitePage outside AppShell — full-screen spinner, no sidebar during join flow (per D-03/Pitfall 6)"
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/router.tsx contains "InvitePage" in import and in route element
    - `grep "'/invite/:code'" src/router.tsx` returns the route entry
    - The InvitePage route is NOT inside `children` of the AppShell route object — verify by reading the file structure
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/` exits 0 (all unit tests pass, no regressions)
    - No existing routes removed or moved
  </acceptance_criteria>
  <done>router.tsx registers /invite/:code inside ProtectedRoute but outside AppShell; TypeScript clean; full unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /invite/:code | User-supplied URL param `code` passed to supabase.rpc; untrusted input |
| ProtectedRoute → InvitePage | Auth gate ensures only authenticated users reach InvitePage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | InvitePage supabase.rpc call | mitigate | p_share_code passed to SECURITY DEFINER function which handles unknown codes by returning NULL (not exposing list existence). Invalid code → "invalid invite" state, no list data shown. |
| T-10-03-02 | Elevation of Privilege | /invite/:code route access | mitigate | Route sits inside ProtectedRoute — unauthenticated access redirects to login via returnTo flow (D-06). Attacker cannot call redeem_invite without a valid JWT. |
| T-10-03-03 | Spoofing | returnTo sessionStorage | accept | sessionStorage is same-origin only; cannot be set by third-party scripts. returnTo value is used for navigation only (navigate(returnTo)) — not executed as code. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
After both tasks:
1. `npx vitest run src/pages/InvitePage` exits 0 — 5 passing tests
2. `npx tsc --noEmit` exits 0
3. `npx vitest run src/` exits 0 — no regressions
4. `grep "invite" src/router.tsx` shows the /invite/:code route entry
5. Confirm InvitePage route is NOT inside AppShell children by reading router.tsx structure
</verification>

<success_criteria>
- InvitePage.tsx created; 5 InvitePage unit tests pass
- /invite/:code route registered inside ProtectedRoute, outside AppShell
- TypeScript build clean
- Full unit suite green (no regressions)
</success_criteria>

<output>
Create .planning/phases/10-list-sharing/10-03-SUMMARY.md when done
</output>
