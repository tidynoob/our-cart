---
phase: 04-real-time-sync
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/stores/itemsStore.test.ts
  - src/pages/ListPage.test.tsx
  - src/components/SyncStatus.test.tsx
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03

must_haves:
  truths:
    - "itemsStore.test.ts has a vi.mock for supabase that includes channel() and removeChannel() stubs, with capturedSubscribeCb to trigger status strings"
    - "ListPage.test.tsx has the same channel/removeChannel mock so subscribeToList does not throw during existing tests"
    - "SyncStatus.test.tsx exists and contains 3 describe-level it() assertions for live, connecting, and reconnecting states"
    - "npx vitest run --reporter=verbose exits 0 (existing tests pass, stubs compile, new stub tests pass)"
  artifacts:
    - path: "src/stores/itemsStore.test.ts"
      provides: "Extended mock with channel API + describe blocks for subscribeToList, unsubscribe, syncStatus, and mergeReducer behaviors"
    - path: "src/pages/ListPage.test.tsx"
      provides: "Extended supabase mock with channel/removeChannel so ListPage subscribeToList call does not error"
    - path: "src/components/SyncStatus.test.tsx"
      provides: "New test file — three state renders (live, connecting, reconnecting)"
  key_links:
    - from: "src/stores/itemsStore.test.ts"
      to: "src/stores/itemsStore.ts"
      via: "vi.mock captures subscribe callback to drive syncStatus assertions"
      pattern: "capturedSubscribeCb"
    - from: "src/components/SyncStatus.test.tsx"
      to: "src/components/SyncStatus.tsx"
      via: "vi.mock itemsStore selector, render component, assert text"
      pattern: "useItemsStore"
---

<objective>
Wave 0 test stubs — extend existing test files with the Supabase channel mock and write
failing/stub test cases for every SYNC-01/02/03 behavior in the validation map.

Purpose: The test infrastructure must exist before implementation so every task in Plans 02
and 03 can run `npx vitest run` and get a green/red signal. Stubs compile but tests that
depend on not-yet-implemented store actions will be written to be skipped (`.todo`) or
will fail until Plan 02 lands — that is intentional.

Output:
- `src/stores/itemsStore.test.ts` — extended with channel mock + describe blocks covering
  subscribeToList, unsubscribe, syncStatus transitions, and the mergeReducer (INSERT, UPDATE,
  DELETE, echo no-op, DELETE-only-id). Tests marked `.todo` where implementation does not
  yet exist.
- `src/pages/ListPage.test.tsx` — extended channel mock so existing tests keep passing;
  two new it() stubs for visibilitychange and online handlers (marked .todo).
- `src/components/SyncStatus.test.tsx` — new file; three state render assertions
  (marked .todo until SyncStatus.tsx exists).
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/04-real-time-sync/04-VALIDATION.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/04-real-time-sync/04-PATTERNS.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/04-real-time-sync/04-RESEARCH.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/04-real-time-sync/04-CONTEXT.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/ROADMAP.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/04-real-time-sync/04-VALIDATION.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/04-real-time-sync/04-PATTERNS.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/src/stores/itemsStore.test.ts
@C:/Users/mitch/Documents/CodingProjects/our-cart/src/pages/ListPage.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend itemsStore.test.ts with channel mock and SYNC behavior stubs</name>
  <files>src/stores/itemsStore.test.ts</files>
  <read_first>
    - src/stores/itemsStore.test.ts (full file — read the existing vi.mock shape, beforeEach state reset, describe/it naming convention)
    - .planning/phases/04-real-time-sync/04-PATTERNS.md §Test File Patterns — "src/stores/itemsStore.test.ts" section (exact mock extension code, capturedSubscribeCb pattern, beforeEach additions)
    - .planning/phases/04-real-time-sync/04-VALIDATION.md §Per-Task Verification Map (rows for SYNC-01 mergeReducer and SYNC-02/03 subscribeToList behaviors)
    - .planning/phases/04-real-time-sync/04-RESEARCH.md §Mocking Strategy for Unit Tests (full mock shape with capturedSubscribeCb)
  </read_first>
  <behavior>
    - After edit: vi.mock('@/lib/supabase') includes channel() returning {on, subscribe} and removeChannel — capturedSubscribeCb captures the subscribe callback
    - beforeEach resets syncStatus: 'connecting', channel: null alongside existing fields; capturedSubscribeCb = null
    - describe('itemsStore — subscribeToList') block has it.todo for: sets syncStatus live on SUBSCRIBED + calls fetchItems; sets syncStatus reconnecting on CHANNEL_ERROR; sets syncStatus reconnecting on TIMED_OUT; sets syncStatus reconnecting on CLOSED
    - describe('itemsStore — unsubscribe') block has it.todo for: calls supabase.removeChannel with the channel ref; sets channel to null
    - describe('itemsStore — mergeReducer') block has it.todo for: INSERT from partner appends item; INSERT echo (id already exists) is no-op; UPDATE replaces matching row by id; DELETE removes item by payload.old.id; DELETE with only id in payload.old handles partial object safely; multiple DELETE events from clearChecked remove correct rows
    - npx vitest run src/stores/itemsStore.test.ts exits 0 (all existing tests pass; .todo tests are skipped, not errors)
  </behavior>
  <action>
    Read src/stores/itemsStore.test.ts in full first — extract the current vi.mock shape (lines ~40-44), the beforeEach state reset (lines ~49-68), and the existing describe block structure.

    Extend the vi.mock('@/lib/supabase') factory to add channel and removeChannel alongside the existing from mock. Use the capturedSubscribeCb pattern from 04-PATTERNS.md: declare capturedSubscribeCb as a module-level let variable, implement mockChannelSubscribe to capture the callback and return an empty object (the channel), implement mockChannelOn to return `this` (enables chaining), add mockRemoveChannel as vi.fn(). The channel mock returns {on: mockChannelOn, subscribe: mockChannelSubscribe}.

    In beforeEach: add vi.clearAllMocks() if not present, add syncStatus: 'connecting' and channel: null to useItemsStore.setState(), and reset capturedSubscribeCb = null.

    Add describe blocks with it.todo for all behaviors listed in the behavior section — do not write implementations yet. Follow the naming convention from 04-PATTERNS.md §describe block + it() naming (include the SYNC requirement ID in each test description as a suffix).

    Per D-03 (CONTEXT.md): mergeReducer stubs reference INSERT upsert (skip if id present), UPDATE replace by id, DELETE remove by payload.old.id only. Per D-09: subscribeToList stubs reference syncStatus and channel fields living in the store.
  </action>
  <verify>
    <automated>npx vitest run src/stores/itemsStore.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - vi.mock('@/lib/supabase') in itemsStore.test.ts includes channel: vi.fn() and removeChannel: vi.fn() alongside the existing from mock
    - capturedSubscribeCb variable is declared and reset in beforeEach
    - Grep confirms: `grep -c "it.todo" src/stores/itemsStore.test.ts` returns at least 10
    - All existing passing tests still pass (exit code 0)
    - No TypeScript errors (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>itemsStore.test.ts compiles, existing tests pass, .todo stubs exist for all 11 SYNC behaviors in the validation map (subscribeToList x4, unsubscribe x2, mergeReducer x6)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ListPage.test.tsx channel mock + add SyncStatus.test.tsx</name>
  <files>src/pages/ListPage.test.tsx, src/components/SyncStatus.test.tsx</files>
  <read_first>
    - src/pages/ListPage.test.tsx (full file — read the existing vi.mock shape lines ~43-80, renderAtRoute helper lines ~82-91, beforeEach pattern lines ~171-178)
    - .planning/phases/04-real-time-sync/04-PATTERNS.md §"src/pages/ListPage.test.tsx" section (exact channel mock extension with setTimeout SUBSCRIBED trigger)
    - .planning/phases/04-real-time-sync/04-PATTERNS.md §"src/components/SyncStatus.test.tsx" section (full new file structure, selector mock pattern, three it() assertions)
    - .planning/phases/04-real-time-sync/04-RESEARCH.md §Mocking Strategy for Unit Tests
  </read_first>
  <behavior>
    - ListPage.test.tsx vi.mock('@/lib/supabase') includes channel and removeChannel; subscribe calls cb('SUBSCRIBED') via setTimeout(0) so tests don't hang
    - Existing ListPage tests still pass (exit code 0)
    - ListPage.test.tsx has two it.todo stubs: "calls fetchItems when document becomes visible (visibilitychange → visible) (SYNC-02)" and "calls fetchItems on window online event (SYNC-02)"
    - src/components/SyncStatus.test.tsx exists; it.todo for: renders 'Live' text when syncStatus is 'live' (SYNC-03); renders 'Connecting…' text when syncStatus is 'connecting' (SYNC-03); renders 'Reconnecting…' text when syncStatus is 'reconnecting' (SYNC-03)
    - npx vitest run --reporter=verbose exits 0 (all non-todo tests pass)
  </behavior>
  <action>
    For ListPage.test.tsx: read the existing supabase vi.mock and extend it to add channel and removeChannel. The channel mock's subscribe implementation must call setTimeout(() => cb('SUBSCRIBED'), 0) to trigger the status immediately in tests without hanging — this is the pattern from 04-PATTERNS.md. Do NOT break any existing tests. Add two it.todo blocks inside a new describe('ListPage — reconnect event handlers') block following existing describe naming conventions.

    For SyncStatus.test.tsx: create the file at src/components/SyncStatus.test.tsx using the full structure from 04-PATTERNS.md §SyncStatus.test.tsx. Mock useItemsStore as a vi.fn() that calls the selector with a fake state object (starting with {syncStatus: 'live'}). Each it.todo targets one of the three syncStatus states. Import SyncStatus from './SyncStatus' — this will fail to resolve until Plan 03 creates the file, but the file itself should compile correctly since the import path is valid as .todo stubs.

    Note: SyncStatus.test.tsx it() bodies are .todo — if SyncStatus.tsx does not yet exist, use `it.todo(...)` form rather than `it(description, () => { ... })` with a failing import, to keep the suite green.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Grep: `grep -c "removeChannel" src/pages/ListPage.test.tsx` is at least 1
    - Grep: `grep -c "visibilitychange" src/pages/ListPage.test.tsx` is at least 1
    - src/components/SyncStatus.test.tsx exists: `grep -c "it.todo" src/components/SyncStatus.test.tsx` returns at least 3
    - Full `npx vitest run --reporter=verbose` exits 0 — no pre-existing tests regress
  </acceptance_criteria>
  <done>All three Wave 0 gap files (itemsStore.test.ts, ListPage.test.tsx, SyncStatus.test.tsx) are updated/created, full test suite exits 0, validation map Wave 0 requirements are satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → source files | Vitest reads test and source files on disk; no network boundary in Wave 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | vi.mock factory shape | accept | Wave 0 only creates test stubs; mock shape is validated by test run (exit 0 confirms mock compiles and existing tests pass) |
| T-04-W0-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; existing vitest + testing-library already in package.json |
</threat_model>

<verification>
Full suite green: `npx vitest run --reporter=verbose` exits 0

Stub coverage: the three Wave 0 gaps from 04-VALIDATION.md are closed:
- itemsStore.test.ts has channel mock + .todo stubs for all 11 SYNC behaviors
- ListPage.test.tsx has channel mock + .todo stubs for visibilitychange and online
- SyncStatus.test.tsx exists with .todo stubs for three state renders
</verification>

<success_criteria>
- `npx vitest run --reporter=verbose` exits 0 with no regressions
- All Wave 0 file gaps from 04-VALIDATION.md are created/extended
- No new npm packages installed
- TypeScript (`npx tsc --noEmit`) exits 0
</success_criteria>

<output>
Create `.planning/phases/04-real-time-sync/04-01-SUMMARY.md` when done
</output>
