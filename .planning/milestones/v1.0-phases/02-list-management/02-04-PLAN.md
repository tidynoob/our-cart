---
phase: 02-list-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/NamePromptDialog.tsx
  - src/components/NamePromptDialog.test.tsx
autonomous: true
gap_closure: true
requirements:
  - LIST-04
must_haves:
  truths:
    - "Clicking Save in the NamePromptDialog persists the name to localStorage and closes the dialog"
    - "Refreshing the page after saving a name does NOT re-show the name prompt dialog"
  artifacts:
    - path: "src/components/NamePromptDialog.tsx"
      provides: "Name prompt dialog with working Save button"
      contains: "onClick"
    - path: "src/components/NamePromptDialog.test.tsx"
      provides: "Regression tests for name save and dialog dismiss"
      min_lines: 20
  key_links:
    - from: "src/components/NamePromptDialog.tsx"
      to: "localStorage"
      via: "localStorage.setItem on Save click"
      pattern: "localStorage\\.setItem"
    - from: "src/components/NamePromptDialog.tsx"
      to: "ListPage.tsx onNameSaved"
      via: "onNameSaved callback invocation"
      pattern: "onNameSaved"
---

<objective>
Fix the NamePromptDialog blocker bug where the Save button does not persist the user's name or dismiss the dialog, then add a regression test to prevent recurrence.

Purpose: The @base-ui/react Button primitive forces type="button" on the rendered element, overriding the explicit type="submit". The form's onSubmit handler never fires, so localStorage.setItem and onNameSaved never execute. This blocks LIST-04 (attribution display) since no name is ever saved.

Output: A working NamePromptDialog that saves the name and closes on Save click, plus a test file proving the behavior.
</objective>

<execution_context>
@C:\Users\mitch\Documents\CodingProjects\our-cart\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\mitch\Documents\CodingProjects\our-cart\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-list-management/02-HUMAN-UAT.md
@.planning/debug/name-dialog-persist-close.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix NamePromptDialog Save button and add regression test</name>
  <files>src/components/NamePromptDialog.tsx, src/components/NamePromptDialog.test.tsx</files>
  <read_first>
    - src/components/NamePromptDialog.tsx (the broken component, 57 lines)
    - src/components/ui/button.tsx (shadcn Button wrapping @base-ui/react ButtonPrimitive)
    - src/pages/ListPage.tsx lines 150-160 (how NamePromptDialog is consumed)
    - src/components/CreateListForm.test.tsx (test patterns used in this project)
  </read_first>
  <behavior>
    - Test 1: When user types a name and clicks Save, onNameSaved is called with the trimmed name
    - Test 2: When user types a name and clicks Save, localStorage.setItem is called with key "our-cart-name-{listId}" and the trimmed name
    - Test 3: Save button is disabled when the input is empty or whitespace-only
    - Test 4: When user types a name and presses Enter in the input, onNameSaved is called (Enter key submission still works)
  </behavior>
  <action>
    FIX (NamePromptDialog.tsx): The root cause is that the shadcn Button wraps @base-ui/react ButtonPrimitive, which forces type="button" on the rendered element, silently overriding the type="submit" prop. This prevents the form onSubmit from firing.

    Fix by adding an onClick handler to the Button that calls the same save logic currently in handleSubmit. Keep the form onSubmit handler as a fallback for Enter-key submission from the Input. The onClick handler must call e.preventDefault() to avoid any default button behavior, then execute the trim-check, localStorage.setItem, and onNameSaved call.

    Specifically in NamePromptDialog.tsx:
    - Add a handleSave function that takes no event arg: trims name, early-returns if empty, calls localStorage.setItem with key our-cart-name-{listId}, calls onNameSaved(trimmed).
    - Refactor handleSubmit to call e.preventDefault() then call handleSave().
    - Add onClick={handleSave} to the Button element. Keep type="submit" as a documentation intent marker (harmless even if overridden).

    Do NOT replace the shadcn Button with a plain HTML button element -- the shadcn Button styling and variants are used throughout the app and switching would create visual inconsistency. The onClick approach is the correct fix.

    TEST (NamePromptDialog.test.tsx): Create a new test file following patterns from CreateListForm.test.tsx. Use vitest + @testing-library/react. Mock localStorage.setItem via vi.spyOn(Storage.prototype, 'setItem'). Render NamePromptDialog with open=true, a known listId, and a vi.fn() for onNameSaved. All four behavior cases above must be covered.
  </action>
  <verify>
    <automated>npx vitest run src/components/NamePromptDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NamePromptDialog.tsx contains an onClick handler on the Button that calls the save logic directly
    - handleSave function exists and is called from both onClick and form onSubmit
    - All 4 tests in NamePromptDialog.test.tsx pass
    - npx tsc --noEmit exits with code 0
    - npx vitest run exits with 0 failures (full suite, no regressions)
  </acceptance_criteria>
  <done>Save button click triggers localStorage write and onNameSaved callback. Enter key in input also triggers save. Empty/whitespace names are rejected. All existing tests continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | User-controlled storage; name value is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | localStorage name value | accept | Name is display-only (initials badge), already trimmed before storage; no injection vector in current rendering (React auto-escapes). Low risk for 2-person private app. |
</threat_model>

<verification>
1. npx vitest run src/components/NamePromptDialog.test.tsx -- all new tests pass
2. npx vitest run -- full suite passes (no regressions)
3. npx tsc --noEmit -- TypeScript compiles clean
</verification>

<success_criteria>
- Save button in NamePromptDialog persists name to localStorage and invokes onNameSaved callback on click
- Enter key in the name input still triggers save via form submission
- Dialog closes after successful save (controlled by ListPage via userName state)
- Page refresh after saving name does NOT re-show the dialog
- All tests pass, TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/02-list-management/02-04-SUMMARY.md` when done
</output>
