---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - index.html
  - public/apple-touch-icon.png
  - src/components/AddItemBar.tsx
  - src/components/ItemRow.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Browser tab shows 'Our Cart' not 'temp-scaffold'"
    - "iOS theme-color meta is #ffffff"
    - "apple-touch-icon.png exists at public/apple-touch-icon.png (180x180 PNG)"
    - "Category Select trigger in AddItemBar expanded mode is at least 44px tall (h-11)"
    - "Category Select trigger in ItemRow edit mode is at least 44px tall (h-11)"
    - "'More details' toggle button has min-h-[44px] flex items-center classes"
    - "vercel.json at project root contains SPA rewrite rule (already correct — verified)"
    - "All previously passing tests still pass"
  artifacts:
    - path: "index.html"
      provides: "Fixed title, theme-color meta, apple-touch-icon link"
      contains: "<title>Our Cart</title>"
    - path: "public/apple-touch-icon.png"
      provides: "180x180 PNG for iOS home screen"
    - path: "src/components/AddItemBar.tsx"
      provides: "h-11 SelectTrigger, min-h-[44px] more-details toggle"
    - path: "src/components/ItemRow.tsx"
      provides: "h-11 SelectTrigger in edit mode"
  key_links:
    - from: "index.html"
      to: "public/apple-touch-icon.png"
      via: "<link rel='apple-touch-icon' href='/apple-touch-icon.png'>"
      pattern: "apple-touch-icon"
    - from: "src/components/AddItemBar.tsx"
      to: "src/components/ui/select.tsx"
      via: "SelectTrigger className override"
      pattern: "SelectTrigger.*h-11"
---

<objective>
Wave 1: Deploy polish and tap-target fixes (UX-01, UX-02, D-07 through D-14).

This plan delivers the deploy hardening and mobile tap-target compliance. All four work items are pure configuration or Tailwind class changes on existing files — no new logic, no new state, no new components.

Work items:
1. index.html — fix title (D-07), add theme-color meta (D-08), add apple-touch-icon link (D-08)
2. public/apple-touch-icon.png — generate 180x180 PNG from favicon.svg (D-08)
3. src/components/AddItemBar.tsx — bump SelectTrigger h-8 → h-11 (D-12), add min-h-[44px] flex items-center to "More details" toggle (D-14)
4. src/components/ItemRow.tsx — bump SelectTrigger h-8 → h-11 in edit mode (D-12)
5. vercel.json — already correct; verify existence and content (D-09)

Per D-10: No PWA manifest or service worker. Do not add these.
Per D-11/D-13: ItemRow display mode and checkbox are already compliant — no changes needed.

Purpose: Ship a properly titled, deployment-ready app with all interactive elements meeting the 44px touch target minimum.
Output: index.html updated, apple-touch-icon.png in public/, AddItemBar.tsx and ItemRow.tsx with h-11 triggers, full test suite green.
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/PROJECT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/ROADMAP.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-CONTEXT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-PATTERNS.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-UI-SPEC.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix index.html meta tags and generate apple-touch-icon.png</name>
  <files>index.html, public/apple-touch-icon.png</files>
  <read_first>
    - index.html — current full file (13 lines); must read before editing to know exact line positions
    - public/favicon.svg — source SVG (purple lightning bolt on transparent, 48x46 viewBox); apple-touch-icon adapts from this
    - .planning/phases/05-mobile-ux/05-PATTERNS.md §index.html section — exact before/after for all three changes
    - .planning/phases/05-mobile-ux/05-UI-SPEC.md §Deploy Polish Contract — theme-color value (#ffffff) and icon spec (180x180 PNG, white background)
  </read_first>
  <action>
    index.html changes (3 edits — all in the `<head>` block):

    1. Change `<title>temp-scaffold</title>` to `<title>Our Cart</title>` (per D-07)
    2. After the `<meta name="viewport" ...>` line, insert: `<meta name="theme-color" content="#ffffff" />` (per D-08)
    3. After the theme-color line, insert: `<link rel="apple-touch-icon" href="/apple-touch-icon.png" />` (per D-08)

    Do NOT add a manifest link. Do NOT add a service worker registration. Do NOT modify the viewport meta, charset, icon, or script tags.

    apple-touch-icon.png generation (per D-08 and UI-SPEC):
    Generate a 180x180 PNG at public/apple-touch-icon.png. Background: white (#ffffff). Center the lightning bolt from favicon.svg on the white canvas. The lightning bolt path fill is #863bff. Use one of these approaches in priority order:
    a) Write a Node.js script using the canvas API or sharp (check if sharp is available via `npm list sharp`). If sharp is not available, write a minimal Node script using the built-in `vm` + `canvas` module or create a simple SVG wrapper that embeds the favicon SVG centered in a 180x180 viewBox with a white rect behind it, then convert to PNG using any available CLI tool (Inkscape, librsvg, ImageMagick — check availability with `which inkscape rsvg-convert convert`).
    b) If no conversion tool is available, create a 180x180 SVG fallback at public/apple-touch-icon.svg with a white rect background and the centered lightning bolt — many iOS versions accept SVG for apple-touch-icon. Document this as a fallback in the plan summary.
    c) Last resort: use a plain purple 180x180 PNG generated via canvas in a Node script.

    The file must exist at public/apple-touch-icon.png (or .svg fallback) after this task.
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && grep -c "Our Cart" index.html && grep -c "theme-color" index.html && grep -c "apple-touch-icon" index.html && ls -la public/apple-touch-icon.* 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - index.html line with title: `<title>Our Cart</title>` (not "temp-scaffold")
    - index.html contains `<meta name="theme-color" content="#ffffff">`
    - index.html contains `<link rel="apple-touch-icon" href="/apple-touch-icon.png">`
    - index.html does NOT contain `<link rel="manifest">`
    - public/apple-touch-icon.png (or .svg fallback) exists — `ls public/apple-touch-icon.*` returns a file
  </acceptance_criteria>
  <done>index.html shows "Our Cart" in browser tab; apple-touch-icon.png/svg exists in public/</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bump SelectTrigger h-8 → h-11 and fix "More details" tap target; fill UX-02 test stubs</name>
  <files>src/components/AddItemBar.tsx, src/components/ItemRow.tsx, src/components/AddItemBar.test.tsx, src/components/ItemRow.test.tsx</files>
  <read_first>
    - src/components/AddItemBar.tsx — full file; line 116 is `<SelectTrigger className="h-8 flex-1"`, lines 94-100 are the "More details" `<button>` — read to confirm exact line content before editing
    - src/components/ItemRow.tsx — lines 183-186: `<SelectTrigger className="h-8 flex-1" onMouseDown={(e) => e.preventDefault()}>` — confirm onMouseDown is already present (must NOT be removed)
    - src/components/AddItemBar.test.tsx — read after Task 1 of Plan 01 created it; find the UX-02 stub to fill in
    - src/components/ItemRow.test.tsx — find the UX-02 stub added in Plan 01 Task 2
    - .planning/phases/05-mobile-ux/05-PATTERNS.md §AddItemBar and §ItemRow sections — exact before/after diffs for both files
  </read_first>
  <behavior>
    UX-02 AddItemBar SelectTrigger test: render AddItemBar with listId='list-1' addedBy='Test User', click the 'More details' button to expand, find the select trigger button (role="combobox" or data-slot="select-trigger"), assert its className includes 'h-11'
    UX-02 ItemRow SelectTrigger test: render ItemRow with isEditing=true, find the select trigger button, assert its className includes 'h-11'
    UX-02 "More details" toggle test (add if not already stubbed): render AddItemBar, find the 'More details' button by text, assert its className includes 'min-h-[44px]' and 'flex' and 'items-center'
  </behavior>
  <action>
    AddItemBar.tsx changes:

    1. Line 116: Change `className="h-8 flex-1"` to `className="h-11 flex-1"` on the SelectTrigger inside the expanded section (per D-12). This is the ONLY SelectTrigger in AddItemBar.

    2. Lines 94-100: The "More details" button currently has:
       `className="self-start text-sm text-muted-foreground hover:text-foreground"`
       Change to:
       `className="self-start min-h-[44px] flex items-center text-sm text-muted-foreground hover:text-foreground"`
       (per D-14)

    ItemRow.tsx changes:

    1. Line 184: Change `className="h-8 flex-1"` to `className="h-11 flex-1"` on the SelectTrigger (per D-12). The existing `onMouseDown={(e) => e.preventDefault()}` on that SelectTrigger MUST remain — it is part of the focus-scope pattern and is NOT a tap-target fix.

    Test stub fill-in:
    In src/components/AddItemBar.test.tsx, replace the UX-02 it.todo stub with a real test: render AddItemBar, simulate clicking 'More details', find the SelectTrigger (query by role="combobox" or test-id), assert className includes 'h-11'. Also add assertion that 'More details' button className includes 'min-h-[44px]'.
    In src/components/ItemRow.test.tsx, replace the UX-02 it.todo stub with a real test: render ItemRow with isEditing=true and defaultProps, find the SelectTrigger, assert className includes 'h-11'.

    No other changes to either component file. Do not touch the name Input, the Add button, the quantity Input, or any other element.
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run src/components/AddItemBar.test.tsx src/components/ItemRow.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "h-11" src/components/AddItemBar.tsx` returns the SelectTrigger line
    - `grep "h-11" src/components/ItemRow.tsx` returns the SelectTrigger line
    - `grep "min-h-\[44px\]" src/components/AddItemBar.tsx` returns the "More details" button line
    - `grep "h-8" src/components/AddItemBar.tsx` returns 0 matches (old class removed)
    - `grep "h-8" src/components/ItemRow.tsx` returns 0 matches (old class removed)
    - ItemRow.tsx SelectTrigger still has `onMouseDown={(e) => e.preventDefault()}`
    - UX-02 tests in AddItemBar.test.tsx and ItemRow.test.tsx pass (not todo)
    - `npx vitest run` exits 0 across the full suite
  </acceptance_criteria>
  <done>SelectTrigger h-11 in both components; "More details" toggle 44px; UX-02 stubs filled and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html → browser | Static file; no user input processed |
| public/ static assets → browser/CDN | Apple touch icon served as static file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | index.html theme-color | accept | theme-color is a display hint; reveals no sensitive data |
| T-05-02-02 | Spoofing | vercel.json SPA rewrite | accept | Rewrite serves index.html for all paths; API routes are handled by Supabase directly (no Vercel functions); no internal routes exposed |
| T-05-02-SC | Tampering | No new npm installs | accept | No package-manager installs in this plan; no slopcheck needed |
</threat_model>

<verification>
- `grep "Our Cart" index.html` — 1 match
- `grep "theme-color" index.html` — 1 match
- `grep "apple-touch-icon" index.html` — 1 match
- `ls public/apple-touch-icon.*` — file exists
- `grep "h-11" src/components/AddItemBar.tsx` — 1 match on SelectTrigger
- `grep "h-11" src/components/ItemRow.tsx` — 1 match on SelectTrigger
- `grep "min-h-\[44px\]" src/components/AddItemBar.tsx` — 1 match on "More details" button
- `cat vercel.json` — contains `"rewrites"` and `"destination": "/index.html"` (already correct per D-09)
- `npx vitest run` — 0 failures
</verification>

<success_criteria>
- Browser tab title reads "Our Cart"
- iOS home screen add prompts use the purple lightning bolt icon (180x180)
- All interactive elements in AddItemBar and ItemRow meet 44px minimum
- vercel.json SPA rewrite confirmed present (no change needed)
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/05-mobile-ux/05-02-SUMMARY.md` when done
</output>
