---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AddItemBar.test.tsx
  - src/components/ItemRow.test.tsx
autonomous: true
requirements:
  - UX-02
  - LIST-05

must_haves:
  truths:
    - "AddItemBar.test.tsx exists with failing stubs for all autocomplete behaviors (LIST-05)"
    - "AddItemBar.test.tsx has a stub for SelectTrigger h-11 class (UX-02)"
    - "ItemRow.test.tsx has a stub for SelectTrigger h-11 in edit mode (UX-02)"
    - "Full test suite still passes (no regressions from stub additions)"
  artifacts:
    - path: "src/components/AddItemBar.test.tsx"
      provides: "Test stubs for autocomplete (LIST-05) and tap-target (UX-02) behaviors"
    - path: "src/components/ItemRow.test.tsx"
      provides: "Added stub for SelectTrigger h-11 in edit mode (UX-02)"
  key_links:
    - from: "src/components/AddItemBar.test.tsx"
      to: "src/components/AddItemBar.tsx"
      via: "vitest unit test import"
      pattern: "import.*AddItemBar"
---

<objective>
Wave 0: Create test stubs for Phase 5 behaviors.

This plan establishes the test scaffolding before any implementation begins. All stubs start as `.todo()` or with `.skip` so the existing 78-test suite continues to pass. Wave 1 plans fill in the implementations; the stubs prevent regressions by requiring tests to be written before code is changed.

Per D-01 through D-04 (autocomplete behavior locked in CONTEXT.md), D-12 (SelectTrigger h-11 in AddItemBar and ItemRow), and the Wave 0 gap list in RESEARCH.md Validation Architecture.

Purpose: Ensure no implementation happens without a corresponding test expectation.
Output: Two test files with stubs; existing 78 tests remain green.
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/PROJECT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/ROADMAP.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-CONTEXT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-RESEARCH.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AddItemBar.test.tsx with autocomplete and tap-target stubs</name>
  <files>src/components/AddItemBar.test.tsx</files>
  <read_first>
    - src/pages/ListPage.test.tsx — Supabase mock shape (chained from → select → eq → order), vi.mock('@/lib/supabase'), channel mock structure; AddItemBar test must use the SAME 4-level chain
    - src/components/CreateListForm.test.tsx — simpler component test structure with userEvent.setup()
    - src/components/AddItemBar.tsx — current component shape: props (listId, addedBy, disabled), state (name, quantity, category, expanded, submitting), the Select component structure
    - src/components/ItemRow.test.tsx — @base-ui/react/checkbox mock and lucide-react mock patterns (AddItemBar uses lucide Plus; same mock pattern needed)
  </read_first>
  <behavior>
    - Stub 1 (LIST-05): "shows suggestions after typing a prefix" — render AddItemBar with listId prop, mock Supabase returning [{name:'Milk',category:'Dairy',quantity:'2'}], type 'mi' into the name input, expect suggestion item with text 'Milk' to appear in document
    - Stub 2 (LIST-05): "populates name, category, quantity fields on suggestion selection" — after typing prefix and suggestion appears, click/tap the suggestion, expect name input value to be 'Milk', category to be populated, quantity to be populated
    - Stub 3 (LIST-05): "does NOT auto-submit on suggestion selection (D-04)" — after suggestion selection, mockAddItem must NOT have been called
    - Stub 4 (LIST-05): "Escape key dismisses the dropdown" — type prefix, dropdown appears, press Escape, dropdown disappears but name input retains typed text
    - Stub 5 (LIST-05): "shows no suggestions when input is empty" — render with mock suggestion data, do not type, assert no listbox/suggestion item in document
    - Stub 6 (UX-02): "AddItemBar Category Select trigger has h-11 class" — render in expanded mode (click 'More details'), query the SelectTrigger element, assert className includes 'h-11'
    All 6 stubs use .todo() or it.skip(...) so they do not fail before implementation
    Supabase mock: vi.mock('@/lib/supabase') with the chained shape `from → select → eq → order` returning Promise.resolve({data: mockSuggestionData, error: null})
    itemsStore mock: vi.mock('@/stores/itemsStore') with useItemsStore selector returning { addItem: mockAddItem }
    @base-ui/react/checkbox: mock same way as ItemRow.test.tsx (Checkbox.Root as span + Checkbox.Indicator)
    lucide-react: mock Plus icon as svg with data-testid="plus-icon" (follow CreateListForm or ItemRow pattern)
  </behavior>
  <action>
    Create src/components/AddItemBar.test.tsx from scratch. Do not copy-paste the entire ListPage.test.tsx — build a focused file.

    File structure:
    1. Imports: describe, it, expect, vi, beforeEach from 'vitest'; render, screen from '@testing-library/react'; userEvent from '@testing-library/user-event'; AddItemBar from './AddItemBar'
    2. Three vi.mock() declarations: '@/lib/supabase' (4-level chain: from/select/eq/order), '@/stores/itemsStore' (useItemsStore selector pattern), '@base-ui/react/checkbox' (Checkbox.Root + Checkbox.Indicator), lucide-react (Plus as svg)
    3. Module-level variable: `let mockSuggestionData: Array<{name:string;category:string|null;quantity:string|null}> = []` and `const mockAddItem = vi.fn()`
    4. beforeEach: vi.clearAllMocks(), mockSuggestionData = []
    5. describe('AddItemBar autocomplete', () => { ... }) with 5 it.todo() stubs for LIST-05 behaviors
    6. describe('AddItemBar tap targets', () => { ... }) with 1 it.todo() stub for UX-02 SelectTrigger h-11

    The Supabase mock's order() must return `Promise.resolve({ data: mockSuggestionData, error: null })` — test cases set mockSuggestionData before rendering.

    Render helper: render AddItemBar with props `{ listId: 'list-1', addedBy: 'Test User' }`.
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run src/components/AddItemBar.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AddItemBar.test.tsx exists
    - Running the file produces 0 failures (all 6 stubs are .todo or .skip — they are counted as "todo" not "failed")
    - The file imports AddItemBar from './AddItemBar'
    - The Supabase mock uses the 4-level chain: from → select → eq → order
    - describe block names: "AddItemBar autocomplete" and "AddItemBar tap targets" (or equivalent)
  </acceptance_criteria>
  <done>AddItemBar.test.tsx exists with 6 stubs that do not cause test failures</done>
</task>

<task type="auto">
  <name>Task 2: Add SelectTrigger h-11 stub to ItemRow.test.tsx and run full suite</name>
  <files>src/components/ItemRow.test.tsx</files>
  <read_first>
    - src/components/ItemRow.test.tsx — entire file (understand existing describe blocks and mock structure before adding)
    - src/components/ItemRow.tsx — edit mode JSX (line 183): the SelectTrigger currently has className="h-8 flex-1"
  </read_first>
  <behavior>
    - New stub: "edit mode Category Select trigger has h-11 class" — render ItemRow with isEditing=true, query the button with role="combobox" or the SelectTrigger, assert className contains 'h-11'. Use it.todo() so it does not fail before implementation.
  </behavior>
  <action>
    Open src/components/ItemRow.test.tsx. Locate the last describe block or end of file. Add a new describe block:

    ```
    describe('ItemRow tap targets', () => {
      it.todo('edit mode Category Select trigger has h-11 class (UX-02)')
    })
    ```

    Do not modify any existing tests. Do not change any mock structure. Append only.

    After adding the stub, run the full Vitest suite to confirm all existing tests still pass:
    `npx vitest run` — should report 78+ tests passing, 0 failures (the new todo does not count as a failure).
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ItemRow.test.tsx contains a new it.todo stub for UX-02 SelectTrigger h-11
    - `npx vitest run` exits 0
    - Test output shows 0 failed tests (todos are not failures in Vitest)
    - Previously passing 78 tests still pass (count must not decrease)
  </acceptance_criteria>
  <done>Full suite green; ItemRow.test.tsx has the UX-02 SelectTrigger stub added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → production code | Test files read production code; no external data ingestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | npm/vitest test runner | accept | No new packages installed in this plan; Vitest already in devDependencies |
</threat_model>

<verification>
Full suite: `npx vitest run` — 0 failures, all existing tests pass, new todos appear as skipped not failed.
</verification>

<success_criteria>
- AddItemBar.test.tsx created with 6 .todo stubs covering LIST-05 and UX-02
- ItemRow.test.tsx extended with 1 .todo stub for UX-02
- `npx vitest run` exits 0 with 78+ passing, 0 failing
</success_criteria>

<output>
Create `.planning/phases/05-mobile-ux/05-01-SUMMARY.md` when done
</output>
