---
phase: "05"
plan: "04"
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified: []
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - LIST-05

must_haves:
  truths:
    - "App is deployed to Vercel and loads on a real phone browser"
    - "Browser tab shows 'Our Cart'"
    - "Adding to home screen on iOS shows purple lightning bolt icon"
    - "Category select in AddItemBar and ItemRow edit mode is visibly tall enough for one-handed tap"
    - "Typing a prefix in the name input shows matching suggestions below the input"
    - "Tapping a suggestion populates the fields and does not auto-submit"
    - "The list URL works on direct access (/list/:code) — no 404 on refresh"
    - "Full test suite passes on CI / local run"
  artifacts:
    - path: "vercel.json"
      provides: "SPA rewrite rule confirmed live in production"
    - path: "public/apple-touch-icon.png"
      provides: "180x180 PNG confirmed displaying on iOS home screen"
  key_links:
    - from: "Vercel deployment"
      to: "/list/:code route"
      via: "SPA rewrite in vercel.json"
      pattern: "vercel.json rewrites"
---

<objective>
Wave 3: Human verification checkpoint — phone walkthrough and Vercel deployment confirmation.

All implementation is complete by this point. This plan verifies the full phase goal: "The app is fast and frictionless on a phone — adding an item takes under 3 taps and works one-handed in a store."

Claude automates what it can (final test run, TypeScript check, git status), then pauses for human verification of the mobile UX and deployment.

Purpose: Confirm that Phase 5 requirements UX-01, UX-02, UX-03, and LIST-05 are met in production, not just in unit tests.
Output: Phase 5 signed off as complete.
</objective>

<execution_context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/PROJECT.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/ROADMAP.md
@C:/Users/mitch/Documents/CodingProjects/our-cart/.planning/phases/05-mobile-ux/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated checks and pre-deploy verification</name>
  <files></files>
  <read_first>
    - .planning/phases/05-mobile-ux/05-03-SUMMARY.md — confirm all implementation tasks completed successfully
  </read_first>
  <action>
    Run the following checks in sequence and report results:

    1. Full test suite: `npx vitest run` — must exit 0, must show 0 failures, must show all LIST-05 and UX-02 tests as passing (not todo)

    2. TypeScript check: `npx tsc --noEmit` — must exit 0

    3. Build check: `npm run build` — must succeed with no errors. This confirms the Vite build is clean before deployment.

    4. File existence checks:
       - `cat vercel.json` — confirm `"rewrites"` key and `"destination": "/index.html"` present
       - `ls public/apple-touch-icon.*` — confirm file exists
       - `grep "Our Cart" index.html` — confirm title fixed
       - `grep "theme-color" index.html` — confirm meta added
       - `grep "role=\"combobox\"" src/components/AddItemBar.tsx` — confirm autocomplete wired

    Report all results. If any check fails, fix the issue before proceeding to the human checkpoint task.
  </action>
  <verify>
    <automated>cd /c/Users/mitch/Documents/CodingProjects/our-cart && npx vitest run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0, 0 failures
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 (dist/ directory created)
    - vercel.json, apple-touch-icon.*, index.html, AddItemBar.tsx all pass file checks above
  </acceptance_criteria>
  <done>All automated checks pass; build is clean and ready to deploy</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phone walkthrough — verify Phase 5 requirements on a real device</name>
  <what-built>
    Phase 5 delivered: deploy polish (title, icon, SPA rewrite), tap-target fixes (SelectTrigger h-11, toggle min-h-[44px]), and autocomplete suggestions in the name input (one-time Supabase fetch on mount, local prefix filter, populate-on-selection, no auto-submit).
  </what-built>
  <how-to-verify>
    Deploy first (if not already deployed): push to main branch and confirm Vercel deploys successfully.

    Then on a real phone (iOS or Android mobile browser):

    **UX-01 — Phone-first layout:**
    1. Open the app URL in your mobile browser
    2. Confirm no horizontal scroll — content stays within the viewport width
    3. Confirm the list container is narrow and centered on desktop too (open in desktop browser)

    **UX-02 — Tap targets:**
    4. Navigate to a list with at least one item
    5. Expand "More details" in the AddItemBar — confirm the toggle is easy to tap one-handed (tall enough)
    6. Open the Category select in AddItemBar — confirm the trigger is visibly taller than before (44px minimum)
    7. Tap an item to enter edit mode — confirm the Category select in edit mode is also taller

    **LIST-05 — Autocomplete:**
    8. Add 2-3 items with different names (e.g., "Milk", "Eggs", "Bread")
    9. Tap the name input and type "mi" — confirm a "Milk" suggestion appears below the input
    10. Tap the "Milk" suggestion — confirm the name field populates with "Milk" and category/quantity pre-fill if set
    11. Confirm the item was NOT auto-submitted (Add button still required)
    12. Press the Add button — confirm item added successfully
    13. Type in an empty input — confirm no dropdown appears
    14. Type a prefix, then press Escape — confirm dropdown closes, typed text remains

    **UX-03 — Under 3 taps:**
    15. From the list view, count taps to add a new item:
        - Tap 1: tap name input
        - Type item name
        - Tap 2: tap Add button (or autocomplete suggestion)
        Confirm total is 2 taps (3 or fewer required)

    **D-07 — Title:**
    16. Confirm browser tab shows "Our Cart" (not "temp-scaffold")

    **D-09 — SPA rewrite:**
    17. Copy your list URL (e.g., https://your-app.vercel.app/list/ABCD1234)
    18. Open a new browser tab and paste the URL directly — confirm the list loads (no 404)

    **D-08 — iOS icon (iOS only):**
    19. In iOS Safari, tap Share → "Add to Home Screen"
    20. Confirm the icon shown is the purple lightning bolt (not a generic page icon)
  </how-to-verify>
  <verify>
    <human-check>All 20 verification steps completed. Tap targets feel comfortable one-handed. Autocomplete appears and populates correctly. No 404 on direct URL. Title shows "Our Cart".</human-check>
  </verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe any issues found (e.g., "autocomplete not appearing on iOS" or "Escape key not working") and Claude will create gap closure plans.
  </resume-signal>
  <done>Phase 5 verified on a real mobile device — all UX-01, UX-02, UX-03, LIST-05 requirements confirmed working in production</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verifier → production app | Human opens the production URL on a real device |
| Vercel CDN → browser | Static SPA bundle served from Vercel edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | Production URL shared during verify | accept | The list URL is the access credential by design (SHARE-03). Sharing it during testing is expected behavior. |
| T-05-04-SC | Tampering | No new packages | accept | No package-manager installs in this plan. |
</threat_model>

<verification>
Human verification is the gate for this plan. All automated checks pass in Task 1 before human review begins.
</verification>

<success_criteria>
- Full automated suite green (0 failures)
- Build succeeds
- Human confirms on a real phone: layout works one-handed, tap targets feel comfortable, autocomplete suggestions appear and pre-fill correctly, SPA routing works on direct URL, title is "Our Cart"
- Phase 5 marked complete in ROADMAP.md and STATE.md
</success_criteria>

<output>
Create `.planning/phases/05-mobile-ux/05-04-SUMMARY.md` when done
</output>
